Expected renderer properties for seq07.3 missing-unsafe-audit fixture:

- diagnostic identifies `unsafe lifetime @unsafe.cache_last_line` as an unsafe audit obligation.
- output includes `Generate unsafe audit metadata` as a help suggestion.
- output includes a patch preview replacing the opening `{` with:

+  reason = _
+ {
+     /// SAFETY: TODO: justify this unsafe lifetime block.

- applicability is `HasPlaceholders`.
- if the parser cannot produce the exact opening-brace range, no source edit should be emitted and the action should remain a host command.

Current full CLI status:

- `arcw verify fixtures/diagnostics/verifier-actions/missing-unsafe-audit.arcw`
  stops in `sema.typecheck` before verifier diagnostics are emitted because
  missing `reason` and missing `/// SAFETY:` are still hard typecheck errors.
- The verifier/LSP source-edit path is covered by seq07.3 unit tests.
- Full CLI convergence is tracked by
  `docs/reviews/requests/2026-06-30-seq-07.3.1-unsafe-audit-cli-repair-path-convergence.md`.
