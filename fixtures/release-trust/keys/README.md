# TEST ONLY - NOT PRODUCTION RELEASE CREDENTIALS

The deterministic Ed25519 seed in this directory is fixture-only material for
Seq-02.9 tests. It is intentionally public inside the test package so generated
fixture bytes are reproducible. It must not be installed into any production
release credential store, KMS, signing service, CI secret, or developer signing
profile.
