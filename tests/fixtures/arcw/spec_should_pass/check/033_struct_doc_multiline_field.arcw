pub struct AudioConfig {}
pub struct VisualConfig {}

pub struct Settings {
    /// Volume configuration.
    audio: AudioConfig,
    /// Nested visual configuration.
    visual: VisualConfig,
}

flow main() -> String { return "done" }
