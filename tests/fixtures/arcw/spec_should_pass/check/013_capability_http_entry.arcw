extern capability http {
    fn respond() effects { http.respond }
}

entry server @entry.http {
    route GET "/health" -> @flow.health
}

flow health() -> String effects { http.respond } {
    return "ok"
}
