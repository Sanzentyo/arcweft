struct Hoge {
    a: i32
    b: i32
    c: i32
}

fn make_hoge(a: i32, b: i32, c: i32) -> Hoge {
    Hoge {
        a,
        b,
        c,
    }
}

flow @flow.struct_edge struct_edge {
    let h = Hoge { a: 1i32, b: 2i32, c: 3i32 }
    let Hoge { a, b, c } = h
    return a
}
