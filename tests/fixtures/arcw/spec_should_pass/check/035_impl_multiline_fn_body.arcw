impl RouteInfo {
    fn label(self) -> String {
        if self.enabled {
            self.label
        } else {
            "disabled"
        }
    }
}

flow @flow.main main() -> String { return "done" }
