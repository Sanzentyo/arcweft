struct GameState {}

struct Summary {
    route: Ref<Flow>,
}

fn summarize(
    state: GameState,
    route: Ref<Flow>,
) -> Summary {
    Summary { route }
}

flow main() -> String { return "done" }
