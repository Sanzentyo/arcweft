extern capability cli { fn stdout(text: String) effects { stdio.write } }
entry cli @entry.main { goto @flow.main }
flow main() -> String effects { stdio.write } {
    cli.stdout("ok")
    return "done"
}
