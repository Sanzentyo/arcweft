extern capability http { type HttpRequest type HttpResponse fn text(status: i32, body: String) -> HttpResponse }
entry server @entry.http { route GET "/health" -> @flow.health }
flow @flow.health(req: HttpRequest) -> HttpResponse effects { http.respond } {
    return http.text(status = 200i32, body = "ok")
}
