entry cli @entry.main { goto @flow.main }

source @source.events: Source<Record, String> {
    from adapter.events()
    backpressure = latest
    replay = event_only
    privacy = transient
    on item ev => yield ev
}
flow @flow.main main { return "done" }
