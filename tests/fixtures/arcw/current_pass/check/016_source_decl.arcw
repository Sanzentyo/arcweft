source events: Source<String, String> {
    from adapter.events()
    backpressure = latest
    replay = event_only
    privacy = transient

    on item ev => yield ev
}

flow main() -> String {
    return "done"
}
