struct RouteInfo {
    label: String
}

pub trait Named {
    type Item
    fn name(self) -> String
}

impl Named for RouteInfo {
    type Item = RouteInfo

    fn name(self) -> String {
        self.label
    }
}

flow @flow.main main() -> String {
    return "done"
}
