struct IteratorItem {}
struct CaptureError {}

fn passthrough(frames: Stream<IteratorItem, CaptureError>) -> Stream<IteratorItem, CaptureError> {
    for frame in frames {
        yield frame
    }
}

flow generation_edge() -> String {
    return "done"
}
