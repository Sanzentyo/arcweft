entry cli @entry.main { goto @flow.main }

flow @flow.main main() -> String {
    if true {
        goto @flow.done
    }
    return "unreachable"
}

flow @flow.done done() -> String {
    return "done"
}
