entry cli @entry.main { goto @flow.main }

pub character alice {
    display = "Alice"
}

flow main() -> String {
    alice: hello[mark @.shown]
    with:
        on mark(@.shown) => log.info("shown")
    return "done"
}
