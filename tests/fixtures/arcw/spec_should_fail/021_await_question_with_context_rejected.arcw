flow await_question_with_context -> ArcResult<Unit> {
    let bg = await asset.image(@asset:.room)
        .context("opening background failed")? with:
        pending p:
            log.info("loading")

    return Ok(())
}
