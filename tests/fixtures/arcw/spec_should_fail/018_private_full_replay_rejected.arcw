pub source @source.private_full: Source<IteratorItem, CaptureError> {
    from "fixture"
    backpressure = latest
    replay = full
    privacy = private

    on item frame => yield frame
}

flow done {
    return "done"
}
