extern capability fs { type FsError fn read_text(path: String) -> Need<Result<String, FsError>> effects { fs.read } }
flow main effects { fs.read } {
    let text = match (await fs.read_text("C:\Users\me\profile.json")) { .Ok(text) => text, .Err(_) => "missing" }
    return text
}
