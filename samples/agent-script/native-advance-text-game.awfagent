
fn native_advance_text_game() -> Result<Unit, AgentError>
effects { agent.observe, agent.act.semantic }
{
    observe()

    advance_text()

    return Ok(())
}

entry agent @entry.agent.main {
    controller = native_advance_text_game
}
