signal @signal.current_flow: Watch<Ref<Flow>>

entry game @entry.main {
    goto @flow.opening
}

flow @flow.opening opening effects { signal.write } {
    signal.set(@signal.current_flow, @flow.opening)
    return "opening"
}
