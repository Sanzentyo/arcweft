fn read_resource_value_smoke() -> Result<Unit, AgentError>
effects { agent.resource.read }
{
    let resource = read_resource("arcweft://session/cli/observation/latest.json")
    return Ok(())
}

entry agent @entry.agent.main {
    controller = read_resource_value_smoke
}
