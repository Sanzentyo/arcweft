#[agent(version = 1)]

agent @agent.native.invoke_action native_invoke_action()
effects { agent.observe, agent.act.semantic }
{
    observe()

    invoke(@target.sample.clipped_pulse, "action.inspect.clipped_pulse")

    return "done"
}
