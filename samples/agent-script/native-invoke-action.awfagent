
fn native_invoke_action() -> Result<Unit, AgentError>
effects { agent.observe, agent.act.semantic }
{
    observe()

    invoke(@target.sample.clipped_pulse, "action.inspect.clipped_pulse")

    return Ok(())
}

entry agent @entry.agent.main {
    controller = native_invoke_action
}
