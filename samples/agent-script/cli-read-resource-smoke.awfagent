fn read_resource_smoke() -> Result<Unit, AgentError>
effects { agent.resource.read, debug.record }
{
    let resource = read_resource("arcweft://session/cli/observation/latest.json")
    checkpoint("after-read-resource")
    return Ok(())
}

entry agent @entry.agent.main {
    controller = read_resource_smoke
}
