fn assertions_smoke() -> Result<Unit, AgentError>
effects {}
{
    let accepted = true
    let blocked = false

    expect(accepted, message = "accepted should be true")
    deny(blocked, message = "blocked should stay false")

    Ok(())
}

entry agent @entry.agent.main {
    controller = assertions_smoke
}
