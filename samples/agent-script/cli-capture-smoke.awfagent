fn capture_smoke() -> Result<Unit, AgentError>
effects { agent.capture }
{
    let shot = capture(
        viewport(),
        format = .png,
        kind = .color,
        name = "viewport",
    )
    return Ok(())
}

entry agent @entry.agent.main {
    controller = capture_smoke
}
