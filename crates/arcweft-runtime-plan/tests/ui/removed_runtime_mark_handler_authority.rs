use arcweft_runtime_plan::semantic_facts::{
    RuntimeDialogueApplication, RuntimeDialogueMarkHandler,
};

fn old_handler_surface(application: &RuntimeDialogueApplication) {
    let _ = application.mark_handlers;
}

fn main() {}
