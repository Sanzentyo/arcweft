use arcweft_lang_hir::reference::{
    HirBorrowExpr, HirDerefExpr, HirReferenceType, HirRegion,
};

fn main() {
    let _: Option<(HirRegion, HirBorrowExpr, HirDerefExpr, HirReferenceType)> = None;
}
