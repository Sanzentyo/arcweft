# Lang-01.3.1.2.3.2.1.1 return intake

Date: 2026-08-12

Inspected Git baseline:
`a38c736ba577172b1f4c3fe1a0c3e85443e97e6f` on `main`, equal to
`origin/main`. The working tree also contained the preserved, unstaged A1
production investigation listed in
[`2026-08-12-lang-01-3-a1-opaque-composite-checked-type-blocker.md`](2026-08-12-lang-01-3-a1-opaque-composite-checked-type-blocker.md).
No package-intake operation altered or staged that production work.

This note supersedes the blocked production-readiness conclusion in that
blocker note. Its test evidence and rejection of selected-case `Never`
refinement remain valid.

## Returned archive

- retained-byte authority:
  `docs/reviews/packages/zips/arcweft-lang-01.3.1.2.3.2.1.1-opaque-composite-checked-type-owner-reconciliation-correction-final-contract.zip`;
- searchable frozen mirror:
  `docs/reviews/packages/arcweft-lang-01.3.1.2.3.2.1.1-opaque-composite-checked-type-owner-reconciliation-correction-final-contract/`;
- byte length: `56,072`;
- SHA-256:
  `93af482a2914ca4a9e6b985aa7a09c040f569bd71141611dcaa4d579ac01640c`;
- members: 36 flat files, with no directory entries or redundant wrapper.

The retained ZIP is byte-identical to the attachment. All 36 extracted files
match their ZIP members.

## Validation performed and passed

- ZIP path, drive/root traversal, case-collision, symlink/reparse, CRC, sorted
  member, fixed timestamp/mode, and extension checks passed.
- `OPEN_QUESTIONS.md` is exactly `none`.
- `SOURCE_REQUEST.md` is byte-identical to the maintained repository request.
- The retained parent ZIP independently hashes to the required
  `4b15a5eaea31663a9323f41f75345b2acb6faa0ea3a61784eeeabd482a13966a`.
- The package validator passed with 36 members and 150 unique normative test
  rows.
- The executable reference model passed all 20 owner, value, composite,
  Option, and Result checks.
- The package's repository pin, current source, and local Git baseline all
  identify commit `a38c736ba577172b1f4c3fe1a0c3e85443e97e6f`.

Python validators were run with `uv run` and process-local `PYTHONUTF8=1`.

## Readiness decision

Classification: `READY_FOR_IMPLEMENTATION`.

The correction closes the opaque-composite gap with one final model:

- `RuntimeCheckedType::Opaque` retains a typed producer, semantic identity,
  and exact/producer-wide admission;
- `RuntimeValue::Opaque` carries producer-validated exact evidence and an
  opaque payload;
- complete recursive Result/Option/composite owners are retained, with no
  selected-case refinement or `Never` sentinel;
- producer-bearing accepted nominal rows replace runtime-facing producerless
  `Named` success paths;
- native acceptance and AWBC use the same owner relation;
- AWBC remains ABI 1 and moves codec 10 to 11 with fixed tags;
- session-save schema 2 moves to 3 as a hard cut; and
- the parent A1 gate is split into compile-clean A1.1 through A1.4 subgates.

The package preserves the accepted nominal-record layout, record-sequence
error, identity/slot/path, activation, View, and Stream decisions. Parent A2
may resume only after A1.4 validates.

## Post-intake A1.2 declaration-authority split

Production reconciliation found one external declaration surface that the
returned package describes semantically but does not close at Rust/API/codec
level. Current adapter-native and Rust-export descriptors cannot author the
mandatory opaque producer that must exist before accepted catalog publication.
Choosing that authority changes two public descriptor models, two serialized
schemas, proc-macro input, registration digests, generated source, and typed
errors.

The independently throwable correction request is
[`2026-08-12-lang-01.3.1.2.3.2.1.1.1-external-opaque-producer-declaration-authority-correction.md`](../reviews/requests/2026-08-12-lang-01.3.1.2.3.2.1.1.1-external-opaque-producer-declaration-authority-correction.md).
Until it returns, A1.1 and other already-closed implementation work may be
validated, but A1.2 cannot be accepted and A1.3/A1.4 must not be reported as
complete. This split does not reopen the returned core opaque owner/value,
composite, AWBC tag, or persistence decisions.

## Not run at intake

No production Rust was changed by this intake cut. Cargo formatting, checks,
Clippy, workspace tests, Tier 2, and structure audit were not rerun for the
archive-copy and evidence-note operation. They remain required by the A1
implementation gates.
