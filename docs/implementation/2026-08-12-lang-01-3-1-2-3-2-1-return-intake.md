# Lang-01.3.1.2.3.2.1 return intake

Date: 2026-08-12

Inspected clean Git baseline:
`99c33024f6d63dee7689155443e685e949e365da`, equal to `origin/main` before
intake. There are no Rust, Cargo, or workspace changes between the package's
target `2585f527b02808305b3a8cab0442eb522e8d0352` and this baseline.

This note supersedes the blocked production-readiness conclusion in
[`2026-08-11-lang-01-3-g1-2-a-representation-foundation-and-carrier-blocker.md`](2026-08-11-lang-01-3-g1-2-a-representation-foundation-and-carrier-blocker.md).
The accepted representation foundation at
`08bc30c0c8eac77152a42e92a5ca2f83280b94bc` remains unchanged.

## Returned archive

The attached duplicate-download filename was retained under its contractually
specified archive name:

- retained-byte authority:
  `docs/reviews/packages/zips/arcweft-lang-01.3.1.2.3.2.1-nominal-record-and-record-sequence-owner-reconciliation-correction-final-contract.zip`;
- searchable frozen mirror:
  `docs/reviews/packages/arcweft-lang-01.3.1.2.3.2.1-nominal-record-and-record-sequence-owner-reconciliation-correction-final-contract/`;
- byte length: `78,725`;
- SHA-256:
  `4b15a5eaea31663a9323f41f75345b2acb6faa0ea3a61784eeeabd482a13966a`;
- members: 39 files below one redundant archive-root wrapper.

The retained ZIP is byte-identical to the attachment, and all 39 extracted
files match their ZIP members.

## Validation performed and passed

- ZIP path safety, CRC, member enumeration, and extracted byte parity passed.
- `OPEN_QUESTIONS.md` is exactly `none`.
- The package directory validator passed with 44 decision rows, 48 inventory
  rows, 81 test rows, 21 added symbols, 39 files, and a valid manifest.
- The sealed-ZIP validator passed against the retained attachment bytes.
- The executable reference model passed all 23 checks.
- All three parent archive hashes match `PARENT_ARTIFACTS.sha256`:
  Lang-01.3.1.2.3, Lang-01.3.1.2.3.1, and Lang-01.3.1.2.3.2.

The validator was run with `PYTHONUTF8=1` through `uv run`; without that
process-local UTF-8 mode, Python's Windows CP932 default cannot decode the
package's UTF-8 documentation. A first directory-validation attempt after the
reference model also correctly rejected generated `__pycache__` as a
manifest-external file. The recorded passing run used a fresh extraction and
ran the validator before the reference model.

## Readiness decision

Classification: `READY_FOR_IMPLEMENTATION`.

The correction closes both result-changing gaps without compatibility fiction:

- the absent `RuntimeNominalRecordSchema` is not introduced; the final core
  executable descriptor is `RuntimeNominalRecordLayout`;
- the absent `RecordSeqError` is not introduced; existing `RuntimeSeqError`
  remains the sole record-column admission error and gains the specified
  variants and precedence;
- nominal checked types gain exact `TypeLayoutHash` identity;
- nominal initializer evaluation preserves authored order and scatters results
  into defining layout order through accepted one-based field IDs; and
- live `Clone`/Serde traits remain only for the parent compile-clean interval.

G1.2-A carrier, nominal-layout, admitted-construction, and shared-visitor work
is unblocked and must follow the returned A1–A6 deletion-driven order. G1.2-B
and later affine/View/Stream publication remain out of scope until that cut is
validated.

## Not run at intake

No production Rust was changed in this intake cut. Cargo formatting, checks,
Clippy, tests, Tier 2, and the structural audit were therefore not run here;
they are mandatory for the subsequent Rust implementation cut.
