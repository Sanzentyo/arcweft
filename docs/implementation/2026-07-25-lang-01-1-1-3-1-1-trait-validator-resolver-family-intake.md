# Lang-01.1.1.3.1.1 trait-validator/family return intake

Date: 2026-07-25

## Package verification

The returned archive was supplied directly at:

- `D:\sanze\Downloads\arcweft-lang-01.1.1.3.1.1-trait-validator-and-resolver-family-identity-reconciliation-final-contract.zip`
- byte length: `35,880`
- SHA-256:
  `58330347E6759B38770D512BCAA682A1B3949EF46AFF24462F45C23ED851BC63`

The ZIP has 12 members. Its filename-sorted `MANIFEST.sha256` has 11
non-self rows; every recorded SHA-256 and exact byte length matches the
corresponding member. `OPEN_QUESTIONS.md` is exactly `none`, and both the
README and final status classify the return as `READY_FOR_IMPLEMENTATION`.

The embedded request SHA-256 is
`23419A435D6B632AAB8ACB7E71D3FF7670E87306C032233C51D529C3C1D842AE`;
it matches the repository request exactly. The package also records the exact
accepted parent hashes:

- Lang-01.1.1.3:
  `4FD834564C458639CD4EBE46615E4EC79C54F91D686439AAAACCC7F2B3714B5E`;
- Lang-01.1.1.3.1:
  `0E4746ABD1589F0228ADC62A074DFC07EEC92F3DF4DBC432E58138FD21500F4C`.

All status, summary, contract, implementation-order, traceability, evidence,
test-matrix, and open-question sidecars are inside the archive. No adjacent
sidecar is required. The binary is not duplicated into Git: the verified
external path and digest are the retained intake evidence, as allowed for a
directly supplied archive.

## Intake status

`RETURNED_ACCEPTED_IMPLEMENTATION_READY_DEFERRED_TO_LANG_01_1_1_COHORT`.

This return closes the last design blocker recorded by the parent `.3.1`
intake. No follow-up request is required for trait validator identity, checked
joins, ambiguity identity, or callable-family projection.

The accepted final model is:

- `CallableValidator::Method(CallableMethodRole)` is the pre-check validator;
- `CallableMethodRole` has exactly `TraitRequirement`,
  `TraitImplementation`, and `Inherent`;
- the role owns validation behavior only, never structural or checked identity;
- the enclosing exact `Arc<CallableRecord>` remains accepted identity;
- pending/final checked shells retain that exact record and a
  `CheckedCallableId`;
- concrete conformance joins exact implementation and original requirement
  checked IDs;
- static witnesses target the exact requirement checked ID;
- ambiguity evidence is a sorted, deduplicated `Arc<[CheckedCallableId]>`;
- `CallableFamily::TraitMethod` remains observational accounting only, owned by
  `CallableRecord::family()` and retained by `ResolvedCallable`; and
- schema digest tag 16 is replaced directly by `Method` plus role tag `0`, `1`,
  or `2`, with no dual reader or format alias.

## Required deletion-driven authority switch

The current checkout still contains the exact obsolete authority named by the
contract: `TraitCallableId`, `TraitCallableSource`,
`TraitImplementationIndex`, `CallableCandidateId::TraitMethod`,
`CallableValidator::Trait`, `SignatureOrigin::Trait`, and trait ambiguity
payloads containing the old ID.

When the established Lang-01.1.1 dependency position is reached, implementation
must begin by removing their exports, constructors, variants, digest arms, and
old ambiguity constructors. Focused `cargo check` fallout is the consumer
inventory. Every caller is then repaired toward the accepted record, pending
checked shell, exact checked ID, conformance, and record/resolved family owners.
The old types must not be restored to make an intermediate check pass.

The package's Cuts A-H remain the implementation sequence:

1. finish parent structural declaration owners and exact accepted records;
2. make the obsolete trait identity and variants unavailable first;
3. install the role-only validator and record-coherence gate;
4. build exact pending checked shells and checked-ID trait catalog joins;
5. switch resolver, ambiguity, and typed signature origins;
6. switch family, facts, signature help, and work accounting;
7. switch conformance and compiler/LSP/Agent/persistent/runtime consumers; and
8. add compile-fail removal evidence, validate, audit, commit, and push.

No compatibility wrapper, identity translation map, deprecated variant,
parallel trait catalog, second resolver, source gate, source-text identity,
removed-syntax diagnostic, CSS path, or Takumi path is authorized.

## Dependency and working-copy disposition

This package does not change the current dependency order. Its production cut
belongs to Lang-01.1.1 after the active Proof typed syntax/HIR/project authority
and the preceding typed RichText boundary. It must not be mixed into the
current Proof public-switch working copy merely because its design is now
complete.

The return is nevertheless part of the closed Lang-01.1.1 implementation
input. When that cohort starts, `.3`, `.3.1`, and this correction are
implemented as one final-model authority series rather than as three competing
catalogs or compatibility stages.

## Request disposition and validation

The repository request
[`2026-07-25-lang-01.1.1.3.1.1-trait-validator-and-resolver-family-identity-reconciliation.md`](../reviews/requests/2026-07-25-lang-01.1.1.3.1.1-trait-validator-and-resolver-family-identity-reconciliation.md)
is fulfilled by this verified return and must not be sent again.

This intake changes documentation only. No Rust validation is claimed for the
future production switch. The focused, workspace, Tier 2, compile-fail, digest,
and structural-audit gates remain exactly those specified by the archive.
