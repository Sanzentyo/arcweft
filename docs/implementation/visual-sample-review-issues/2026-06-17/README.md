# Visual Sample Review Issues 2026-06-17

This directory keeps native rich-text capture evidence for the ongoing
`samples/rich-text-full-grammar.arcw` review. Large full observation JSON files
are intentionally not checked in; regenerate them with the commands below when
object metadata needs to be inspected.

| ID | Finding | Evidence | Reproduction |
| --- | --- | --- | --- |
| `SVR-2026-06-17-001` | Native shaping with default ligatures made Latin text in rich-text/effect samples look broken. The layout model currently maps styled source ranges to per-character glyph slots before native shaping; ligature clusters such as `ffi` could overlap multiple slots and be submitted repeatedly. Native body/ruby shaping now disables `liga`/`clig`, fixing duplicated words such as `offfset`/`efffect` while leaving a shaping-aware horizontal layout as a remaining improvement. | Before: [`full-grammar-effects-object-step16.png`](full-grammar-effects-object-step16.png); after: [`full-grammar-effects-object-after-latin.png`](full-grammar-effects-object-after-latin.png). | `cargo run -p arcweft-cli --quiet -- agent observe samples\rich-text-full-grammar.arcw --image png --out docs\implementation\visual-sample-review-issues\2026-06-17\full-grammar-effects-object-after-latin.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.6 --capture-time 0.25` |
| `SVR-2026-06-17-002` | Font-family and shader sample lines exposed the broader non-shaped horizontal layout limitation: Latin advances were proportional enough in framebuffer submission after the first fix, but native measurement/debug geometry still used Sans I/O heuristic origins. Native visual-plan glyph placements, text-run bounds, glyph-cluster bounds, and object/debug crops now reuse glyphon shaped horizontal advances for ordinary non-ruby horizontal runs. Full line breaking and ruby-base allocation remain Sans I/O responsibilities. | Before: [`full-grammar-shader-object-step16.png`](full-grammar-shader-object-step16.png); after: [`full-grammar-decoration-object-after-shaped-latin.png`](full-grammar-decoration-object-after-shaped-latin.png). | `cargo run -p arcweft-cli --quiet -- agent observe samples\rich-text-full-grammar.arcw --image png --out docs\implementation\visual-sample-review-issues\2026-06-17\full-grammar-decoration-object-after-shaped-latin.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.5 --capture-time 0.25` |
| `SVR-2026-06-17-003` | `[effect .shader id=soft_glow ...]` was previously only exposed as visual-plan metadata. Native capture now emits deterministic soft-glow glyph passes before the main glyph pass, so the shader sample has visible framebuffer output instead of a no-op renderer path. The full grammar regression now also reads the `shader` run's raw RGBA object crop and requires blue-tinted glow pixels, proving the shader is visible in native/debug capture rather than only surviving as metadata. | Fixed: [`full-grammar-shader-object-after-soft-glow.png`](full-grammar-shader-object-after-soft-glow.png). | `cargo run -p arcweft-cli --quiet -- agent observe samples\rich-text-full-grammar.arcw --image png --out docs\implementation\visual-sample-review-issues\2026-06-17\full-grammar-shader-object-after-soft-glow.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.7 --capture-time 0.25` |
| `SVR-2026-06-17-004` | Horizontal Latin was still laid out from deterministic Sans I/O width heuristics in native submission, so words such as `serif`, `style`, `transform`, and `effect` looked over-spaced even after ligature duplication was fixed. Native submission and native debug measurement now feed glyphon shaped advances back into horizontal glyph origins for ordinary non-ruby horizontal runs, while leaving ruby-base allocations and vertical text untouched. | Fixed decoration: [`full-grammar-decoration-object-after-shaped-latin.png`](full-grammar-decoration-object-after-shaped-latin.png); fixed shader/effect family: [`full-grammar-shader-object-after-shaped-latin.png`](full-grammar-shader-object-after-shaped-latin.png). | `cargo run -p arcweft-cli --quiet -- agent observe samples\rich-text-full-grammar.arcw --image png --out docs\implementation\visual-sample-review-issues\2026-06-17\full-grammar-shader-object-after-shaped-latin.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.7 --capture-time 0.25` |
| `SVR-2026-06-17-005` | Current top-level review captures for all 11 `samples/rich-text-full-grammar.arcw` dialogue objects produce native PNGs with no runtime/type diagnostics; renderer diagnostics are now reported separately when a captured object contains an unresolved native custom effect. The wait/page/clear grammar object must be reviewed by page: page 0 only shows the pre-line-wait text, while pages 1-3 show the later wait, page, and clear aliases. The vertical extreme ruby object needs a tall viewport/textbox capture for meaningful inspection; the default object crop intentionally shows only the visible right-side column segment. | Top-level captures: [`full-grammar-object-00-current.png`](full-grammar-object-00-current.png) through [`full-grammar-object-10-current.png`](full-grammar-object-10-current.png); paged wait object: [`full-grammar-object-04-page-1-current.png`](full-grammar-object-04-page-1-current.png), [`full-grammar-object-04-page-2-current.png`](full-grammar-object-04-page-2-current.png), [`full-grammar-object-04-page-3-current.png`](full-grammar-object-04-page-3-current.png); tall vertical ruby: [`full-grammar-object-02-tall-current.png`](full-grammar-object-02-tall-current.png). | `cargo run -p arcweft-cli --quiet -- agent observe samples\rich-text-full-grammar.arcw --image png --out docs\implementation\visual-sample-review-issues\2026-06-17\full-grammar-object-00-current.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.0 --capture-time 0.25` |
| `SVR-2026-06-17-006` | Native capture previously dropped renderer diagnostics produced during glyph submission, so unresolved custom effects looked like successful no-ops in Agent JSON. Native framebuffer/color/mask/object-id captures now carry `NativeVisualDiagnostic` values through the CLI, and the native default host registry renders the sample `[.sparkle amp=2px]custom effect[/]` span instead of treating it as unresolved. Unknown custom IDs still surface as Agent diagnostics. | Current `.sparkle` capture: [`full-grammar-object-06-current.png`](full-grammar-object-06-current.png). | `cargo run -p arcweft-cli --quiet -- agent observe samples\rich-text-full-grammar.arcw --json --image png --out target\agent-review\full-grammar-object-06-color.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.6 --capture-time 0.25` |
| `SVR-2026-06-17-007` | `[if flag]条件[else]代替[endif]` was lowered as host-event metadata only, so both local text branches were visible in resolved display text. `LineDisplayFrame` resolution now evaluates local text conditionals against `RuntimeLineContext`, treats missing bindings as false, suppresses inactive branch text/styles/interpolations/host events, and keeps conditional markers in `display_map.host_events` for debugging. The full grammar sample now renders only `cue: 代替` for the undefined `flag` path. | Fixed cue conditional: [`full-grammar-cue-conditional-fixed.png`](full-grammar-cue-conditional-fixed.png). | `cargo run -p arcweft-cli --quiet -- agent observe samples\rich-text-full-grammar.arcw --json --image png --out docs\implementation\visual-sample-review-issues\2026-06-17\full-grammar-cue-conditional-fixed.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.8 --capture-time 0.25` |
| `SVR-2026-06-17-008` | `.rotate angle=8deg` was accepted as a transform span but lowered to `rotate.degrees = 0`, because transform lowering only checked a positional `deg` key. Transform lowering now accepts both named `angle=...` / `deg=...` and positional angle tokens such as `[transform .rotate 10deg]`, so the full grammar rotate sample carries `rotate.degrees = 8000` and renders as an actual rotated run. | Fixed transform rotate: [`full-grammar-transform-rotate-fixed.png`](full-grammar-transform-rotate-fixed.png). | `cargo run -p arcweft-cli --quiet -- agent observe samples\rich-text-full-grammar.arcw --json --image png --out target\agent-review\goal-audit-continued\object-6-rotate-fixed.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.6 --capture-time 0.25` |
| `SVR-2026-06-17-009` | The full grammar sample now exercises the supported effect/transform metadata that had only partial sample coverage: transform `target`/`origin`, builtin `.arc`, effect `target`/`phase`/`state_scope`, explicit `[effect .jitter ...]`, and shader `phase=run_offscreen_pass`. Native observe reports diagnostics-free PNGs for the expanded inferred and explicit-family lines. While validating that metadata, `state_scope` was found leaking into custom effect params; effect lowering now treats it as descriptor metadata only, matching `target` and `phase`. Full grammar regression also reads the `typewriter` run's raw mask at `--capture-step 1` and compares it with the same step plus `--capture-time 0`, proving animated glyph-mask state is inspectable after a specified runtime step. | Expanded inferred line: [`full-grammar-expanded-effects-transforms.png`](full-grammar-expanded-effects-transforms.png); expanded explicit family: [`full-grammar-expanded-explicit-family.png`](full-grammar-expanded-explicit-family.png). | `cargo run -p arcweft-cli --quiet -- agent observe samples\rich-text-full-grammar.arcw --json --image png --out docs\implementation\visual-sample-review-issues\2026-06-17\full-grammar-expanded-effects-transforms.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.6 --capture-time 0.25` |
| `SVR-2026-06-17-010` | `.host` now has a concrete success path instead of being only a diagnostic/no-op path: `[.host id=sparkle ...]` canonicalizes as an effect span, lowers its `id`/`effect`/`name` metadata to the renderer registry id, and native dispatches it through the default `sparkle` registry entry with no diagnostics. The full grammar sample includes the host-dispatched span while keeping unknown custom ids diagnostic-only. | Fixed host dispatch: [`full-grammar-host-effect-dispatch.png`](full-grammar-host-effect-dispatch.png). | `cargo run -p arcweft-cli --quiet -- agent observe samples\rich-text-full-grammar.arcw --json --image png --out docs\implementation\visual-sample-review-issues\2026-06-17\full-grammar-host-effect-dispatch.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.6 --capture-time 0.25` |
| `SVR-2026-06-17-011` | Vertical Latin was still treated as one sideways glyph per character, so `ABC` in a Japanese vertical run looked like separate stacked letters instead of an engine-rotated horizontal Latin run. Text layout now groups consecutive sideways Latin/Greek graphemes into one vertical cluster, sizes its bounds by the horizontal shaping advance, and the glyphon adapter maps shaped x offsets into vertical progression before applying `Rotate90Cw`. The full grammar vertical sample now observes `ABC` as one `sideways_cw` cluster with a run-shaped bbox. | Before: [`issue-011-sideways-latin-run-before.png`](issue-011-sideways-latin-run-before.png); fixed: [`issue-011-sideways-latin-run-fixed.png`](issue-011-sideways-latin-run-fixed.png). | `cargo run -p arcweft-cli --quiet -- agent observe samples\rich-text-full-grammar.arcw --json --image png --out docs\implementation\visual-sample-review-issues\2026-06-17\issue-011-sideways-latin-run-fixed.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.6 --capture-time 0.25` |
| `SVR-2026-06-17-012` | Full grammar effect verification now checks rendered pixels, not only metadata. The native Agent regression reads raw RGBA object crops for `.wave`, `.shake`, `.sparkle`, and `.host id=sparkle`; `wave` is sampled at the same runtime step with two explicit animation times, while `shake`, custom sparkle, and host-dispatched sparkle are sampled at `--capture-step 1` and `--capture-step 2`. Each pair must differ in raw pixels while still reporting native object scope, content pixels, and capture timing metadata. | Raw RGBA evidence is generated under the test temp directory and intentionally not checked in. Existing PNG review captures for the same full grammar object remain [`full-grammar-expanded-effects-transforms.png`](full-grammar-expanded-effects-transforms.png) and [`full-grammar-host-effect-dispatch.png`](full-grammar-host-effect-dispatch.png). | `cargo test -p arcweft-cli --test check agent_observe_native_renderer_reports_full_grammar_sample_rich_text_constructs -- --exact --nocapture` |
| `SVR-2026-06-17-013` | Ruby placement was still too far from the body text in horizontal-under and vertical side-track cases because Arcweft treated the annotation/base bboxes as hard-separated rectangles. Chrome's HTML ruby rendering permits a small natural overlap at zero gap, so text layout now applies the same HTML-like natural overlap for horizontal over/under and vertical left/right ruby tracks before adding explicit `ruby_gap`. Native object capture also now resolves hidden rich-text child IDs through their parent textbox and native element, so typewriter-hidden text-combine clusters can be cropped at `--capture-time 0` with stable geometry and zero content pixels, then compared to a later visible capture. | Evidence is in native raw-crop regression output; PNG review captures should be regenerated for any visual audit that depends on ruby gap perception. | `cargo test -p arcweft-cli --test check agent_observe_native_renderer_writes_vertical_goal_clear_smoke_raw_crops -- --exact --nocapture` |
| `SVR-2026-06-17-014` | Custom text decoration coverage now includes registered `glyph_color` effects, not only placement effects. The native default `sparkle` registry entry supports `phase=glyph_color`; `samples/rich-text-effects-animation.arcw` includes `色sparkle`, and the Agent regression captures that run's raw RGBA object crop at a pinned step/time and requires red-tinted visible pixels with no renderer diagnostics. | Raw RGBA evidence is generated under the test temp directory and intentionally not checked in. The sample is covered by `samples/rich-text-effects-animation.arcw`. | `cargo test -p arcweft-cli --test check agent_observe_native_renderer_captures_combined_typewriter_animation_sample -- --exact --nocapture` |
| `SVR-2026-06-17-015` | Rich-text shaders now use a native shader registry rather than a hard-coded `soft_glow` branch. The default registry renders `soft_glow` and `warm_glow`, and `NativeOffscreenCaptureSession::shader_registry_mut` can register arbitrary shader IDs for Rust/native adapters. The effects animation sample includes `warm glow shader`; the Agent regression captures that run's raw RGBA object crop at a pinned step/time and requires warm-tinted visible pixels with no renderer diagnostics. A native unit test separately registers a custom `rose_glow` shader and verifies red-tinted framebuffer pixels. | Raw RGBA evidence is generated under the test temp directory and intentionally not checked in. The sample is covered by `samples/rich-text-effects-animation.arcw`. | `cargo test -p arcweft-player-native shader -- --nocapture`; `cargo test -p arcweft-cli --test check agent_observe_native_renderer_captures_combined_typewriter_animation_sample -- --exact --nocapture` |
| `SVR-2026-06-17-016` | Rich-text `.motion fn=...` now resolves through `RichTextMotionRegistry` rather than a hard-coded sampler/hash fallback. The default registry renders `breath_orbit` and `elastic_bloom`, and `NativeOffscreenCaptureSession::motion_registry_mut` can register arbitrary motion IDs for Rust/native adapters. Missing motion IDs emit `missing_motion_function`; a native unit test registers `snap_rise` and verifies framebuffer glyph pixels move, while the effects animation sample still captures `関数motion` at two pinned times and requires differing raw RGBA crops. | Raw RGBA evidence is generated under the test temp directory and intentionally not checked in. The sample is covered by `samples/rich-text-effects-animation.arcw`. | `cargo test -p arcweft-player-native motion -- --nocapture`; `cargo test -p arcweft-cli --test check agent_observe_native_renderer_captures_combined_typewriter_animation_sample -- --exact --nocapture` |
| `SVR-2026-06-17-017` | Text proxy objects are now regression-checked as real capture targets, not only metadata entries. The full grammar sample's `#[text_proxy] KeywordHit` defaults still resolve to role `keyword`, hit-test enabled, depth `4000`, and params, and the Agent regression now reads the proxy object's raw mask and object-id capture refs through `--read-uri`. The object-id readback must decode as raw RGBA and contain the proxy object's own `capture_refs.object_id_color`, so a parent run/page color can no longer satisfy the test. | Raw mask/object-id evidence is generated by `--read-uri` during the test and intentionally not checked in. The sample is covered by `samples/rich-text-full-grammar.arcw`. | `cargo test -p arcweft-cli --test check agent_observe_native_renderer_reports_full_grammar_sample_rich_text_constructs -- --exact --nocapture` |
| `SVR-2026-06-17-018` | Rich-text rendered pages are now first-class Agent objects. Agent observe emits `rich_text_page` children with `rich_text_ref.kind = text_page`, page-local range, hit region, page-aware capture refs, and object-id colors. The full grammar regression reads a page object's raw mask and object-id resources through `--read-uri`, requiring native object-scope pixels and an object-id tint that matches the page object's own color. | Raw mask/object-id evidence is generated by `--read-uri` during the test and intentionally not checked in. The sample is covered by `samples/rich-text-full-grammar.arcw`. | `cargo test -p arcweft-cli --test check agent_observe_native_renderer_reports_full_grammar_sample_rich_text_constructs -- --exact --nocapture` |
| `SVR-2026-06-17-019` | Authored rich-text lines are now first-class Agent objects. Agent observe emits `rich_text_line` children with `rich_text_ref.kind = text_line`, line-local range, hit region, page-aware capture refs, and object-id colors. Line ranges split at authored hard breaks and page boundaries, and native captures use the text/ruby element set that overlaps the line rather than a fallback rectangle. The full grammar regression reads a line object's raw mask and object-id resources through `--read-uri`, requiring native object-scope pixels and an object-id tint that matches the line object's own color. | Raw mask/object-id evidence is generated by `--read-uri` during the test and intentionally not checked in. The sample is covered by `samples/rich-text-full-grammar.arcw`. | `cargo test -p arcweft-cli --test check agent_observe_native_renderer_reports_full_grammar_sample_rich_text_constructs -- --exact --nocapture` |
| `SVR-2026-06-17-020` | Non-zero rich-text pages now have explicit Agent regression coverage for page and line objects. The clear-after-page fixture observes `object.dialogue.0.0.page.1` and `object.dialogue.0.0.line.1`, requires their capture refs to carry `?page=1`, then reads those color PNG resources back through `--read-uri` and verifies native object-scope pixels on page 1. | PNG evidence is generated by `--read-uri` during the test and intentionally not checked in. | `cargo test -p arcweft-cli --test check agent_observe_native_renderer_captures_clear_after_page_object -- --exact --nocapture` |
| `SVR-2026-06-17-021` | Nested rich-text proxy spans are now covered by the full grammar sample and regression. The sample defines both `KeywordHit` and `HoverHit`, nests them on `multi proxy`, and verifies that runtime lowering preserves both proxy objects with struct defaults plus inline overrides. Agent observe must report two separate `rich_text_proxy` objects for the same visible span, each with its own hit region, params, capture refs, and object-id raw tint. | Raw mask/object-id evidence is generated by `--read-uri` during the test and intentionally not checked in. The sample is covered by `samples/rich-text-full-grammar.arcw`. | `cargo test -p arcweft-runtime-plan nested_text_proxy_struct_attributes_accumulate_with_inline_overrides`; `cargo test -p arcweft-cli --test check agent_observe_native_renderer_reports_full_grammar_sample_rich_text_constructs -- --exact --nocapture` |
| `SVR-2026-06-17-022` | Canonical rich-text tooling now preserves parameters when the selector itself names a visible text proxy struct. `[.HoverHit tone=alert]...[/]` canonicalizes to `[object .HoverHit type=HoverHit tone=alert]...[/object]` instead of dropping `tone=alert`, and nested inferred proxy spans remain objects rather than being misclassified as effects. This keeps CLI and LSP source actions suitable for proxy objects that carry hit-test, depth, or adapter metadata through custom attributes. | Source-level canonicalization evidence is covered by focused tooling, CLI, and LSP regressions; no PNG artifact is needed because the failure was a source rewrite data-loss bug. | `cargo test -p arcweft-tooling canonical_rich_text_expands_nested_inferred_text_proxy_objects`; `cargo test -p arcweft-cli --test check fmt_canonical_rich_text_preserves_nested_proxy_params -- --exact --nocapture`; `cargo test -p arcweft-lsp code_actions_canonical_rich_text_preserve_nested_proxy_params` |
| `SVR-2026-06-17-023` | Text glyphs are now explicit Agent objects instead of being reachable only through glyph-cluster objects. Agent observe emits `rich_text_glyph` children with `rich_text_ref.kind = text_glyph`, text range, hit region, capture refs, orientation, vertical-form metadata, and independent object-id colors. Native raw mask/object-id crops for `object.dialogue.*.glyph.*` resolve through the same native glyph bounds as drawing, so debuggers can request a specific rendered text glyph as an image-backed object. | Raw mask/object-id evidence is generated by the focused CLI regression and intentionally not checked in. | `cargo test -p arcweft-agent-protocol observation_report_serializes_stable_snake_case_enums`; `cargo test -p arcweft-cli --test check agent_observe_json_reports_rich_text_display_objects -- --exact --nocapture`; `cargo test -p arcweft-cli --test check agent_observe_native_renderer_writes_vertical_text_glyph_raw_crops -- --exact --nocapture` |
| `SVR-2026-06-17-024` | Agent hit-testing now consumes the same rich-text hit regions used by observe/capture. `arcw agent hit-test` and MCP `arcweft.hit_test` return an `AgentHitTestReport` for a viewport coordinate, ranking hits by resolved depth and semantic specificity so nested custom proxy objects behave as real clickable/depth-aware text objects. The focused CLI regression observes nested `KeywordHit`/`HoverHit` proxies, hit-tests the measured proxy bbox center, and requires the deeper `HoverHit` object to be top-ranked while the shallower proxy remains in the candidate list. | Source-level and JSON evidence is generated by the focused CLI/MCP regressions; no PNG artifact is needed because the change verifies object/region routing rather than pixel generation. | `cargo test -p arcweft-cli --test check agent_hit_test_reports_depth_sorted_rich_text_proxy -- --exact --nocapture`; `cargo test -p arcweft-agent-mcp hit_test_tool_schema_requires_viewport_coordinate`; `cargo check -p arcweft-agent-protocol -p arcweft-agent-mcp -p arcweft-cli` |
| `SVR-2026-06-17-025` | Custom rich-text proxy `layer` metadata is now first-class object data instead of a forwarded custom param. `#[text_proxy(... layer=ui)]` and inline `layer=` / `object_layer=` lower to `RichTextObjectProxy.layer`, Agent observe reports it as `rich_text_ref.object_layer`, and hit-test regions report it as `region.proxy_layer`. This keeps text proxies aligned with image/model-like debug objects that have semantic layer, depth, hit-test, and capture identity. | Source-level and JSON evidence is generated by focused runtime-plan and CLI regressions; no PNG artifact is needed because the change verifies proxy metadata routing rather than pixel generation. | `cargo test -p arcweft-runtime-plan nested_text_proxy_struct_attributes_accumulate_with_inline_overrides`; `cargo test -p arcweft-cli --test check agent_hit_test_reports_depth_sorted_rich_text_proxy -- --exact --nocapture`; `cargo check -p arcweft-agent-protocol -p arcweft-render-text -p arcweft-runtime-plan -p arcweft-cli` |
| `SVR-2026-06-17-026` | Typewriter `delay` is now part of native animation timing instead of documentation-only metadata. The native renderer accepts `delay` as seconds and parses raw `s` / `ms` suffixes such as `500ms`; visual-plan glyph opacity and framebuffer capture remain hidden before delay and visible after delay plus `cps` time without relayout. The effects animation sample now includes a delayed typewriter span. | Source-level and native framebuffer evidence is generated by the focused native regression; no PNG artifact is needed because the change is alpha/object visibility timing. | `cargo test -p arcweft-player-native native_typewriter_delay_offsets_visibility_time -- --nocapture`; `cargo test -p arcweft-player-native native_typewriter_capture_changes_visibility_without_relayout -- --nocapture` |
| `SVR-2026-06-17-027` | Native builtin rich-text effects now diagnose unsupported visual phases instead of silently no-oping. A recognized effect such as `.wave phase=post_process` remains preserved as metadata and does not get reinterpreted as a placement effect, but native visual-plan and Agent observe/capture diagnostics now report `unsupported_builtin_effect_phase`, matching the existing custom effect and shader phase diagnostics. | Source-level and native framebuffer evidence is generated by focused native and CLI regressions; no PNG artifact is needed because the change verifies renderer diagnostics. | `cargo test -p arcweft-player-native native_visual_plan_reports_unsupported_builtin_effect_phase -- --nocapture`; `cargo test -p arcweft-cli --test check agent_observe_native_renderer_reports_unsupported_builtin_effect_phase -- --exact --nocapture` |
| `SVR-2026-06-17-028` | Typewriter cursor metadata now affects native debug rendering instead of being metadata-only. `cursor=true` previews the next unrevealed glyph at low alpha, `cursor_alpha` / `cursor_opacity` controls that alpha, and the preview is visible in native visual-plan glyph opacity plus framebuffer/mask/object-id capture without relayout. The effects animation sample now exercises delayed typewriter with cursor preview. | Source-level and native framebuffer evidence is generated by the focused native regression; no PNG artifact is needed because the change verifies alpha/object visibility timing. | `cargo test -p arcweft-player-native native_typewriter_cursor_previews_next_glyph_without_relayout -- --nocapture`; `cargo run -p arcweft-cli --quiet -- check samples\rich-text-effects-animation.arcw` |
| `SVR-2026-06-17-029` | Registered rich-text shaders now support `phase=glyph_color` as a concrete native glyph tint path instead of reporting `unsupported_shader_phase`. The default `soft_glow` / `warm_glow` shader registry entries still emit offscreen glow passes for `run_offscreen_pass`, and now return full-alpha main-glyph tint passes for `glyph_color`. The effects animation sample includes a `glyph color shader` span, and native visual-plan, framebuffer, Agent observe, and object PNG capture all verify that the shader is executable decoration rather than metadata-only. | Fixed effects sample: [`effects-animation-shader-glyph-color-fixed.png`](effects-animation-shader-glyph-color-fixed.png). | `cargo test -p arcweft-player-native soft_glow_shader_glyph_color_tints_native_glyphs -- --nocapture`; `cargo test -p arcweft-cli --test check agent_observe_native_renderer_applies_shader_glyph_color_phase -- --exact --nocapture`; `cargo run -p arcweft-cli --quiet -- agent observe samples\rich-text-effects-animation.arcw --json --image png --out docs\implementation\visual-sample-review-issues\2026-06-17\effects-animation-shader-glyph-color-fixed.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.0 --capture-time 0.25` |
| `SVR-2026-06-17-030` | Rich-text effects authored with `phase=host_event` now lower to typed `DialogueHostEvent::Effect` markers instead of remaining visual effect metadata that the native renderer diagnoses as unsupported. The visible span text remains ordinary display text, while Agent observe exposes the resolved effect id and raw attrs through `rich_text.host_events`. The effects animation sample includes a host-event cue. `post_process` remains the unsupported visual phase used to verify native diagnostics. | Source-level and JSON evidence is generated by focused runtime-plan and CLI regressions; no PNG artifact is needed because the change verifies event routing rather than pixels. | `cargo test -p arcweft-runtime-plan host_event_phase_effect_lowers_to_typed_host_event -- --nocapture`; `cargo test -p arcweft-cli --test check agent_observe_reports_host_event_phase_effects -- --exact --nocapture`; `cargo test -p arcweft-player-native native_visual_plan_reports_unsupported_builtin_effect_phase -- --nocapture`; `cargo run -p arcweft-cli --quiet -- check samples\rich-text-effects-animation.arcw` |
| `SVR-2026-06-17-031` | Registered rich-text shaders now support `phase=post_process` for native color framebuffer/readback captures. `RichTextShaderRegistry::insert_post_process_lambda` lets native adapters register raster passes, the default registry provides `screen_tint`, and object-id/mask captures stay unmodified identification attachments. The effects animation sample includes a `post process shader` span; native and CLI regressions verify default and custom post-process shaders by reading tinted RGBA pixels rather than relying on metadata. | Raw RGBA evidence is generated by focused native/CLI regressions and intentionally not checked in. The sample is covered by `samples/rich-text-effects-animation.arcw`. | `cargo test -p arcweft-player-native post_process_shader -- --nocapture`; `cargo test -p arcweft-cli --test check agent_observe_native_renderer_applies_shader_post_process_phase -- --exact --nocapture`; `cargo run -p arcweft-cli --quiet -- check samples\rich-text-effects-animation.arcw` |
| `SVR-2026-06-17-032` | Text proxy spans are now native-addressable debug elements rather than only reusing their parent run during capture resolution. `NativeFrameElement::TextObjectProxy { run_index, proxy_index }` is measured from the same post-transform glyph geometry as the proxy span and CLI object-id parsing resolves `object.dialogue.*.proxy.<run>.<proxy>` to that native element. This makes proxy object color/mask/object-id captures flow through a first-class text object target while preserving the existing observed JSON ids. | Source-level/native measurement and CLI observe/hit-test evidence is generated by focused regressions; no PNG artifact is needed because the change verifies object routing and capture identity rather than a new visual effect. | `cargo test -p arcweft-player-native text_object_proxy -- --nocapture`; `cargo test -p arcweft-cli --test check agent_observe_json_reports_rich_text_display_objects -- --exact --nocapture`; `cargo test -p arcweft-cli --test check agent_hit_test_reports_depth_sorted_rich_text_proxy -- --exact --nocapture` |
| `SVR-2026-06-17-033` | Rich-text page and line objects now retain proxy metadata for their covered text ranges instead of exposing only a broad page/line hit box. Agent observe reports the deepest proxy `object_layer` / `object_depth` on page and line refs, and their `hit_regions` include native-bounds-backed `text_object_proxy` entries for nested `KeywordHit` / `HoverHit` spans. Hit testing still ranks the concrete `rich_text_proxy` child above the aggregate page/line objects. | Source-level and JSON evidence is generated by the focused CLI regression; no PNG artifact is needed because the change verifies metadata and hit-region routing. | `cargo test -p arcweft-cli --test check agent_hit_test_reports_depth_sorted_rich_text_proxy -- --exact --nocapture` |
| `SVR-2026-06-17-034` | Proxy hit regions now preserve typed custom proxy params instead of exposing only id/type/role/layer/depth. `AgentHitRegion.proxy_params` carries the resolved `RichTextParam` map from `#[text_proxy]` defaults plus inline overrides, and `arcw agent hit-test` returns the same map on each hit. The focused nested proxy regression now requires the lower-ranked `KeywordHit` hit to retain `channel=choice`, proving input/debug tools can consume custom proxy metadata directly from the hit-test result. | Source-level and JSON evidence is generated by protocol and CLI regressions; no PNG artifact is needed because the change verifies hit-test metadata transport. | `cargo test -p arcweft-agent-protocol hit_region_serializes_proxy_params_when_present -- --nocapture`; `cargo test -p arcweft-cli --test check agent_hit_test_reports_depth_sorted_rich_text_proxy -- --exact --nocapture` |
