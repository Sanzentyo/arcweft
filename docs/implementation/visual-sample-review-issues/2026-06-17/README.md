# Visual Sample Review Issues 2026-06-17

> Status note: `SVR-2026-06-17-013` is historical and its fixed ruby cell-overlap
> conclusion was superseded by the 2026-07-13 W3C/WPT parity review. The child-ID
> capture fix in that row remains current; canonical ruby cells now stack
> without block-axis overlap.
>
> The page/wait/clear aliases mentioned by dated captures were later removed.
> Current source uses only the control spellings owned by
> `DialogueRichTextControl`; the images remain historical rendering evidence.
>
> Inline `#effect(...)`, `.run_offscreen_pass`, inferred/open-close tag, and
> similar spellings quoted in the dated rows below describe the source that
> produced those historical captures. They are not accepted syntax. The
> maintained surface uses typed `#fx(call(...))[content]` applications (for
> example `#fx(wave(...))[content]`) and `.offscreen_pass`; no alias,
> tombstone, canonicalizer, or migration fallback is retained.
>
> The dated source snapshots may likewise contain `[tag][/tag]`, `[tag][/]`,
> or `[/` because they preserve captured historical input. Those snapshots are
> evidence only, not maintained fixtures, examples, or acceptance commands.

This directory keeps native rich-text capture evidence for the ongoing
`samples/rich-text-full-grammar.arcw` review. Large full observation JSON files
are intentionally not checked in; regenerate them with the commands below when
object metadata needs to be inspected.

| ID | Finding | Evidence | Reproduction |
| --- | --- | --- | --- |
| `SVR-2026-06-17-001` | Native shaping with default ligatures made Latin text in rich-text/effect samples look broken. The layout model currently maps styled source ranges to per-character glyph slots before native shaping; ligature clusters such as `ffi` could overlap multiple slots and be submitted repeatedly. Native body/ruby shaping now disables `liga`/`clig`, fixing duplicated words such as `offfset`/`efffect` while leaving a shaping-aware horizontal layout as a remaining improvement. | Before: [`full-grammar-effects-object-step16.png`](full-grammar-effects-object-step16.png); after: [`full-grammar-effects-object-after-latin.png`](full-grammar-effects-object-after-latin.png). | `cargo run -p arcweft-cli --features native-capture --quiet -- agent observe samples\rich-text-full-grammar.arcw --image png --out docs\implementation\visual-sample-review-issues\2026-06-17\full-grammar-effects-object-after-latin.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.6 --capture-time 0.25` |
| `SVR-2026-06-17-002` | Font-family and shader sample lines exposed the broader non-shaped horizontal layout limitation: Latin advances were proportional enough in framebuffer submission after the first fix, but native measurement/debug geometry still used Sans I/O heuristic origins. Native visual-plan glyph placements, text-run bounds, glyph-cluster bounds, and object/debug crops now reuse glyphon shaped horizontal advances for ordinary non-ruby horizontal runs. Full line breaking and ruby-base allocation remain Sans I/O responsibilities. | Before: [`full-grammar-shader-object-step16.png`](full-grammar-shader-object-step16.png); after: [`full-grammar-decoration-object-after-shaped-latin.png`](full-grammar-decoration-object-after-shaped-latin.png). | `cargo run -p arcweft-cli --features native-capture --quiet -- agent observe samples\rich-text-full-grammar.arcw --image png --out docs\implementation\visual-sample-review-issues\2026-06-17\full-grammar-decoration-object-after-shaped-latin.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.5 --capture-time 0.25` |
| `SVR-2026-06-17-003` | This capture verified that the provisional shader-effect surface reached native framebuffer output instead of stopping at visual-plan metadata. The captured source used the later-removed `#effect(...)` spelling; it is historical evidence, not a maintained syntax example. Current acceptance uses `#fx(shader(...))[content]` and the checked Fx catalog. | Fixed historical capture: [`full-grammar-shader-object-after-soft-glow.png`](full-grammar-shader-object-after-soft-glow.png). | Historical capture only; validate the current surface through maintained `#fx(shader(...))[content]` fixtures. |
| `SVR-2026-06-17-004` | Horizontal Latin was still laid out from deterministic Sans I/O width heuristics in native submission, so words such as `serif`, `style`, `transform`, and `effect` looked over-spaced even after ligature duplication was fixed. Native submission and native debug measurement now feed glyphon shaped advances back into horizontal glyph origins for ordinary non-ruby horizontal runs, while leaving ruby-base allocations and vertical text untouched. | Fixed decoration: [`full-grammar-decoration-object-after-shaped-latin.png`](full-grammar-decoration-object-after-shaped-latin.png); fixed shader/effect family: [`full-grammar-shader-object-after-shaped-latin.png`](full-grammar-shader-object-after-shaped-latin.png). | `cargo run -p arcweft-cli --features native-capture --quiet -- agent observe samples\rich-text-full-grammar.arcw --image png --out docs\implementation\visual-sample-review-issues\2026-06-17\full-grammar-shader-object-after-shaped-latin.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.7 --capture-time 0.25` |
| `SVR-2026-06-17-005` | Current top-level review captures for all 11 `samples/rich-text-full-grammar.arcw` dialogue objects produce native PNGs with no runtime/type diagnostics; renderer diagnostics are now reported separately when a captured object contains an unresolved native custom effect. The wait/page/clear grammar object must be reviewed by page: page 0 only shows the pre-line-wait text, while pages 1-3 show the later wait, page, and clear aliases. The vertical extreme ruby object needs a tall viewport/textbox capture for meaningful inspection; the default object crop intentionally shows only the visible right-side column segment. | Top-level captures: [`full-grammar-object-00-current.png`](full-grammar-object-00-current.png) through [`full-grammar-object-10-current.png`](full-grammar-object-10-current.png); paged wait object: [`full-grammar-object-04-page-1-current.png`](full-grammar-object-04-page-1-current.png), [`full-grammar-object-04-page-2-current.png`](full-grammar-object-04-page-2-current.png), [`full-grammar-object-04-page-3-current.png`](full-grammar-object-04-page-3-current.png); tall vertical ruby: [`full-grammar-object-02-tall-current.png`](full-grammar-object-02-tall-current.png). | `cargo run -p arcweft-cli --features native-capture --quiet -- agent observe samples\rich-text-full-grammar.arcw --image png --out docs\implementation\visual-sample-review-issues\2026-06-17\full-grammar-object-00-current.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.0 --capture-time 0.25` |
| `SVR-2026-06-17-006` | Native capture previously dropped renderer diagnostics produced during glyph submission, so unresolved custom effects looked like successful no-ops in Agent JSON. This dated capture used a provisional custom-effect spelling that was later removed. Current custom Fx must resolve through a typed `#[fx]` producer and `#fx(value)[content]`; unknown identities remain errors rather than fallback effects. | Historical `.sparkle` capture: [`full-grammar-object-06-current.png`](full-grammar-object-06-current.png). | Historical capture only; current validation belongs to maintained project-Fx and renderer-diagnostic tests. |
| `SVR-2026-06-17-007` | Historical evidence for the removed bracket-conditional prototype. That prototype lowered conditional markers as host-event metadata and this capture verified its runtime branch filtering. It is not evidence for the current structural Content surface; current acceptance must use maintained typed Content fixtures. | Fixed prototype capture: [`full-grammar-cue-conditional-fixed.png`](full-grammar-cue-conditional-fixed.png). | Historical command retained only by the image metadata; do not rerun it as current syntax acceptance. |
| `SVR-2026-06-17-008` | Rotation lowering previously lost an authored angle and produced `rotate.degrees = 0`. The typed `#transform(.rotate, angle=10deg)[content]` call now retains the schema-owned angle operand, so the full grammar sample carries the checked rotation and renders an actual rotated run. | Fixed transform rotate: [`full-grammar-transform-rotate-fixed.png`](full-grammar-transform-rotate-fixed.png). | `cargo run -p arcweft-cli --features native-capture --quiet -- agent observe samples\rich-text-full-grammar.arcw --json --image png --out target\agent-review\goal-audit-continued\object-6-rotate-fixed.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.6 --capture-time 0.25` |
| `SVR-2026-06-17-009` | This dated capture expanded effect/transform metadata coverage and exposed `state_scope` leaking into custom effect parameters. Its `#effect(...)` and `.run_offscreen_pass` spellings belonged to a provisional surface that was later deleted. The maintained equivalents are typed `#fx(value)[content]` applications and the closed `.offscreen_pass` phase; current acceptance must not rerun the captured source as canonical syntax. | Historical expanded effect/transform evidence: [`full-grammar-expanded-effects-transforms.png`](full-grammar-expanded-effects-transforms.png); explicit family: [`full-grammar-expanded-explicit-family.png`](full-grammar-expanded-explicit-family.png). | Historical capture only; validate through maintained Fx metadata, mask, and phase tests. |
| `SVR-2026-06-17-010` | This dated issue verified a provisional host-selector dispatch path. That selector and canonicalizer were later deleted. Current custom presentation behavior is admitted through a typed `#[fx] fn ... -> Fx` definition and `#fx(expression)[content]` application; unknown custom identities do not become effects through fallback. | Fixed host-dispatch image from the dated implementation: [`full-grammar-host-effect-dispatch.png`](full-grammar-host-effect-dispatch.png). | The image is historical evidence; current acceptance belongs to maintained Fx compiler/runtime tests. |
| `SVR-2026-06-17-011` | Vertical Latin was still treated as one sideways glyph per character, so `ABC` in a Japanese vertical run looked like separate stacked letters instead of an engine-rotated horizontal Latin run. Text layout now groups consecutive sideways Latin/Greek graphemes into one vertical cluster, sizes its bounds by the horizontal shaping advance, and the glyphon adapter maps shaped x offsets into vertical progression before applying `Rotate90Cw`. The full grammar vertical sample now observes `ABC` as one `sideways_cw` cluster with a run-shaped bbox. | Before: [`issue-011-sideways-latin-run-before.png`](issue-011-sideways-latin-run-before.png); fixed: [`issue-011-sideways-latin-run-fixed.png`](issue-011-sideways-latin-run-fixed.png). | `cargo run -p arcweft-cli --features native-capture --quiet -- agent observe samples\rich-text-full-grammar.arcw --json --image png --out docs\implementation\visual-sample-review-issues\2026-06-17\issue-011-sideways-latin-run-fixed.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.6 --capture-time 0.25` |
| `SVR-2026-06-17-012` | Full grammar effect verification now checks rendered pixels, not only metadata. The native Agent regression reads raw RGBA object crops for `.wave`, `.shake`, `.sparkle`, and `.host id=sparkle`; `wave` is sampled at the same runtime step with two explicit animation times, while `shake`, custom sparkle, and host-dispatched sparkle are sampled at `--capture-step 1` and `--capture-step 2`. Each pair must differ in raw pixels while still reporting native object scope, content pixels, and capture timing metadata. | Raw RGBA evidence is generated under the test temp directory and intentionally not checked in. Existing PNG review captures for the same full grammar object remain [`full-grammar-expanded-effects-transforms.png`](full-grammar-expanded-effects-transforms.png) and [`full-grammar-host-effect-dispatch.png`](full-grammar-host-effect-dispatch.png). | `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_reports_full_grammar_sample_rich_text_constructs -- --exact --nocapture` |
| `SVR-2026-06-17-013` | Ruby placement was still too far from the body text in horizontal-under and vertical side-track cases because Arcweft treated the annotation/base bboxes as hard-separated rectangles. Chrome's HTML ruby rendering permits a small natural overlap at zero gap, so text layout now applies the same HTML-like natural overlap for horizontal over/under and vertical left/right ruby tracks before adding explicit `ruby_gap`. Native object capture also now resolves hidden rich-text child IDs through their parent textbox and native element, so typewriter-hidden text-combine clusters can be cropped at `--capture-time 0` with stable geometry and zero content pixels, then compared to a later visible capture. | Evidence is in native raw-crop regression output; PNG review captures should be regenerated for any visual audit that depends on ruby gap perception. | `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_writes_vertical_goal_clear_smoke_raw_crops -- --exact --nocapture` |
| `SVR-2026-06-17-014` | Custom text decoration coverage now includes registered `glyph_color` effects, not only placement effects. The native default `sparkle` registry entry supports `phase=glyph_color`; `samples/rich-text-effects-animation.arcw` includes `色sparkle`, and the Agent regression captures that run's raw RGBA object crop at a pinned step/time and requires red-tinted visible pixels with no renderer diagnostics. | Raw RGBA evidence is generated under the test temp directory and intentionally not checked in. The sample is covered by `samples/rich-text-effects-animation.arcw`. | `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_captures_combined_typewriter_animation_sample -- --exact --nocapture` |
| `SVR-2026-06-17-015` | Rich-text shaders now use a native shader registry rather than a hard-coded `soft_glow` branch. The default registry renders `soft_glow` and `warm_glow`, and `NativeOffscreenCaptureSession::shader_registry_mut` can register arbitrary shader IDs for Rust/native adapters. The effects animation sample includes `warm glow shader`; the Agent regression captures that run's raw RGBA object crop at a pinned step/time and requires warm-tinted visible pixels with no renderer diagnostics. A native unit test separately registers a custom `rose_glow` shader and verifies red-tinted framebuffer pixels. | Raw RGBA evidence is generated under the test temp directory and intentionally not checked in. The sample is covered by `samples/rich-text-effects-animation.arcw`. | `cargo test -p arcweft-render-native shader -- --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_captures_combined_typewriter_animation_sample -- --exact --nocapture` |
| `SVR-2026-06-17-016` | Rich-text `.motion fn=...` now resolves through `RichTextMotionRegistry` rather than a hard-coded sampler/hash fallback. The default registry renders `breath_orbit` and `elastic_bloom`, and `NativeOffscreenCaptureSession::motion_registry_mut` can register arbitrary motion IDs for Rust/native adapters. Missing motion IDs emit `missing_motion_function`; a native unit test registers `snap_rise` and verifies framebuffer glyph pixels move, while the effects animation sample still captures `関数motion` at two pinned times and requires differing raw RGBA crops. | Raw RGBA evidence is generated under the test temp directory and intentionally not checked in. The sample is covered by `samples/rich-text-effects-animation.arcw`. | `cargo test -p arcweft-render-native motion -- --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_captures_combined_typewriter_animation_sample -- --exact --nocapture` |
| `SVR-2026-06-17-017` | Text proxy objects are now regression-checked as real capture targets, not only metadata entries. The full grammar sample's `#[text_proxy] KeywordHit` defaults still resolve to role `keyword`, hit-test enabled, depth `4000`, and params, and the Agent regression now reads the proxy object's raw mask and object-id capture refs through `--read-uri`. The object-id readback must decode as raw RGBA and contain the proxy object's own `capture_refs.object_id_color`, so a parent run/page color can no longer satisfy the test. | Raw mask/object-id evidence is generated by `--read-uri` during the test and intentionally not checked in. The sample is covered by `samples/rich-text-full-grammar.arcw`. | `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_reports_full_grammar_sample_rich_text_constructs -- --exact --nocapture` |
| `SVR-2026-06-17-018` | Rich-text rendered pages are now first-class Agent objects. Agent observe emits `rich_text_page` children with `rich_text_ref.kind = text_page`, page-local range, hit region, page-aware capture refs, and object-id colors. The full grammar regression reads a page object's raw mask and object-id resources through `--read-uri`, requiring native object-scope pixels and an object-id tint that matches the page object's own color. | Raw mask/object-id evidence is generated by `--read-uri` during the test and intentionally not checked in. The sample is covered by `samples/rich-text-full-grammar.arcw`. | `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_reports_full_grammar_sample_rich_text_constructs -- --exact --nocapture` |
| `SVR-2026-06-17-019` | Authored rich-text lines are now first-class Agent objects. Agent observe emits `rich_text_line` children with `rich_text_ref.kind = text_line`, line-local range, hit region, page-aware capture refs, and object-id colors. Line ranges split at authored hard breaks and page boundaries, and native captures use the text/ruby element set that overlaps the line rather than a fallback rectangle. The full grammar regression reads a line object's raw mask and object-id resources through `--read-uri`, requiring native object-scope pixels and an object-id tint that matches the line object's own color. | Raw mask/object-id evidence is generated by `--read-uri` during the test and intentionally not checked in. The sample is covered by `samples/rich-text-full-grammar.arcw`. | `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_reports_full_grammar_sample_rich_text_constructs -- --exact --nocapture` |
| `SVR-2026-06-17-020` | Non-zero rich-text pages now have explicit Agent regression coverage for page and line objects. The clear-after-page fixture observes `object.dialogue.0.0.page.1` and `object.dialogue.0.0.line.1`, requires their capture refs to carry `?page=1`, then reads those color PNG resources back through `--read-uri` and verifies native object-scope pixels on page 1. | PNG evidence is generated by `--read-uri` during the test and intentionally not checked in. | `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_captures_clear_after_page_object -- --exact --nocapture` |
| `SVR-2026-06-17-021` | Nested rich-text proxy spans are now covered by the full grammar sample and regression. The sample defines both `KeywordHit` and `HoverHit`, nests them on `multi proxy`, and verifies that runtime lowering preserves both proxy objects with struct defaults plus inline overrides. Agent observe must report two separate `rich_text_proxy` objects for the same visible span, each with its own hit region, params, capture refs, and object-id raw tint. | Raw mask/object-id evidence is generated by `--read-uri` during the test and intentionally not checked in. The sample is covered by `samples/rich-text-full-grammar.arcw`. | `cargo test -p arcweft-runtime-plan nested_text_proxy_struct_attributes_accumulate_with_inline_overrides`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_reports_full_grammar_sample_rich_text_constructs -- --exact --nocapture` |
| `SVR-2026-06-17-022` | This dated issue verified parameter preservation in the then-current RichText canonicalizer. That inferred span and source-action path has since been deleted. The current and only source authority is the typed `#object(id = @.HoverHit, type = HoverHit, tone = "alert")[...]` application; no removed spelling or source rewrite is admitted. | The original source-level evidence is historical; current acceptance is owned by the typed Object application and its compiler/runtime tests. | The historical command names are retained in Git history and are not current acceptance commands. |
| `SVR-2026-06-17-023` | Text glyphs are now explicit Agent objects instead of being reachable only through glyph-cluster objects. Agent observe emits `rich_text_glyph` children with `rich_text_ref.kind = text_glyph`, text range, hit region, capture refs, orientation, vertical-form metadata, and independent object-id colors. Native raw mask/object-id crops for `object.dialogue.*.glyph.*` resolve through the same native glyph bounds as drawing, so debuggers can request a specific rendered text glyph as an image-backed object. | Raw mask/object-id evidence is generated by the focused CLI regression and intentionally not checked in. | `cargo test -p arcweft-agent-protocol observation_report_serializes_stable_snake_case_enums`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_json_reports_rich_text_display_objects -- --exact --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_writes_vertical_text_glyph_raw_crops -- --exact --nocapture` |
| `SVR-2026-06-17-024` | Agent hit-testing now consumes the same rich-text hit regions used by observe/capture. `arcw agent hit-test` and MCP `arcweft.hit_test` return an `AgentHitTestReport` for a viewport coordinate, ranking hits by resolved depth and semantic specificity so nested custom proxy objects behave as real clickable/depth-aware text objects. The focused CLI and MCP stdio regressions observe nested `KeywordHit`/`HoverHit` proxies, hit-test the measured proxy bbox center, and require the deeper `HoverHit` object to be top-ranked while the shallower `KeywordHit` hit remains in the candidate list with `proxy_params.channel=choice`. | Source-level and JSON evidence is generated by the focused CLI/MCP regressions; no PNG artifact is needed because the change verifies object/region routing rather than pixel generation. | `cargo test -p arcweft-cli --features native-capture --test check agent_hit_test_reports_depth_sorted_rich_text_proxy -- --exact --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_mcp_stdio_hit_test_reports_depth_sorted_rich_text_proxy -- --ignored --exact --nocapture`; `cargo test -p arcweft-agent-mcp hit_test_tool_schema_requires_viewport_coordinate`; `cargo check -p arcweft-agent-protocol -p arcweft-agent-mcp -p arcweft-cli` |
| `SVR-2026-06-17-025` | Custom rich-text proxy `layer` metadata is now first-class object data instead of a forwarded custom param. `#[text_proxy(... layer=ui)]` and inline `layer=` / `object_layer=` lower to `RichTextObjectProxy.layer`, Agent observe reports it as `rich_text_ref.object_layer`, and hit-test regions report it as `region.proxy_layer`. This keeps text proxies aligned with image/model-like debug objects that have semantic layer, depth, hit-test, and capture identity. | Source-level and JSON evidence is generated by focused runtime-plan and CLI regressions; no PNG artifact is needed because the change verifies proxy metadata routing rather than pixel generation. | `cargo test -p arcweft-runtime-plan nested_text_proxy_struct_attributes_accumulate_with_inline_overrides`; `cargo test -p arcweft-cli --features native-capture --test check agent_hit_test_reports_depth_sorted_rich_text_proxy -- --exact --nocapture`; `cargo check -p arcweft-agent-protocol -p arcweft-render-text -p arcweft-runtime-plan -p arcweft-cli` |
| `SVR-2026-06-17-026` | Typewriter `delay` is now part of native animation timing instead of documentation-only metadata. The native renderer accepts `delay` as seconds and parses raw `s` / `ms` suffixes such as `500ms`; visual-plan glyph opacity and framebuffer capture remain hidden before delay and visible after delay plus `cps` time without relayout. The effects animation sample now includes a delayed typewriter span. | Source-level and native framebuffer evidence is generated by the focused native regression; no PNG artifact is needed because the change is alpha/object visibility timing. | `cargo test -p arcweft-render-native native_typewriter_delay_offsets_visibility_time -- --nocapture`; `cargo test -p arcweft-render-native native_typewriter_capture_changes_visibility_without_relayout -- --nocapture` |
| `SVR-2026-06-17-027` | Native builtin rich-text effects no longer leave `phase=post_process` as an unsupported visual phase. A recognized effect such as `.wave phase=post_process` remains preserved as metadata and still does not get reinterpreted as glyph placement, but native color framebuffer capture now applies a deterministic post-process pass and reports no `unsupported_builtin_effect_phase` diagnostics. | Source-level and native framebuffer evidence is generated by focused native and CLI regressions; no PNG artifact is needed because the change verifies raw framebuffer mutation and diagnostics. | `cargo test -p arcweft-render-native native_capture_applies_builtin_post_process_effect_phase -- --exact --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_applies_builtin_effect_post_process_phase -- --exact --nocapture` |
| `SVR-2026-06-17-028` | Typewriter cursor metadata now affects native debug rendering instead of being metadata-only. `cursor=true` previews the next unrevealed glyph at low alpha, `cursor_alpha` / `cursor_opacity` controls that alpha, and the preview is visible in native visual-plan glyph opacity plus framebuffer/mask/object-id capture without relayout. The effects animation sample now exercises delayed typewriter with cursor preview. | Source-level and native framebuffer evidence is generated by the focused native regression; no PNG artifact is needed because the change verifies alpha/object visibility timing. | `cargo test -p arcweft-render-native native_typewriter_cursor_previews_next_glyph_without_relayout -- --nocapture`; `cargo run -p arcweft-cli --quiet -- check samples\rich-text-effects-animation.arcw` |
| `SVR-2026-06-17-029` | Registered rich-text shaders now support `phase=glyph_color` as a concrete native glyph tint path instead of reporting `unsupported_shader_phase`. The default `soft_glow` / `warm_glow` shader registry entries still emit offscreen glow passes for `run_offscreen_pass`, and now return full-alpha main-glyph tint passes for `glyph_color`. The effects animation sample includes a `glyph color shader` span, and native visual-plan, framebuffer, Agent observe, and object PNG capture all verify that the shader is executable decoration rather than metadata-only. | Fixed effects sample: [`effects-animation-shader-glyph-color-fixed.png`](effects-animation-shader-glyph-color-fixed.png). | `cargo test -p arcweft-render-native soft_glow_shader_glyph_color_tints_native_glyphs -- --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_applies_shader_glyph_color_phase -- --exact --nocapture`; `cargo run -p arcweft-cli --features native-capture --quiet -- agent observe samples\rich-text-effects-animation.arcw --json --image png --out docs\implementation\visual-sample-review-issues\2026-06-17\effects-animation-shader-glyph-color-fixed.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.0 --capture-time 0.25` |
| `SVR-2026-06-17-030` | Rich-text effects authored with `phase=host_event` now lower to typed `DialogueHostEvent::Effect` markers instead of remaining visual effect metadata. The visible span text remains ordinary display text, while Agent observe exposes the resolved effect id and raw attrs through `rich_text.host_events`. The effects animation sample includes a host-event cue, while `phase=post_process` remains in the visual pipeline and is covered separately as a framebuffer effect path. | Source-level and JSON evidence is generated by focused runtime-plan and CLI regressions; no PNG artifact is needed because the change verifies event routing rather than pixels. | `cargo test -p arcweft-runtime-plan host_event_phase_effect_lowers_to_typed_host_event -- --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_reports_host_event_phase_effects -- --exact --nocapture`; `cargo run -p arcweft-cli --quiet -- check samples\rich-text-effects-animation.arcw` |
| `SVR-2026-06-17-031` | Registered rich-text shaders now support `phase=post_process` for native color framebuffer/readback captures. `RichTextShaderRegistry::insert_post_process_lambda` lets native adapters register raster passes, the default registry provides `screen_tint`, and object-id/mask captures stay unmodified identification attachments. The effects animation sample includes a `post process shader` span; native and CLI regressions verify default and custom post-process shaders by reading tinted RGBA pixels rather than relying on metadata. | Raw RGBA evidence is generated by focused native/CLI regressions and intentionally not checked in. The sample is covered by `samples/rich-text-effects-animation.arcw`. | `cargo test -p arcweft-render-native post_process_shader -- --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_applies_shader_post_process_phase -- --exact --nocapture`; `cargo run -p arcweft-cli --quiet -- check samples\rich-text-effects-animation.arcw` |
| `SVR-2026-06-17-032` | Text proxy spans are now native-addressable debug elements rather than only reusing their parent run during capture resolution. `NativeFrameElement::TextObjectProxy { run_index, proxy_index }` is measured from the same post-transform glyph geometry as the proxy span and CLI object-id parsing resolves `object.dialogue.*.proxy.<run>.<proxy>` to that native element. This makes proxy object color/mask/object-id captures flow through a first-class text object target while preserving the existing observed JSON ids. | Source-level/native measurement and CLI observe/hit-test evidence is generated by focused regressions; no PNG artifact is needed because the change verifies object routing and capture identity rather than a new visual effect. | `cargo test -p arcweft-render-native text_object_proxy -- --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_json_reports_rich_text_display_objects -- --exact --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_hit_test_reports_depth_sorted_rich_text_proxy -- --exact --nocapture` |
| `SVR-2026-06-17-033` | Rich-text page and line objects now retain proxy metadata for their covered text ranges instead of exposing only a broad page/line hit box. Agent observe reports the deepest proxy `object_layer` / `object_depth` on page and line refs, and their `hit_regions` include native-bounds-backed `text_object_proxy` entries for nested `KeywordHit` / `HoverHit` spans. Hit testing still ranks the concrete `rich_text_proxy` child above the aggregate page/line objects. | Source-level and JSON evidence is generated by the focused CLI regression; no PNG artifact is needed because the change verifies metadata and hit-region routing. | `cargo test -p arcweft-cli --features native-capture --test check agent_hit_test_reports_depth_sorted_rich_text_proxy -- --exact --nocapture` |
| `SVR-2026-06-17-034` | Proxy hit regions now preserve typed custom proxy params instead of exposing only id/type/role/layer/depth. `AgentHitRegion.proxy_params` carries the resolved `RichTextParam` map from `#[text_proxy]` defaults plus inline overrides, and `arcw agent hit-test` returns the same map on each hit. The focused nested proxy regression now requires the lower-ranked `KeywordHit` hit to retain `channel=choice`, proving input/debug tools can consume custom proxy metadata directly from the hit-test result. | Source-level and JSON evidence is generated by protocol and CLI regressions; no PNG artifact is needed because the change verifies hit-test metadata transport. | `cargo test -p arcweft-agent-protocol hit_region_serializes_proxy_params_when_present -- --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_hit_test_reports_depth_sorted_rich_text_proxy -- --exact --nocapture` |
| `SVR-2026-06-17-035` | Object-scoped image resources now preserve the captured observed object metadata in `image.object`. Rich-text proxy mask/object-id/color readback keeps the same `rich_text_ref` as the observed proxy object, including selected `presentation.object_proxies[].params`, hit regions, object layer/depth, and source range. MCP image tool responses also preserve this metadata in their JSON text block, so debuggers can inspect a raw/PNG crop as a text object without separately walking the observation object list or reparsing the URI. | Source-level and JSON evidence is generated by protocol/MCP/CLI regressions; no PNG artifact is needed because the change verifies image-resource metadata transport. | `cargo test -p arcweft-agent-mcp image_tool_content_preserves_object_rich_text_ref_metadata -- --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_json_reports_rich_text_display_objects -- --exact --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_mcp_stdio_observes_and_reads_rich_text_child_image -- --ignored --exact --nocapture` |
| `SVR-2026-06-17-036` | Animated text proxy hit-testing and object-scoped image capture now use the same `capture_time`-aware native bounds as observe/capture. A proxy span wrapped around a large `.wave` transform moves far enough that a coordinate at the late sampled bbox center is outside the time-zero bbox; `arcw agent hit-test --capture-time 0.25` hits the proxy with its `proxy_params`, while `--capture-time 0` at the same coordinate does not. The same regression captures raw mask and object-id images for the late proxy object, verifies their `content_viewport_bbox` overlaps the late bbox but not the stale time-zero bbox, and checks the object-id tint against the proxy object's own color. | Source-level and JSON/raw RGBA evidence is generated by the focused CLI regression; generated raw captures are temporary because the change verifies animated hit/crop geometry rather than a review PNG. | `cargo test -p arcweft-cli --features native-capture --test check agent_hit_test_capture_time_follows_animated_text_proxy_bounds -- --exact --nocapture`; `cargo test -p arcweft-render-native measure_frame_elements_with_time_follows_glyph_transform_effects -- --nocapture` |
| `SVR-2026-06-17-037` | Rich-text effect `phase=post_process` is now a concrete native effect path, not shader-only and not a diagnostic-only phase. Builtin `.wave phase=post_process` mutates the raw color framebuffer without moving visual-plan glyph placements, and `RichTextEffectRegistry::insert_post_process_lambda` lets native adapters register custom effect framebuffer passes. The effects animation sample now includes both `post wave effect` and `.host id=sparkle phase=post_process` spans, and the Agent regression captures their raw object crops with no renderer diagnostics. | Raw RGBA evidence is generated under the focused native/CLI regressions and intentionally not checked in. The sample is covered by `samples/rich-text-effects-animation.arcw`. | `cargo test -p arcweft-render-native native_capture_applies_builtin_post_process_effect_phase -- --exact --nocapture`; `cargo test -p arcweft-render-native custom_effect_registry_for_post_process -- --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_captures_combined_typewriter_animation_sample -- --exact --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_applies_builtin_effect_post_process_phase -- --exact --nocapture` |
| `SVR-2026-06-17-038` | Text-proxy lowering now starts from the explicit typed `#object(id = @.hotspot, type = KeywordHit, ...)[content]` application. Struct defaults, inline overrides, params, depth/layer/hit-test metadata, observed `rich_text_proxy` objects, object-id/mask captures, and Agent hit-test results flow from that one authority. Unknown selectors and inferred proxy/effect spans are not fallback inputs. | Source-level and JSON/raw RGBA evidence is generated by focused runtime-plan and CLI regressions; generated captures are temporary because this verifies metadata and object-id/mask transport rather than a fixed review bitmap. | Current validation must use the typed Object application tests and the maintained full-grammar sample. |
| `SVR-2026-06-17-039` | Ordinary rich-text objects expose presentation depth/opacity through typed `#style(.z_index, z_index=3)[...]` and `#style(.opacity, opacity=0.8)[...]` calls instead of relying on proxy-only metadata. Agent observe reports `presentation.z_index` and `presentation.opacity`, and `rich_text_ref.object_depth` becomes `z_index * 1000` for runs, lines, and pages. Hit-testing uses the same depth, so ordinary text can participate in image/model-like ordering through the final typed presentation model. | Source-level and JSON evidence is generated by focused runtime-plan and CLI regressions; no PNG artifact is needed because the change verifies object metadata and hit depth routing. The full grammar sample includes `z depth`. | Current validation must use maintained typed Style-call fixtures. |
| `SVR-2026-06-17-040` | Ordinary rich-text objects expose presentation layer through the typed `#style(.layer, layer=@.hud)[...]` call instead of requiring a proxy. Agent observe reports `presentation.layer` and `rich_text_ref.object_layer` for runs, lines, pages, glyphs, clusters, and ruby objects. Proxy objects keep their explicit `layer`, while a proxy without one inherits the parent presentation layer for hit-test region reporting. | Source-level and JSON evidence is generated by focused runtime-plan and CLI regressions; no PNG artifact is needed because the change verifies object metadata and hit layer routing. The full grammar sample's `z depth` content now also carries `hud` layer. | Current validation must use maintained typed Style-call fixtures. |
| `SVR-2026-06-17-041` | The arbitrary metadata selector described by this dated issue is no longer an accepted authoring surface. Custom proxy parameters are admitted only through a typed `#object(...)[content]` application and remain scoped to `presentation.object_proxies[]` and hit-test `proxy_params`; ordinary content does not manufacture proxy metadata through a selector fallback. | The original source-level evidence is historical and does not define current syntax. | Current validation belongs to typed Object application and Agent proxy-metadata tests. |
| `SVR-2026-06-17-042` | Rich-text page and line objects now preserve aggregate presentation metadata instead of reporting only `object_layer` / `object_depth`. Agent observe builds page/line `rich_text_ref.presentation` from the overlapping text runs, so page/line objects retain ordinary `presentation.layer`, `presentation.params`, effects, shaders, and proxy metadata while still selecting the deepest proxy for `object_layer` / `object_depth`. Object-scoped readback therefore returns page/line image metadata that can be inspected like run/glyph objects. | Source-level and JSON/readback evidence is generated by focused CLI and full grammar native regressions; no PNG artifact is needed because the change verifies object metadata preservation. | `cargo test -p arcweft-cli --features native-capture --test check agent_observe_reports_text_presentation_z_index_depth -- --exact --nocapture`; `cargo test --release -p arcweft-cli --features native-capture --test check agent_observe_native_renderer_reports_full_grammar_sample_rich_text_constructs -- --exact --nocapture` |
| `SVR-2026-06-17-043` | Agent layer capture understands rich-text semantic object layers, not only render layers. Observed layer aggregation and `--layer` selection include `rich_text_ref.object_layer`, so ordinary text objects authored with `#style(.layer, layer=@.hud)[content]` can be captured with `--layer hud` while still keeping their parent `dialogue.rich_text` render layer. This keeps text object layer/debug workflows aligned with image/model-like layers. | Source-level, JSON, and PNG capture evidence is generated by the focused CLI regression; no fixed PNG is checked in because the test writes a temporary layer PNG and verifies the header. | Current validation must use maintained typed Style-call fixtures. |
| `SVR-2026-06-17-044` | Agent hit-test top-level `layer` now reports the resolved semantic object layer instead of the parent render layer when rich-text spans or proxies carry layer metadata. Hit layer resolution prefers `region.proxy_layer`, then `rich_text_ref.object_layer`, then the render layer, so input/debug tools can route clicks against text objects the same way they route image/model object layers. | Source-level and JSON evidence is generated by focused CLI regressions; no PNG artifact is needed because the change verifies hit-test metadata routing rather than pixel output. | `cargo test -p arcweft-cli --features native-capture --test check agent_hit_test_reports_depth_sorted_rich_text_proxy -- --exact --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_reports_text_presentation_z_index_depth -- --exact --nocapture` |
| `SVR-2026-06-17-045` | Agent hit-test hits now carry the observed object's `capture_refs`, including object-id color and color/object-id/mask capture URIs. A debugger can therefore hit a text proxy or ordinary rich-text object and immediately request the selected object's image resources without separately walking `objects[]`, matching the image/model-like object workflow. | Source-level and JSON evidence is generated by protocol, focused CLI, and tier-2 MCP stdio regressions; no PNG artifact is needed because the change verifies hit-test capture identity transport. | `cargo test -p arcweft-agent-protocol hit_test_hit_serializes_capture_refs -- --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_hit_test_reports_depth_sorted_rich_text_proxy -- --exact --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_reports_text_presentation_z_index_depth -- --exact --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_mcp_stdio_hit_test_reports_depth_sorted_rich_text_proxy -- --ignored --exact --nocapture` |
| `SVR-2026-06-17-046` | Object-scoped image metadata now carries rich-text semantic `object_layer` / `object_depth` directly on `image.object` while preserving the render layer in `image.object.layer`. Saved color/object-id/mask crops therefore remain self-describing typed text objects even when inspected without the original `objects[]` list. | Source-level and JSON evidence is generated by protocol and focused CLI regressions; no PNG artifact is needed because the change verifies image metadata transport. | `cargo test -p arcweft-agent-protocol image_resource_metadata_preserves_observed_object_ref -- --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_reports_text_presentation_z_index_depth -- --exact --nocapture` |
| `SVR-2026-06-17-047` | Object-scoped image metadata now repeats the captured object's `capture_refs`, including object-id color and sibling color/object-id/mask resource URIs. A returned text crop can therefore be used as a standalone typed object descriptor for follow-up captures without rereading `objects.json`. | Source-level and JSON evidence is generated by protocol and focused CLI/MCP metadata regressions; no PNG artifact is needed because the change verifies image metadata transport. | `cargo test -p arcweft-agent-protocol image_resource_metadata_preserves_observed_object_ref -- --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_reports_text_presentation_z_index_depth -- --exact --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_mcp_stdio_observes_and_reads_rich_text_child_image -- --ignored --exact --nocapture` |
| `SVR-2026-06-17-048` | Object-scoped image metadata now preserves the captured object's viewport `bbox` and `polygon` on `image.object`. A returned text crop therefore carries enough geometry to be hit-tested, compared with content bounds, and placed back into the frame without separately reading the original observation object list. | Source-level and JSON evidence is generated by protocol and focused CLI/MCP metadata regressions; no PNG artifact is needed because the change verifies image metadata transport. | `cargo test -p arcweft-agent-protocol image_resource_metadata_preserves_observed_object_ref -- --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_reports_text_presentation_z_index_depth -- --exact --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_mcp_stdio_observes_and_reads_rich_text_child_image -- --ignored --exact --nocapture` |
| `SVR-2026-06-17-049` | Agent hit-test hits now preserve the observed object's viewport `polygon` alongside `bbox`, `capture_refs`, semantic layer, depth, and `rich_text_ref`. A hit result and a later object-scoped image metadata result therefore describe the same text object geometry without an extra observation lookup. | Source-level and JSON evidence is generated by protocol and focused CLI/MCP hit-test regressions; no PNG artifact is needed because the change verifies hit-test metadata transport. | `cargo test -p arcweft-agent-protocol hit_test_hit_serializes_capture_refs -- --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_hit_test_reports_depth_sorted_rich_text_proxy -- --exact --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_observe_reports_text_presentation_z_index_depth -- --exact --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_mcp_stdio_hit_test_reports_depth_sorted_rich_text_proxy -- --ignored --exact --nocapture` |
| `SVR-2026-06-17-050` | Agent hit-test hits now include `object`, an `AgentImageObjectRef` matching object-scoped image metadata. Hit results therefore carry the same render layer, semantic object layer/depth, viewport geometry, capture refs, text, and `rich_text_ref` descriptor shape as returned image artifacts. | Source-level and JSON evidence is generated by protocol and focused CLI/MCP hit-test regressions; no PNG artifact is needed because the change verifies hit-test metadata transport. | `cargo test -p arcweft-agent-protocol hit_test_hit_serializes_capture_refs -- --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_hit_test_reports_depth_sorted_rich_text_proxy -- --exact --nocapture`; `cargo test -p arcweft-cli --features native-capture --test check agent_mcp_stdio_hit_test_reports_depth_sorted_rich_text_proxy -- --ignored --exact --nocapture` |

