# Visual Sample Review - 2026-06-18

This directory records review evidence for treating rich-text proxy spans and
rich-text child spans as first-class presentation objects. The primary samples
are `samples/rich-text-full-grammar.arcw` and
`samples/rich-text-effects-animation.arcw`.

| ID | Review | Evidence | Reproduction |
|---|---|---|---|
| `SVR-2026-06-18-001` | A `#[text_proxy]`-backed `KeywordHit` span can be found from the standalone presentation-tree resource using `proxy_param.channel=choice`, without reading the whole observation object table. The filtered tree preserves ancestors and resolves the concrete proxy object as `object.dialogue.0.7.proxy.11.0`; the proxy index keeps `id=hotspot`, `type_name=KeywordHit`, `role=keyword`, declaration provenance, `depth=4000`, and typed `params.channel=choice`. | [`proxy-param-choice-presentation-tree.json`](proxy-param-choice-presentation-tree.json) | `cargo run -p arcweft-cli --features native-capture --quiet -- agent observe samples\rich-text-full-grammar.arcw --read-uri "arcweft://session/cli/frame/0/presentation-tree.json?proxy_param.channel=choice" --mode drain --steps 16 --max-ops 256` |
| `SVR-2026-06-18-002` | The resolved proxy object can be captured like an image/model object. Color, object-id, and mask captures all target `object.dialogue.0.7.proxy.11.0`, report `content_pixels=521`, and preserve the same proxy metadata in compact capture metadata. | Color: [`proxy-hotspot-color.png`](proxy-hotspot-color.png); object-id: [`proxy-hotspot-object-id.png`](proxy-hotspot-object-id.png); mask: [`proxy-hotspot-mask.png`](proxy-hotspot-mask.png); metadata: [`proxy-hotspot-capture-metadata.json`](proxy-hotspot-capture-metadata.json) | `cargo run -p arcweft-cli --features native-capture --quiet -- agent observe samples\rich-text-full-grammar.arcw --json --image png --out docs\implementation\visual-sample-review-issues\2026-06-18\proxy-hotspot-color.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.7.proxy.11.0`; repeat with `--capture object-id` and `--capture mask` for the object-id and mask artifacts. |
| `SVR-2026-06-18-003` | Source-local `#[text_shader] #[pure]` helpers now register both glyph-pass and `phase=post_process` entry points under the same shader ID. While reviewing the PNG, isolated object color capture was also fixed so unselected neighboring shader passes do not leak into the selected text object's crop; wrapped run crops may still be wide because a single run spans multiple visual line fragments, but only the selected run pixels remain visible. | [`effects-animation-source-local-post-shader-fixed.png`](effects-animation-source-local-post-shader-fixed.png) | `cargo run -p arcweft-cli --features native-capture --quiet -- agent observe samples\rich-text-effects-animation.arcw --json --image png --out docs\implementation\visual-sample-review-issues\2026-06-18\effects-animation-source-local-post-shader-fixed.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.0.run.7 --capture-time 4` |
| `SVR-2026-06-18-004` | Source-local `#[text_effect] #[pure]` helpers now register both glyph placement/color and `phase=post_process` entry points under the same effect ID. The effects animation sample uses `[.source_drift phase=post_process ...]`, native capture resolves it through the source-local registry, and the object crop shows only the selected text run with deterministic post-process tint. | [`effects-animation-source-local-post-effect-fixed.png`](effects-animation-source-local-post-effect-fixed.png) | `cargo run -p arcweft-cli --features native-capture --quiet -- agent observe samples\rich-text-effects-animation.arcw --json --image png --out docs\implementation\visual-sample-review-issues\2026-06-18\effects-animation-source-local-post-effect-fixed.png --mode drain --steps 16 --max-ops 256 --object object.dialogue.0.1.run.3 --capture-time 4` |
| `SVR-2026-06-18-005` | Glyph, glyph-cluster, and ruby children now preserve semantic object metadata in object-scoped image resources, not only in the original observation object table. A returned crop's `image.object.rich_text_ref` matches the observed child object, including source range, presentation metadata, semantic layer/depth, hit regions, and capture refs, so a debugger can inspect a saved child crop as a standalone text object descriptor. | Source-level JSON/PNG evidence is generated by the focused CLI regression; fixed PNGs are not checked in because the assertion is metadata identity rather than a new visual appearance. | `cargo test -p arcweft-cli --features native-capture --test check agent_observe_reports_text_presentation_z_index_depth -- --exact --nocapture` |
| `SVR-2026-06-18-006` | `#[rich_text_proxy]` / `#[text_proxy]` declaration metadata now survives through object-scoped image metadata as the full `rich_text_ref`, including selected `presentation.object_proxies[]`, declaration attribute family, typed params, object layer/depth, and hit regions. This keeps custom text proxies usable like image/model objects after a crop has been saved or read through MCP. | Source-level JSON evidence is generated by focused CLI and MCP regressions; no PNG artifact is needed because the change verifies image-resource metadata transport. | `cargo test -p arcweft-cli --features native-capture --test check agent_observe_infers_rich_text_proxy_struct_attribute_family -- --exact --nocapture`; `cargo test -p arcweft-agent-mcp -- --nocapture` |
| `SVR-2026-06-18-007` | `capture_time` / `capture_step` are now documented and exposed to MCP clients as native rich-text animation sample time, not a typewriter-only special case. The contract covers effects, shaders, registry-backed motion functions, animated proxy bounds, hit-testing, object-id/mask attachments, and partial image capture while keeping source ranges and object ids stable. | Tool-schema and documentation evidence is generated by MCP unit tests plus the CLI/tooling contract docs. | `cargo test -p arcweft-agent-mcp -- --nocapture`; `cargo check -p arcweft-agent-mcp --tests`; `cargo clippy -p arcweft-agent-mcp --all-targets --all-features` |
| `SVR-2026-06-18-008` | Missing registry-backed motion functions now produce structured diagnostics in the same places as missing custom effects and shaders: the top-level observation report, the selected image metadata, and the image resource returned by `--read-uri`. The diagnostic carries `source=native_rich_text`, `code=missing_motion_function`, and `effect_id=motion`, so an LLM debugger can explain a failed animated text crop without separately replaying the frame. | Source-level JSON evidence is generated by the focused CLI regression; no PNG artifact is needed because the change verifies diagnostic transport. | `cargo test -p arcweft-cli --features native-capture --test check agent_observe_native_rich_text_reports_missing_motion_diagnostics_in_image_resources -- --exact --nocapture` |

The reviewed object is intentionally small: the color crop reads as the visible
word `proxy`, the object-id crop uses the proxy object's stable id color, and
the mask crop covers the same glyph region. This proves the same typed proxy
identity is usable for semantic discovery, partial image capture, object-id
capture, and mask capture.

