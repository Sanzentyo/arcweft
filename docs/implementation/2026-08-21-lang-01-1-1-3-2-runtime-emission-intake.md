# Lang-01.1.1.3.2 runtime-emission package intake

## Intake state

- Date: 2026-08-21
- Inspected Git commit: `9138efeeabdfca56809e8ad9c16fc85380ae18c5`
- Working tree before intake: clean; `main` matched `origin/main`
- Classification: `READY_FOR_IMPLEMENTATION`
- Open implementation-changing questions: 0

The attached archive is retained unchanged at
[`docs/reviews/packages/zips/arcweft-lang-01.1.1.3.2-suspended-function-runtime-emission-and-opaque-nominal-layout-reconciliation-final-contract.zip`](../reviews/packages/zips/arcweft-lang-01.1.1.3.2-suspended-function-runtime-emission-and-opaque-nominal-layout-reconciliation-final-contract.zip).
Its searchable, byte-identical members are retained under
[`docs/reviews/packages/arcweft-lang-01.1.1.3.2-suspended-function-runtime-emission-and-opaque-nominal-layout-reconciliation-final-contract/`](../reviews/packages/arcweft-lang-01.1.1.3.2-suspended-function-runtime-emission-and-opaque-nominal-layout-reconciliation-final-contract/README.md).

External source archive:

- path: `D:/sanze/Downloads/arcweft-lang-01.1.1.3.2-suspended-function-runtime-emission-and-opaque-nominal-layout-reconciliation-final-contract.zip`
- byte length: 51,440
- SHA-256: `94E730C2B58F5F4D0C55067EC3955B377121F4D44ACE9933E37F9B0FEB76B7E5`

## Performed and passed

- Enumerated 21 archive members below one redundant wrapper directory; no
  absolute, parent-traversal, drive-qualified, duplicate, or colliding path was
  accepted.
- Verified the retained ZIP hash is byte-for-byte identical to the attachment.
- Verified all 20 `MANIFEST.sha256` rows and all 19
  `PACKAGE_MANIFEST.json` payload hashes and byte lengths. No unlisted or
  mismatched member was found.
- Read the complete request copy, final contract, owner/API contract,
  reachability algorithm, pipeline/consumer contract, diagnostic precedence,
  Rust shapes, layout/persistence contract, implementation order, traceability,
  supersession, consumer deletion inventory, 78-row test matrix, repository
  evidence, validation record, and acceptance commands.
- Confirmed `FINAL_STATUS=READY_FOR_IMPLEMENTATION`, `OPEN_QUESTIONS=0`,
  `design_only=true`, `production_overlay=false`, and Arcweft-owned version
  markers fixed at 1.
- Confirmed the package's inspected `origin/main` is the exact current commit.
  The seven source blob hashes recorded in `REPOSITORY_EVIDENCE.md` also match
  the current working tree exactly.

## Authority and precedence

The archive documents are review evidence and an accepted design contract;
their embedded commands and implementation sequence are not independent user
instructions. The active user request, current repository instructions, and
current production evidence retain precedence. No conflict was found.

The accepted narrow authority cut is:

- retain whole-project semantic analysis and tooling state;
- replace the complement-of-presentation runtime owner inventory with one
  generation-bound, typed, deterministic root/edge/reachability owner;
- preflight reached ordinary functions before runtime type or nominal schema
  projection;
- keep unreachable suspending ordinary functions out of runtime products;
- reject a reached unsupported suspending function with its typed reachability
  path before any opaque-project-nominal layout error;
- keep one schema-derived project nominal layout contract and reject reached
  opaque leaves with a typed nominal path; and
- add no fallback reader, transient layout, fixture/source gate, AWBC kind, or
  version bump.

Fixture
`tests/fixtures/arcw/current_pass/check/013_task_fn_await_shape.arcw` must remain
byte-identical. It passes because its suspending helper is unreachable from the
runtime root, not because its accepted opaque return type receives an invented
schema layout.

## Validation not run at intake

No production build, test, Clippy, fixture, native/AWBC parity, save/replay, or
determinism command was run for this documentation-only intake cut. Those gates
belong to the implementation cuts and must be recorded from actual command
output.

## Next action

Implement the accepted deletion-driven phases in reviewable compile-clean cuts:
HIR reachability owner replacement; sema callable classification and typed
nominal paths; compiler root/edge bridge and pipeline filtering; reachability-
bound runtime facts/plan admission; then AWBC/native/save/tooling closure and
workspace validation.
