# Lang-01.3.1.2.3.2.1.1.1 return intake

Date: 2026-08-12

Inspected Git baseline:
`78f50f5b5ac082745bab91b7373a6602918a436d` on `main`, equal to
`origin/main`. The working tree also contained preserved, unstaged A1
production reconciliation work. No package-intake operation altered or staged
that production work.

## Returned archive

- retained-byte authority:
  `docs/reviews/packages/zips/arcweft-lang-01.3.1.2.3.2.1.1.1-external-opaque-producer-declaration-authority-correction-final-contract.zip`;
- searchable frozen mirror:
  `docs/reviews/packages/arcweft-lang-01.3.1.2.3.2.1.1.1-external-opaque-producer-declaration-authority-correction-final-contract/`;
- byte length: `59,268`;
- SHA-256:
  `63bf3451e18276086d80ce0fda807f63888174ad0bd03bb68a154c4c875b5309`;
- members: 34 files under the archive root, `examples/`, and `validation/`,
  with no redundant wrapper.

The retained ZIP is byte-identical to the attachment. All 34 extracted files
match their ZIP members.

## Validation performed and passed

- ZIP path, drive/root traversal, case-collision, symlink/reparse, CRC, and
  member checks passed.
- `OPEN_QUESTIONS.md` is exactly `none`.
- `SOURCE_REQUEST.md` is byte-identical to the maintained repository request;
  both have SHA-256
  `a7ab7d47f50804bae5a5b9fff1e5e39b7c97922bdda191b444216724d56ba9a7`.
- The retained preceding package independently hashes to the required
  `93af482a2914ca4a9e6b985aa7a09c040f569bd71141611dcaa4d579ac01640c`.
- The returned validator passed with 34 files, 127 unique normative test rows,
  and 61 inventory rows.
- The package repository pin, current source, and local Git baseline all
  identify commit `78f50f5b5ac082745bab91b7373a6602918a436d`.

Python validation was run with `uv run` and process-local `PYTHONUTF8=1`.

## Readiness decision

Classification: `BLOCKED_BY_NAMED_REQUEST`.

The return closes the large external declaration-authority surface: adapter
and Rust-ABI producer IDs, schema-2 hard cuts, derive input, exact external
admission, reserved producer namespace, adapter-sema conversion, generated
source evidence, and the protected G3a/G3b publication switch. Those decisions
remain accepted substrate for the correction.

It is not yet safe to implement G3b because its accepted nominal catalog
digest v2 is internally contradictory. The shared digest rule excludes source
spans, while “retain the existing catalog hasher/non-opaque grammar” retains
production v1's explicit hashing of exact-record and open-rule source spans.
The package also changes three parent-fixed Rust API shapes without declaring
supersession: optional versus required source, crate-private owned-ID versus
public `Arc` constructor, and the accepted inventory visibility type name.

The independently throwable correction request is
[`2026-08-12-lang-01.3.1.2.3.2.1.1.1.1-accepted-nominal-catalog-digest-and-api-reconciliation-correction.md`](../reviews/requests/2026-08-12-lang-01.3.1.2.3.2.1.1.1.1-accepted-nominal-catalog-digest-and-api-reconciliation-correction.md).
Until it returns, lower G1/G2 and mechanically independent G3a work may be
implemented and validated, but G3b and the parent A1.2 acceptance claim must
remain open. A1.3/A1.4 must not be reported complete.

## Not run at intake

No production Rust was changed by this intake cut. Cargo formatting, checks,
Clippy, workspace tests, Tier 2, and structure audit were not rerun for the
archive-copy and evidence-note operation. They remain required by the
implementation gates.
