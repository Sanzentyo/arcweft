# Repository evidence

Date: 2026-09-07. Repository: D:/git/arcweft, existing main checkout.
Inspected accepted Git SHA: 69b30b530f5f1da39f5c8f0d7ee7f0f3de70bd29.
HEAD and origin/main had no ahead/behind commits after fetch. Index was empty.
The initial and post-probe working tree each reported 658 changed/untracked
status entries. The source migration is inherited uncommitted evidence.

## Authority read

The current root/workspace/documentation/review/implementation AGENTS files,
docs/README.md, docs/reviews/README.md and the applicable complete Rust skill
were read for this continuation. Current source and stable language chapters
remain primary evidence.

REQUEST.md is the exact dispatched child request before its navigation status
is updated. The parent request, its AW-AH-009.4.2 parent, the current project
callable design and the call-application authority amendment were inspected
alongside the source seam. Relevant parent design material was also read
during the preceding implementation continuation. None of its historical
readiness labels proves this dirty source is complete.

The parent request/design are themselves part of inherited untracked working
copy inputs. A clone of the accepted main SHA alone cannot reproduce this
investigation. SOURCE_INPUTS.tsv identifies the exact input bytes used here.
It is an input evidence inventory, not a source-spelling acceptance gate.

## Commands actually run in this continuation

1. Git status, diff/index inspection, rev-parse, log/remote comparison and
   fetch: existing main, empty index, HEAD/origin agreement as above.
2. A temporary focused compiler integration probe was added while continuing
   implementation investigation, before this design-only artifact was selected.
   Command:
   cargo test -p arcweft-compiler --test project_function_instances
   one_curried_prefix_supports_distinct_later_generic_arguments -- --exact
   Result: FAILED, 0 passed, 1 failed, 6 filtered out. Compilation itself
   succeeded. Semantic analysis accepted the source; runtime-plan lowering
   rejected the unclosed generic B. The complete source is in
   ACCEPTANCE_MATRIX.md and the actual command output in VALIDATION_EVIDENCE.txt.
   This probe's own test edit was removed; inherited tests were preserved.
3. cargo test -p arcweft-lang-sema --lib final_analysis::tests::generic_calls
   Result: FAILED, 3 passed, 1 failed, 0 ignored, 681 filtered out.
   The failure remains MalformedSchemaInventory on the recursive call's
   rigid/bindable collision. The command output is retained in
   VALIDATION_EVIDENCE.txt.
4. Read-only source inspection covered declaration IDs, schema inventories,
   lexical topology, lower relation/normalization/completion, callback scopes,
   frozen solution bytes, compiler instance closure/discovery, runtime type
   graph, continuation ABI and native/AWBC value application.
5. Review ZIP inventory before authoring: 70 existing ZIPs. No new root inbox
   archive was supplied. The new contract ZIP is locally authored, not a
   returned external package.

No explicit Cargo job count was supplied. Failed guessed-path searches were
retried using paths returned by rg --files; they did not constitute source
evidence or mutate files.

## What was not run or established

This design did not run workspace tests, all-target/all-feature compilation,
Clippy, doctests, structure audit, the full parent acceptance matrix or
executable runtime/codec tests for the new model. The proposed Rust shapes
were not compiled. Package byte/member/hash verification is archive validation,
not compiler or runtime validation.

The previous implementation note
docs/implementation/2026-09-07-content-callable-continuation.md records earlier
passing syntax/HIR/compiler/core controls, workspace check, Clippy and structure
audit, plus the same sema failure. Those are historical results and are not
relabeled as validation of this new design.

No Rust implementation is contained in the package. READY_FOR_IMPLEMENTATION
means the contract's selected result-changing decisions are closed. It does
not mean the inherited Content/Fx/ProjectCall implementation is ready to merge.

## Additional boundary inspected

core/engine/eval/function.rs rejects a structured Executable function in its
synchronous expression apply path. AWBC ApplyFunction accepts the Function
value carrier. This source evidence prevents inventing a new implicit
quantified-prefix-to-callback coercion in this scope correction. General
concrete callback execution remains a parent obligation and is explicitly
not claimed as fixed or newly validated here.

## Package evidence

The retained ZIP and searchable mirror are created from these exact members.
MANIFEST.txt enumerates all members except itself in ordinal order.
SHA256SUMS.txt covers every substantive member, excluding the two index
sidecars to avoid a self-hash cycle. The verifier checks counts, sets, path
safety, hashes and byte equality. The implementation intake note records the
actual ZIP length, SHA-256 and verification outcome after construction.
