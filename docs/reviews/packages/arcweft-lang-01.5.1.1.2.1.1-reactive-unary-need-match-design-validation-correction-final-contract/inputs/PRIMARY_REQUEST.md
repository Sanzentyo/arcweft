# Lang-01.5.1.1.2.1 — reactive unary-Need match reconciliation

This retained primary scope is incorporated in full by the correction package.
The maintained language authority selects ordinary checked `match` in View,
unary `Need<T>` with `NotStarted`, `Pending(Progress)`, `Ready(T)`, and
`Cancelled`, nested `Result`/`Option` inside Ready, and complete deletion of
direct View Await/AwaitView.

The twelve mandatory decisions are: one checked owner; one retained
subscription identity; deterministic publications; generic Match and ordinary
RuntimeValue/AWBC; nested carriers; producer start/dedup/cancellation; v1
wire/save/replay/replacement; old-Await deletion; static contamination; failure
precedence and atomicity; bounded work; and deletion-driven compile-clean order.

Authoritative repository path:
`docs/reviews/requests/2026-08-21-lang-01.5.1.1.2.1-reactive-unary-need-match-reconciliation.md`
at `cec30b57fa734efb059d7b846b397ac7d2b0701a`.
