# arcweft implementation-ready final-contract package

This ZIP is the no-implementation final contract for the request in `REQUEST_SPEC.md`, evaluated against `Sanzentyo/arcweft` `main` pinned at `UNAVAILABLE`.

## Start here

1. `FINAL_CONTRACT.md` — normative implementation contract; all required decisions, test matrix, and implementation order.
2. `REQUEST_SPEC.md` — byte-for-byte copy of the sole task specification supplied by the user.
3. `SOURCE_REVISION.md` — immutable source revision and branch evidence.
4. `VERIFICATION_STATUS.md` — what was actually inspected/executed and what remains planned.
5. `REQUIRED_DECISIONS.md`, `TEST_MATRIX.md`, `IMPLEMENTATION_ORDER.md` — navigable extracts of the corresponding contract sections.
6. `evidence/AGENTS_INDEX.md` and `evidence/AGENTS/**` — applicable repository instruction evidence.
7. `verification/**` — command statuses and logs.
8. `PACKAGE_STATE.json`, `SHA256SUMS.txt` — machine-readable state and integrity hashes.

## Authority and exclusions

`REQUEST_SPEC.md` is the sole product/task specification. The context premise was used only for repository access. No implementation, commit, branch, issue, or pull request was created. The contract forbids compatibility shims, dual readers, source gates, and CSS/Takumi paths.

## Package validation

- Contract repository-aware draft passed pre-package validator: `NO — fallback document used; inspect validation notice`
- OPEN_QUESTIONS: `0`
- Implementation performed: `NO`
- Pinned HEAD equals observed origin/main: `NO`
- Final worktree clean: `NO`
