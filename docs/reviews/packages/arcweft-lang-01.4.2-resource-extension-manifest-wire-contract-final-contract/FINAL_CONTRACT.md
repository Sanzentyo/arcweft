# FINAL CONTRACT — generation fallback

> This fallback was generated because the repository-aware contract draft did not pass the package validator. It intentionally makes no unsupported design claims. The complete authoritative request is in `REQUEST_SPEC.md`.

## Pinned source

- Commit: `UNAVAILABLE`
- Observed origin/main: `UNAVAILABLE`
- Equal: `NO`

## Authoritative request structure

- `# Lang-01.4.2 — Resource extension-manifest wire contract correction`
- `## Sequence position`
- `## Why this correction is required`
- `## Required decisions`
- `## Implementation order to specify`
- `## Tests to specify`
- `## Constraints`
- `## Expected output`

## Contract boundary

Implement only the normative requirements in `REQUEST_SPEC.md` against the pinned commit. Reuse existing implemented and verified substrate unless a concrete failing invariant or test proves a defect. No compatibility shim, dual reader, source gate, CSS path, or Takumi path is permitted. No source changes were performed by this package generation.

## Required implementation deliverables

1. Resolve each normative requirement in `REQUEST_SPEC.md` in its stated ownership layer and exact wire boundary.
2. Add or update tests for every positive, negative, omission/default, round-trip, malformed-input, and regression dimension expressly required by `REQUEST_SPEC.md`.
3. Execute the implementation in dependency order from domain/wire primitives through manifest serialization/deserialization and then integration/regression tests, without introducing alternate readers or compatibility branches.

## Validation notice

This fallback is not the preferred repository-specific contract and must not be mistaken for evidence that unnamed symbols were inspected. See `VERIFICATION_STATUS.md` for the exact executed checks.

OPEN_QUESTIONS=0
IMPLEMENTATION_PERFORMED=NO
COMPATIBILITY_SHIM=FORBIDDEN
DUAL_READER=FORBIDDEN
SOURCE_GATE=FORBIDDEN
CSS_TAKUMI_PATH=FORBIDDEN
