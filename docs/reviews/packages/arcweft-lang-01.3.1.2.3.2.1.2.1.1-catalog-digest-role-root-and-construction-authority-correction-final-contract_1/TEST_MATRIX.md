# Positive, negative, property, compile-fail, golden, tamper, and integration matrix

The CSV contains **671** test rows: 320 retained parent rows plus 351 correction-specific rows. Correction rows include one presence/omission test for every plan mapping row and one correlation test for every AWBC mapping row; they also cover every included/excluded catalog field, all six authored roles, Style uniqueness, both construction domains, exact MakeRecord bytes, shared Choice budget, typed error propagation, and final deletion gates.
