# Non-goals and prohibited alternatives

- No production code, patch, implementation overlay, branch, PR, or compatibility package is in this archive.
- No second generation identity, role root, catalog root graph, custom digest, plan/AWBC binding digest, root map, operational catalog, or construction capability is introduced.
- No display, debug, Serde bytes, source text, names, dense IDs, iteration order, or caller-provided digest serves as canonical authority.
- No `Named` dialogue-role success, public arbitrary role-type constructor, `RuntimeCheckedType::Dynamic`, boolean-only authority validation, generation-blind nominal construction, raw AWBC execution, or project/producer fallback remains.
- No public nominal layout, root ID, producer ID, catalog key, digest, or layout hash is treated as an operational capability.
- No enum behavior is duplicated in a helper trait, extension trait, upper-layer string table, or ad hoc match; behavior for `CharacterDialogueRuntimeRole` is added to its original inherent implementation.
- No Arcweft-owned version is incremented. Every schema, ABI, codec, digest domain, protocol, save, snapshot, and persistence version remains exactly `1`.
