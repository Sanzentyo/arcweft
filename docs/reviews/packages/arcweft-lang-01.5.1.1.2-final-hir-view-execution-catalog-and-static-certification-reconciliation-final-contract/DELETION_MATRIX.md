# Deletion matrix

Deletion is an implementation step, not a source-text-grep acceptance rule. Each row is proved by typed behavior, API visibility, codec rejection, Cargo metadata, or compiler failure.

| Cut | Owner | Superseded surface | Direct replacement | Deletion evidence |
|---|---|---|---|---|
| C2 | arcweft-lang-sema | `CheckedViewCall` as the only View semantic projection | Retain it only as an internal call-classification input while `CheckedViewCatalog` is built; make compiler consumers unavailable. | The catalog completeness test proves one row for every accepted View node. |
| C3 | arcweft-resource-model | compiler/runtime string lookup for configured resource values | Add inherent contextual `ResourceRefValue::{to_runtime_value, try_from_runtime_value}` on the owning type and migrate callers. | Wrong nominal layout/type/registry generation tests fail before catalog lookup. |
| C3 | arcweft-compiler / runtime-plan | View-only expression interpreter or Fx-scalar substitution for ordinary values | Generate ordinary synthetic AWBC functions through the existing value/function lowerer. | String, nominal, sequence, default, key, and handler-input end-to-end tests pass. |
| C4 | arcweft-compiler::view | `MissingCheckedViewProjection` and its generic fallback arm | Switch all accepted current View shapes to the generation-matched checked catalog in the same product transaction. | No accepted node reaches an unavailable projection; intentionally corrupt catalogs fail `compiler.view.catalog.incomplete`. |
| C4 | arcweft-compiler::view | literal/dialogue-only `lower_expression`, argument-free element gate, ordinal Text IDs, hard-coded zero bounds | Replace with catalog-driven definition/node lowering and typed source identity. | The replacement `view_product` matrix covers all current shapes and stable identities. |
| C4 | arcweft-view / arcweft-bundle | `ViewValueProgramInventory` as an executable Fx-only value-program owner | Move Fx graph/program ownership back to its Fx owner and use AWBC references for generic View values. | Public/API compile-fail tests show no generic View caller can construct the removed evaluator. |
| C4 | arcweft-bundle ViewProgram/ViewText/Input/Style | static-only authored fields and optional dynamic program fields that silently default | Directly replace each with native constant or required typed projected-program binding. | Exact constant/dynamic pairs and missing-program tamper rows pass. |
| C4 | arcweft-bundle ViewText | stringly `Projection { path }` and `Local { name }` source variants | Use `RuntimeProgram` with typed program and projection; preserve typed Dialogue optimization. | No string path/name lookup remains reachable through public codec/runtime APIs. |
| C4 | arcweft-view | ad hoc match lowering outside the owning instruction enum | Add `ViewInstruction::Match` and its inherent validation/visit/dependency behavior directly. | Exhaustive enum/API tests cover Match without extension traits or mirror dispatch. |
| C4 | arcweft-bundle image/View product | old `Image` View builtin expectation and untyped/static image IDs | Use exact `ResourceRef<Image>` binding on `EmitImage`. | PNG/JPEG/GIF/WebP and mismatch/stale-resource tests pass. |
| C4 | arcweft-compiler tests | seven stale pre-Proof `view_product` expectations and obsolete literal-text rejection codes | Replace, do not rename, with final-HIR catalog/product matrix. | No test asserts a removed stage/code/cardinality or Image element constructor. |
| C5 | arcweft-runtime-driver evaluator | generic debug/display stringification and placeholder coercion from `RuntimeValue` | Apply only `ViewValueProjection` after exact runtime checked-type validation. | Every unsupported projection is a typed runtime error with no partial frame. |
| C5 | runtime-driver/native/Web/headless/Agent/MCP | endpoint-specific View value/resource/part resolver tables | Consume one validated `ViewProgramCatalog` and canonical frame/observation path. | Cargo metadata/API tests prove no endpoint catalog authority exists. |
| C5 | save/replay | any persisted certificate selection, decoded fragment cache, or renderer tree | Persist only existing schema-v2 semantic mount/binding state and bind to artifact identity. | Save bytes are independent of dynamic versus certified execution path. |
| C6 | compiler/runtime cache | cache keys based only on source ranges, ordinals, or source spelling | Use stable product identities and deterministic semantic/dependency/certificate digests. | Trivia/range changes preserve semantics while semantic/dependency changes invalidate. |
| C7 | syntax/sema attributes | unchecked `#[static]` hint or compiler-only bypass | Parse the current attribute form and attach a typed requirement to the same automatic proof result. | Success/failure diagnostics share exact proof and first contaminant. |
| C8 | all affected crates | compatibility aliases, dual readers, legacy dispatch, source gates, removed-syntax diagnostics, helper traits around owning enums | Delete directly after all typed callers migrate. | Compile-fail APIs, Cargo metadata, workspace tests, and structure audit close the boundary. |
