# Retained parent substrate

The directly retained retry archive is byte-identical to SHA-256 `e0aa31dfefa5bc0d9fab213d19fef6fd74a142cef6dd7d4e6922d05c077bc998`. This correction does not reopen:

- the version-1 CharacterCatalog and ViewRegistry digest domains/transcripts/order/limits;
- the six authored `std.character_dialogue` opaque role meanings and derived Style Choice;
- lossless 32-byte projection from `RuntimeSemanticTypeId` to distinct project/producer root newtypes;
- distinct project and producer root error owners;
- one parent `AdmittedRuntimeGeneration`, generation-bound borrowed construction domains, and no capability escape;
- the version-1 AWBC nominal-domain table and mandatory project/producer operand on `MakeRecord` and record constants;
- exact Variant owner/ordinal/name/payload validation, unique Choice, one work budget of 65,536, nesting limit 64, ordered branch evidence, and structured error propagation;
- quarantine of raw plan/AWBC artifacts, crate-private nominal construction, and direct replacement without version increments or compatibility readers.

The only superseded retry artifacts are the duplicate `pattern::RuntimeValuePath`, serialized conceptual root-use rows, generic AWBC `slot: u32`, impossible validator phase order, incomplete outer-shape table, guessed raw nominal semantic identity, runtime-driver-owned dialogue catalog wrapper, caller-provided generation scalar, unsupported Character-to-View relationship check, and nonexistent `AcceptedNominalEnvironment` issuance API. Their exact replacements are Decisions 01–12 in this archive.
