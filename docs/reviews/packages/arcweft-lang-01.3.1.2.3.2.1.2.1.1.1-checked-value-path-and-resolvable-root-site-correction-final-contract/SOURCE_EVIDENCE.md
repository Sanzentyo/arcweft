# Current-main source evidence

`SOURCE_EVIDENCE.csv` contains 43 commit-pinned files with byte hashes, sizes, line counts, and the precise design question each supported. Every row is pinned to `36f83f8509417d1110a34f1b32aee6f4a113dcf3`; no current-source conclusion is based only on a conversation summary or a filename.

The container could not create a Git checkout because DNS resolution for `github.com` failed. The exact clone command and stderr are preserved in `git-clone-current-main.log`. The permitted fallback was commit-pinned raw GitHub retrieval. This archive does not claim a local clean tree, Cargo run, or production validation.
