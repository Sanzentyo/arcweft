# Non-goals and prohibitions

- no production source, patch, diff, branch, PR, overlay, implementation crate, or compatibility package in this archive;
- no second `RuntimeValuePath`, conversion path, string diagnostic path, or alternate Serde authority;
- no serialized plan/AWBC root-use row and no root/type claim in `AwbcTypedOrigin`;
- no generic unshaped `slot: u32`; repeated indices appear only in field-named closed variants with normative tables;
- no new digest/root map for plan-to-AWBC equality;
- no `RuntimeCheckedType::Dynamic`, guessed nominal semantic ID, name/layout reconstruction, or boolean construction authority;
- no placeholder admitted generation, public validator constructor, nullable/default authority, or generation-erasing capability;
- no runtime-driver-owned dialogue catalog bridge, caller-provided target generation, or unsupported Character-to-View relationship;
- no nonexistent `AcceptedNominalEnvironment`, post-publication role mutation, relevant `Named` success path, helper trait, or side table for role behavior;
- no schema/ABI/codec/digest/protocol/persistence version increment; all remain version 1.
