# Role issuance API

`decision-12-role-issuance.md` is normative. The exact atomic owner chain is `TypeCheckEnv` → `AcceptedNominalWorld` → `CharacterDialogueRuntimeRoleRegistry` → callable substitution/custom fields → `RegisteredTypeCheckEnv`. No other issuance API exists.
