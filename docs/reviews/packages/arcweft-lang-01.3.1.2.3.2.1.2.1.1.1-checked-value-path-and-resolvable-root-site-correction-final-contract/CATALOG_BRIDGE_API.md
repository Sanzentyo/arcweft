# Catalog bridge API

`decision-11-catalog-admission-bridge.md` is normative. This sidecar exists for implementation lookup and contains no alternate owner. The bridge owner is `arcweft-dialogue::character_dialogue`; provenance is issued by `AdmittedRuntimeGeneration`; runtime-driver only calls and passes it.
