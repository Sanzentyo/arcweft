# Required precedence

## Checked value

1. shared work charge; 2. depth; 3. outer physical shape; 4. local width/owner/length/variant facts; 5. checked index/path push; 6. recursive child; 7. all Choice alternatives in source order; 8. nominal descriptor lookup and defining-order fields; 9. enclosing domain publication.

## RuntimePlan admission

Raw limits/duplicates/path wire shape → generation declaration equality → type declaration resolution against admitted generation → exact current-owner traversal → expression/pattern completeness → lexical/signature/operator/constructor checks → derived site map uniqueness → project/producer reachability → admitted wrapper publication.

## AWBC admission

Header/version/table limits → ranges/bounds/duplicates → runtime-type/constant/pattern cycle checks → signature/frame/function ownership → named instruction/terminator/table slot resolution → nominal domain resolution → declaration-to-generation correlation → coordinate-only origin validation → admitted AWBC publication.

## Plan/AWBC pair

Both sides admitted independently → origin pair uniqueness → actual AWBC site resolution → collapse identical rows per plan site → direct transcript equality → admitted product publication.

## Catalog/dialogue

Character local digest → View local digest → custom catalog digest → borrowed generation digest comparison → custom-field View relationship → role/custom producer correlation → checked value validation → canonical domain decoding → atomic publication.
