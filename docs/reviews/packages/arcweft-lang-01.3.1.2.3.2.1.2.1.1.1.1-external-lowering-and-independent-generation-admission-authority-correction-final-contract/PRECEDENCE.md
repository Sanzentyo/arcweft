# Precedence

1. Current production at the full Git commit in `CURRENT_MAIN_COMMIT.txt`.
2. The maintained request copied byte-for-byte as `SOURCE_REQUEST.md`.
3. This package's decision files, exact API documents, mapping tables, test
   matrix, inventory, and implementation order.
4. Retained parent files explicitly listed in `RETAINED_PARENT_SUBSTRATE.md`.
5. Historical parent wording not retained here.

For the eight defects in the current intake note, this package replaces the
parent's impossible `pub(crate)` cross-crate construction, raw self-admission,
upward fact ownership, recursive opaque payload assumption, command-only audio
coordinate, incomplete expression root rule, circular context order, and
contradictory tests. No compatibility alias or dual interpretation survives.

When prose and a table disagree, the exact Rust API plus the row keyed by the
same owner/coordinate is normative. `REQUEST_DECISION_MATRIX.csv` identifies
that pair for every request decision.
