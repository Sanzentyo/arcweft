# Producer/consumer/deletion inventory

Total rows: **836**. Retained-and-corrected parent rows:
**772**. New correction rows: **64**.

The CSV includes current/final owners across core, runtime-plan, compiler, sema,
verifier, VM, AOT, bundle, save/snapshot, runtime-driver restore/replay,
dialogue, Character, View, tests, and maintained documentation. Parent rows
whose final wording depended on command-only audio, `AudioValue`, recursive
opaque validation, missing expression roots, or raw self-admission are replaced
in place. New rows name every cross-crate constructor/issuer/consumer/deletion
boundary.

Layer counts: `AOT`=1, `AWBC VM diagnostics`=1, `AWBC audio`=6, `AWBC audio slot`=35, `AWBC deliberate exclusion`=2, `AWBC instruction slot`=63, `AWBC nested typed site`=43, `AWBC site deletion`=1, `AWBC terminator slot`=13, `AWBC top-level typed site`=17, `RuntimePlan coordinate descent`=22, `RuntimePlan deliberate exclusion`=1, `RuntimePlan nested typed site`=69, `VM`=2, `View`=1, `View generated products`=1, `View runtime restore`=1, `admission and checked value`=7, `affine ownership`=1, `affine ownership binary`=1, `bundle`=1, `character`=1, `checked-type canonical grammar`=20, `compiler`=1, `core AWBC`=194, `core AWBC deletion`=2, `core AWBC origin`=1, `core AWBC product execution`=3, `core checked validation`=11, `core generation`=1, `core nominal`=1, `core path consumers`=2, `core path deletion`=1, `core plan`=116, `core plan deletion`=1, `core plan/AWBC`=1, `core runtime value`=23, `core validator`=1, `core value path`=11, `core wire`=2, `dialogue`=1, `dialogue bridge`=1, `dialogue deletion`=2, `dialogue patch`=1, `dialogue typed value`=1, `documentation`=2, `execution deletion`=1, `expression nodes`=6, `function capture`=1, `general diagnostics`=1, `generation authority`=10, `generation persistence`=5, `integer-width canonical grammar`=12, `iterator values`=1, `iterator witness`=1, `nominal deletion`=1, `nominal record`=1, `opaque value`=1, `plan/AWBC reference`=19, `raw construction`=11, `replay ingress`=1, `root ingress`=1, `runtime driver`=3, `runtime-plan expression`=47, `runtime-plan pattern`=11, `save crate`=1, `sema deletion`=1, `sema role issuance`=1, `session save/restore`=1, `snapshot`=1, `tests`=4, `tuple/record column`=1, `variant value`=1, `verifier`=1.

No inventory row authorizes a compatibility alias, dual reader, temporary
public field, placeholder generation, or disabled validation branch.
