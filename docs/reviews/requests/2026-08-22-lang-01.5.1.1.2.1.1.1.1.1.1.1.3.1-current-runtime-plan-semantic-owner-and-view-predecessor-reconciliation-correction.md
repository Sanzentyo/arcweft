# Lang-01.5.1.1.2.1.1.1.1.1.1.1.3.1 — current RuntimePlan semantic owner and View predecessor reconciliation correction

Status: `OPEN_DESIGN_REQUEST`

## Parent, split reason, and precedence

This is the blocking correction for the returned task-plan semantic child
encoder and seal contract. That return correctly removes task-plan self-digest
cycles and preserves the core-to-bundle dependency direction, but it was based
on Git commit `515bb071437c3af053f1560c3119906dc8002efc` before the actual Cut 3
and Cut 4 results on current `main`.

Current maintained implementation evidence takes precedence:

- Cut 3 deliberately publishes no `ViewMatchSiteId`,
  `CheckedViewMatchAdmission`, or compiler-local View Match catalog;
- request `.1.2` owns generic Match completion and the View declaration/body
  semantic path;
- request `.1.4` depends on `.1.2` and owns retained View operations, slots,
  captures, stable site, checked admission, and their executable product; and
- Cut 4 publishes only the standalone core identity/catalog substrate, not a
  structured task-plan row or table.

The returned archive's final status and `OPEN_QUESTIONS=none` are package
claims, not current implementation authority. Do not reopen numeric allocation,
producer-instance identity, accepted revision semantics, scheduler
transactions, or two-phase restore.

## Current repository contradiction to close

The returned executable transcript requires values which current
`RuntimePlan` rows do not carry and whose same-cut accepted owners are not
defined:

- local storage/mutability, initialization, and owner roles;
- accepted record-field and variant-case semantic identities;
- accepted function semantic identity, modes, return, endpoints, and effect
  contract;
- endpoint/accepted-field/value-source rows for a hypothetical
  `RuntimeHostTaskRequestTemplate`; and
- a hypothetical `RuntimeControlEffectContract(Id)` table.

Current production instead has type-only `RuntimeLocalDeclaration`, name/type
record and variant rows, params/captures/body-only `RuntimeFunctionSite`, and a
`HostTaskRequestTemplate` containing capability, operation spelling, and
runtime expression arguments. A design cannot call the missing roles
"existing owner placeholders" and remain implementation-ready.

## Decisions required

Return one coherent correction that closes all of the following.

1. Give the exact implementation order
   `.1.2 -> .1.4 -> task-plan semantic integration -> Cut 5`, and identify
   which pieces of this correction may remain private before each predecessor
   without publishing an unused side table or provisional public schema.
2. Inventory every field of all current `RuntimePlan` tables and map each
   executable transcript atom to one current accepted typed owner. Where
   current rows lack required semantics, define the exact sema/lowering seed,
   final core field/type, constructor visibility, validation, and same-cut
   deletion of the name-only or lookup-only route.
3. Define `ProducerFunctionSemanticDigest` from the actual final
   `RuntimeFunctionSite` authority. State how an accepted function semantic
   identity, parameter/receiver modes, result, captures, endpoints, and body
   are constructed, or remove any role that has no execution-semantic owner.
   No source name, raw HIR ID, allocation ordinal standing in for identity, or
   whole-catalog digest is allowed.
4. Define `TaskRequestTemplateDigest` against the actual final Host/runtime
   request-template type and Cut 4 `HostOperationCatalog`. Close capability,
   operation, positional/named/spread, receiver/capture, AwaitMany, timeout,
   and line roles with exact current typed evidence. Do not invent accepted
   field/name identities without defining their legitimate owner and
   construction path.
5. Define `ControlEffectContractDigest` and its referenced ID/table from
   constructible current checked effect/control evidence. Inventory every
   current control/effect family, state where it is retained in the final plan,
   and delete any string/debug or parallel effect-contract lookup route.
6. Reconcile record/variant executable rows with request `.1.2`'s accepted
   field/case identities and current core nominal/type tables. There must be
   one projection path, not a copied sema catalog beside name-only core rows.
7. Consume request `.1.4`'s actual compiler-local View product. Define the
   final `ValidatedViewTaskPlanBinding` only after stable site/admission and
   the executable `CompiledViewProduct`/`ValidatedViewProgramResource`
   consumer exist. The bundle owner must consume a stable lower-layer product,
   not `CompilerLocalViewMatchCatalogRow` or another compiler-owned type that
   would introduce a `bundle -> compiler` dependency. Accepted revision
   remains freshness evidence and is not hashed.
8. Prove the plan-build coordinate owner crosses runtime-plan lowering,
   bundle validation, private decode, and common sealing without a raw token,
   foreign-candidate join, second mutable plan owner, or publication of a
   partial `RuntimePlan`. Reconcile this with the current Arc-backed
   `RuntimePlanConstructionIssuer`; do not replace it with an unspecified
   `NonZeroU32` allocator. Define the public seed/private-row constructor path
   that lets `arcweft-runtime-plan` submit a static task row despite
   `RuntimeTaskPlan` fields remaining private.
9. Close the cross-crate digest completion seam. The bundle authority must not
   be able to omit, reorder, or replace the core-owned base prefix, and core
   must not copy View identity types or expose a general sink, caller digest,
   raw digest constructor, or extension trait. State why any public authority
   implementation boundary is trusted and how misuse fails.
10. Define the exact orchestration API for runtime-plan lowering to retain the
    unsealed builder/coordinate authority, validate upper View bindings, and
    seal that same candidate once. Current lowering calls `finish()` and
    returns a public `RuntimePlan`; the correction must not require a bundle
    owner to recover or reopen that consumed builder.
11. Preserve the existing live `ViewTaskPlanAuthority` validation obligations
    while evolving its semantic-seal role. Do not silently replace generation,
    producer, outcome, and request validation with digest construction only.
12. Provide exact deterministic first-error precedence and limits after the
    actual `.1.2`/`.1.4` owners are joined, plus a deletion-driven compile-clean
    Cut 5 sequence that migrates all consumers before exposing the final row
    and table.

Every Arcweft-owned version marker remains exactly `1`.

## Retained decisions

Unless repository evidence proves a conflict, retain:

- the accepted task-plan transcript domain and seven-role prefix;
- no task-plan self/expected digest field;
- construction coordinates rather than completed keys inside executable
  semantics;
- private decoded expected bytes checked only after recomputation;
- one core static row/table and one common builder/decode sealer;
- actual View identities only in the upper validated bundle/View owner;
- no View authority lookup for an ordinary-only plan; and
- one global duplicate check before atomic publication.

## Required tests and validation

- repository fixtures proving every executable transcript atom is obtainable
  from one current or explicitly introduced accepted owner;
- compile failures when any required sema/lowering field is omitted or a
  source name/raw HIR ID is substituted;
- differential tests for all current RuntimePlan table rows, child order,
  task coordinates, request shapes, control/effect rows, and accepted
  field/case/function identities;
- `.1.2` and `.1.4` generation/path/product mismatch negatives;
- foreign build-coordinate, missing/extra/duplicate/stale View binding,
  expected-key mismatch, cross-family duplicate, cycle, and exact-limit/
  one-over tests;
- a malicious or incomplete View authority cannot seal a digest with a
  missing/reordered core prefix;
- builder and private decoder use one sealer and publish no partial plan;
- structured Cargo metadata proves no `arcweft-core -> arcweft-view` or
  `arcweft-core -> arcweft-bundle` edge; and
- focused core/sema/compiler/runtime-plan/bundle/codec tests, deterministic
  fixture comparison, Clippy record, and structural absence gates.

## Non-goals

- no production patch in the returned archive;
- no implementation of `.1.2` or `.1.4` inside this request;
- no numeric allocation, Need producer-instance, scheduler transaction,
  snapshot, or two-phase restore redesign;
- no source spelling, debug text, raw HIR/plan allocation identity,
  caller-supplied transcript rows, copied View projection, public digest/base
  constructor, general byte sink, extension trait, self digest, expected digest
  field, parallel task/effect catalog, or generic Serde transcript; and
- no V2 type, version bump, old reader, fallback, compatibility alias, or
  optional legacy field.

## Required returned archive

Return exactly:

`arcweft-lang-01.5.1.1.2.1.1.1.1.1.1.1.3.1-current-runtime-plan-semantic-owner-and-view-predecessor-reconciliation-correction-final-contract.zip`

The archive must contain the complete final contract, Rust-shaped schemas,
current owner/consumer and dependency matrices, exact executable/child/View
transcripts, predecessor graph, cycle and cross-crate authority proofs,
builder/decode state machines, compile-clean deletion order, exhaustive test
matrix, repository-aware validator and negative self-tests, manifest, source
inventory, `FINAL_STATUS`, and `OPEN_QUESTIONS`. It may claim
`READY_FOR_IMPLEMENTATION` only when every result-changing decision above is
closed and `OPEN_QUESTIONS` is exactly `none`.
