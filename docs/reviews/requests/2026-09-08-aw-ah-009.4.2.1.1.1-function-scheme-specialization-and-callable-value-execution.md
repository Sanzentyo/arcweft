# AW-AH-009.4.2.1.1.1 — Function-scheme specialization and callable-value execution

- Date: 2026-09-08
- Status: `ACTIVE`
- Parent: [AW-AH-009.4.2.1.1](2026-09-07-aw-ah-009.4.2.1.1-recursive-generic-call-constraint-scope-reconciliation.md)
- Parent execution authority: [AW-AH-009.4.2.1](2026-09-01-aw-ah-009.4.2.1-project-callable-attached-content-declaration-and-runtime-abi-correction.md)
- Assignment: design only. The returned design must not edit production code,
  tests, manifests, branches, patches, or implementation overlays.
- Output language: English.

## Required outcome and precedence

Close one complete contract connecting function schemes, use-site
specialization, frozen generic solutions, curried values, source evidence,
closed-instance discovery, and native/AWBC function-value execution. These
decisions constrain one another and must be adjudicated together. This is a
correction to the parent scope contract, not permission to leave ordinary
callback execution outside the implementation cut.

The current user-directed [convergence goal](../../implementation/2026-09-08-convergence-goal-plan.md)
requires reassessing the previous prohibition on a published polymorphic
prefix being used at a monomorphic callback type. Do not preserve that
prohibition because the corresponding runtime route is missing. Distinguish
rank-one use-site instantiation from binding a monotype hole to an entire
scheme, higher-rank checking, impredicative inference, and arbitrary
let-generalization. Decide each from the language's ownership and inference
rules and complete consumer needs. The old blanket prohibition and the
current reproducer expectations are inputs to adjudication, not substitutes
for that decision.

Preserve accepted and validated source/HIR, physical argument ordering,
attached-content/default ownership, and same-fiber call/return/suspension
substrate unless a concrete source-evidenced flaw requires a change. Identify
each such flaw and change explicitly. Parent non-goals must not excuse the
missing callable-value integration. All Arcweft-owned contract versions stay
`1`; evolve unreleased shapes directly.

## Dispatch and current evidence

- Existing repository: `D:/git/arcweft`, branch `main`.
- Inspected accepted commit:
  `4da7bfd7a9f8324f3763d9e579fce1f0ab38965d`.
- The checkout is dirty and the index is empty. It contains inherited
  Content/Fx/ProjectCall work and an in-flight generic-reference migration.
  The current sema/compiler test targets compile, but the callable execution
  matrix still fails. Supply this checkout or an exact
  snapshot of its changed and untracked inputs; a clone of the commit alone
  does not contain the relevant implementation.
- Current implementation evidence:
  [constructor schemas and nominal roots](../../implementation/2026-09-08-constructor-schema-and-nominal-roots.md),
  [contextual variant owners](../../implementation/2026-09-08-contextual-variant-owner.md),
  [saved-prefix reapplication](../../implementation/2026-09-08-prefix-reapplication.md),
  [function-scheme inventory](../../implementation/2026-09-08-function-scheme-inventory.md),
  [flat instance/template projection](../../implementation/2026-09-08-flat-instance-and-template-projection.md),
  [generic migration](../../implementation/2026-09-08-generic-scope-migration.md)
  and [earlier execution tests](../../implementation/2026-09-08-callable-execution-continuation.md).
  Earlier test results do not validate the current tree.
- Re-read Git state, applicable instructions, current source, and maintained
  language documentation. Record full Git SHA, dirty/index state, input paths,
  byte lengths, and SHA-256. Do not use conversation summaries or a `FINAL`
  filename as implementation authority. Use Git only; historical Jujutsu
  requirements are superseded.
- Read the complete parent requests, the maintained functions/pipeline
  chapter, the call-application authority amendment, and the parent contracts,
  Rust shapes, matrices, and consumer inventories. Frozen archives and
  extracted mirrors must remain byte-identical.

The retained scope archive is
`docs/reviews/packages/zips/arcweft-aw-ah-009.4.2.1.1-recursive-generic-call-constraint-scope-reconciliation-final-contract.zip`
(45,039 bytes; SHA-256
`6c6d2f0885a7f9e76e815e09be397556bac99b3097d290255eee66f75ad32185`).
Its `FINAL_CONTRACT.md` sections 7–8 prohibit later monomorphic specialization
of a published prefix and leave concrete callback execution to the parent.
Those clauses are specifically under reconsideration here.

## Concrete contradictions

### Schemes need an application authority of their own

The current type migration distinguishes Free declaration references, Bound
lexical slots, and application-local Inference references. Functions have
type/constant/effect binders. Completed type/constant rows reify future
variables into a residual binder and reopen it freshly on continuation use.

The working copy now admits declaration Free references and root-bound
type/const scheme slots as scoped inventory and completed template keys.
`CallableSignatureSchema::for_function_value` consumes the binder. A scheme's
bound slots do not acquire a new source declaration when passed as an argument.
Review this implementation together with the still-required function-scheme
opening at value uses and generic effects. Do not fabricate a declaration
owner or encode an application issuer as a stable parameter ID.

### Current value routes are disjoint

`arcweft-core/src/value.rs` has distinct `RuntimeValue::Function` and
`RuntimeValue::ProjectContinuation` payloads. AWBC `ApplyFunction` requires
the former. Native function-value application now uses the common same-fiber
`FunctionCallFrame` invocation/return/unwind path, shared with ProjectCall
targets and omitted defaults. Its pure expression evaluator still rejects an
executable FunctionSite. Root and project closures now share the same closed
instance/ABI producer and consumers. See the
[closure and invocation evidence](../../implementation/2026-09-08-closure-instances-and-function-invocation.md):
at that checkpoint, 1,212 library tests passed, including native await/resume;
the 50-case paired execution matrix had 36 passes and 14 failures. This did not close the
disjoint continuation/function-value payload or use-site specialization model.

The subsequent [evaluation-order and nominal AWBC evidence](../../implementation/2026-09-08-call-evaluation-order-and-nominal-awbc.md)
repairs eager child materialization, callee-first evaluation, nominal field
read/write consumers and invalid record-name recovery. At that checkpoint, callable
execution was 41 passed / 12 failed out of 53 tests (including one negative
codec/verifier case). Stronger named block-argument and block-LHS pipe tests
failed before execution. The [block-binding context follow-up](../../implementation/2026-09-08-block-binding-context.md)
now closes those two failures through contextual statement-binding inference;
both stronger fixtures execute successfully in native and AWBC. Its current
callable matrix is 43 passed / 12 failed out of 55 tests, including a new
closure-in-block pair. The six remaining failing families below are still
required. Preserve candidate-local inference and rollback when closing the
evaluation/constraint contract. The subsequent
[higher-order effect ownership investigation](../../implementation/2026-09-08-higher-order-effect-ownership.md)
adds nonempty, independently combined, unused and separately instantiated row
probes. At that checkpoint the main executable matrix was 43 passed / 14 failed out of 57;
the new uninvoked-callback pair passes sema but still fails runtime projection.
Its selected parameter/prefix ABI exposes a stale empty row, and a strengthened
catalog regression shows the same empty row in a closure's function value type
despite a nonempty final closure catalog. The subsequent
[contextual effect hint repair](../../implementation/2026-09-09-contextual-effect-hints.md)
exposes authored bounds during source checking and retains effect variables in
parametric hints. Nonempty unused-callback and retained-prefix ABI tests now
pass, including closures inside blocks, branches and aggregates. Inferred
invocation rows and complete declaration-to-instance effect projection remain
unfinished; the linked record distinguishes current validation from these
earlier failures. The earlier
bare-local-assignment probe also requires explicit language/place adjudication;
do not infer a final restriction from the current nominal-field-only assignment
model or introduce a special assignment variant merely for that example.

Consequently, even a fully monomorphic curried prefix cannot currently be
used by an ordinary callback parameter. Merely changing type compatibility
or `value_matches_type` would publish a value that its consumer cannot call.
The result must give every admitted callable value one legitimate execution
route, including returns, suspension, effects, and cleanup.

The [closure instance and invocation repair](../../implementation/2026-09-08-closure-instances-and-function-invocation.md)
connects root and project closures to shared executable projection and native
function call frames. Preserve that substrate. A closure containing a project
call needs the Flow algebra even when its effect set is empty; effect spelling
or a pure label cannot select an expression-only body route. Remaining scheme
and continuation changes must preserve capture, return and suspension behavior.

### Completion must include the source and runtime evidence

Current `TypeConstraintSolution` and keyed projection views carry residual
scope. Source traces now have distinct active and closed states, and closed
rows own their normalized actual, selected expectation, and container shape.
Source type/constant closure occurs before materialization; a function
operand's own binder remains inside its type. The automatic second projection
of argument/receiver types has been removed. The linked implementation records
identify the focused scope and transaction tests that have passed. They do not
prove complete initial probing, specialization, or branch publication.

Initial discriminator observation now retains the owner `TypeKind`, and
`ConstraintDomain` separates observed evidence from the checked evidence
issued with a normalized actual. One `SemanticValueEvidence<Owner>` algebra
provides those phase roles. The source callback no longer hashes its
provisional variant owner, and pending equations no longer copy discriminator
evidence or container headers from the source trace. This does not prove all
nested contextual inference. Reconcile generic guard owner projection with
the same candidate scope and bindings used by the value expectation, so an
unopened declaration variable cannot be mistaken for an opened inference
variable during alternative selection.

The [candidate-completion investigation](../../implementation/2026-09-09-candidate-completion-and-contextual-sources.md)
identifies another precise initial-probe boundary. A nested `.Empty()` or
`.Left(value)` call can lack a result-only generic key until the parent solves
other arguments. Completion now preserves that ordinary missing-key rejection;
the [recovery-value follow-up](../../implementation/2026-09-09-recovery-value-evidence.md)
removes the subsequent path that leaked nominal declaration-Free keys as a
successful value. Rejected and ambiguous calls now supply no value evidence;
the missing partial inference remains. Adjudicate partial source constraint transfer and nested
application completion together. Include `combine(.Left(1i64), .Right("two"))`
where both arguments have expected type `Either<A, B>`: each contributes one
type and needs the other, in either argument order. Whole-argument retries,
permissive future projections or a unit-case exception cannot replace the
complete correlated source/call authority. Retain the existing materialization
fatal ordering, accounting, cancellation and cleanup obligations.

The [ordinary-call probes](../../implementation/2026-09-10-callable-convergence-model.md)
at `54fae657e04f95688671fd2d8c0395266383858f` extend this requirement beyond
constructor syntax: `fallback(empty(), 42i64)` for `empty<T>() -> Option<T>`,
and complementary `left<A, B>(A) -> Either<A, B>` /
`right<A, B>(B) -> Either<A, B>` calls. The new sema probes return reports
containing unselected calls without callable diagnostics. The first compiler
pair reaches runtime projection and fails on a structural Call; the second
fails type checking. Require successful selected applications and native/AWBC
returns for these cases too. Tooling may retain rejected-call evidence, but
its presence and an empty diagnostic list cannot authorize executable
projection. This is additional current-source evidence, not a completed design
or an implementation result.

At `22c5a64ef15e1561d5ac4177015a371efafa4605`, the investigation also adds
`callback_returns_a_nonterminal_prefix`: a monomorphic three-group prefix is
passed through a callback whose first application returns the remaining
function value, twice with different second-group values. All six sema calls
select and retain their execution plans. Native and AWBC reach execution but
reject `project-continuation/1` at ordinary function application. The required
result is 84 in both engines. Reusable application authority must cover
Continue as well as Invoke; a caller-owned ProjectCall site with its operand
expressions and result pattern cannot be stored as that value's reusable plan.
The linked investigation records the current owners and unresolved joins.

If partial source terms introduce local inference binders, distinguish their
existential completion obligation from a function value's universal scheme.
Authorize cross-source references explicitly and discharge them before checked
publication; a partially inferred constructed value is not a runtime
polymorphic value. Nested candidate alternatives must also remain correlated
until the parent's constraints can distinguish them. Do not choose one inner
candidate merely because it was probed first.

The latest working copy deletes unscoped solution application and instance
layers. Completed declaration-template projection retains an owned scoped
term, separates root value closure from continuation quantifier transfer,
and does not reapply callee keys inside caller-owned replacement values.
Completed source values have a separate projection entry point. Closed
instance environments project binding values through the caller exactly once;
the selected ABI is also closed by the caller. Compiler body projection uses
the resulting flat callee environment. Review the actual validation in the
linked implementation records against every consumer; the engine matrix
still fails.

Generic effects still use the old issuer overlay. The earlier generic-scope
checkpoint integrated anonymous type/const slots with the callable and solution
inventories through scoped template references and fresh application openings.
It compiled and passed the recorded scope tests and all 730 sema unit tests;
the then-current native/AWBC matrix was 28 passed / 14 failed out of 42 tests.
Review that historical evidence alongside the current higher-order-effect record:
this is not completed general specialization,
effect quantification, callable execution, or engine acceptance. Ignoring a
binder, manufacturing declaration IDs, or rejecting every nonempty binder
would not resolve the remaining coupled contract.

Saved-prefix reapplication now checks the retained function type through the
prepared continuation authority and opens the inherited solution, instead of
comparing the quantified value with a declaration template. Two- and
three-group semantic regressions and 20 existing transaction tests pass.
The shared-prefix engine case now fails while projecting its scheme into a
runtime type. The monomorphic-callback use remains unresolved. This change
does not supply the complete specialization or runtime contract below.

A checked monomorphic use must retain the exact evidence linking its source
value, quantified parameters, selected substitution, capture/prefix values,
and executable target. Source equivalence, rollback, digests, discovery, and
program-bound restore cannot independently reconstruct that link.

## Required examples

### Payload identity must specialize with its owner type

The earlier implementation retained only the owner's `SemanticTypeDigest` in
`VariantPayloadType`. Its `try_map_variant_payload_type` helper transformed
fields and reissued their identities under the unchanged owner digest. This
made an `Option<T>` payload keep the old owner when `T` became `i64`, and its
infallible wrapper could panic when opening an inference-bearing field.

The current working copy replaces that representation. Logical payload terms
in `types/variant_payload/projection.rs` retain the actual owner type and raw
tuple/record field types. The common constraint shape traverses the owner
first and then the fields. `CheckedVariantPayload` in `variant_payload.rs`
seals stable rows only after validating the transformed owner and fields;
scoped type encoding accounts for the retained binder. The old field-only
mapping helpers and prepared record-field identity enum have been deleted.
Prepared record fields now retain declaration coordinates, and the final seal
joins them to actual project, environment, or sealed payload definitions.
`AcceptedEnvironmentRecord` retains the accepted schema through shared owned
access, replacing the former digest/count-only identity carrier.

These are partially validated migration inputs, not completed engine acceptance.
Owner-only generic dependencies must also reach the complete closed-instance
authority; recursively projecting an entire payload owner as a runtime
carrier would cycle through Option/Result payload construction. The new flat
instance and scoped template paths still require complete executable validation, and
source completion, anonymous scheme slots, and the effect namespace require
the remaining integration. Review the actual logical and sealed
representations together with those consumers.

The full structural fold must cover this boundary. Specify the exact owner
type/projection evidence that survives transformation, when active terms may
exist, and the phase at which stable field/case identities may be issued.
Keep logical type transformation distinct from a promise that checked semantic
rows have already been sealed. Do not retain an old digest as an origin alias,
reconstruct an owner type from a digest or source string, attach an optional
unchecked identity, or add a second resolver/catalog. Include function-local
binders, residual binders and caller-Free references inside owner arguments and
payload fields. Fixed vectors now enter the shared unary child shape; their
component traversal must remain connected to opening, normalization, occurs
checks, and completion.

The implementation model must connect payload projection to Match/record
sealing, native/AWBC nominal projection, and program-bound restore as well as
callable specialization. This is part of decisions 1, 2, 7, 8 and 10 below,
not an optional follow-up nominal cut.

The real engine matrix now also includes
`generic_option_payload_keeps_the_instantiated_owner` in
`arcweft-compiler/tests/callable_execution.rs`:

```arcw
fn choose<T>(input: Option<T>, fallback: T) -> T {
    if let Some(value) = input { value } else { fallback }
}
flow main() -> i64 { return choose(Some(42i64), 0i64) }
```

Both native and AWBC return `42` through the instantiated variant owner in
the recorded current matrix.
Cover the same ownership relation for project enums and `Result`, including
nested payloads and constants; a helper-only field substitution test is not
the complete acceptance evidence.

Also cover contextual information that arrives from a later argument. The
native/AWBC fixture `later_argument_closes_an_earlier_contextual_variant`
uses the same `choose<T>` declaration with:

```arcw
flow main() -> i64 { return choose(None, 42i64) }
```

Both engines now return `42` in the recorded matrix. Source variants and
variant patterns retain a common prepared owner/case inventory, and the final
seal issues IDs only after its logical types close. The source-phase carrier
also fixes the generic project-enum `.Empty` case below.
Define the ownership and completion order for the earlier contextual
constructor and the enclosing candidate's variables. Do not hash a provisional
variant owner, fabricate a declaration for an inference hole, publish an
unquantified type variable as a value, or reorder physical argument evaluation.

The current matrix includes these project-enum cases:

```arcw
enum Slot<T> { Empty, Full T }
fn fallback<T>(input: Slot<T>, value: T) -> T {
    if let .Full(item) = input { item } else { value }
}
flow main() -> i64 { return fallback(.Full(42i64), 0i64) }
```

`later_argument_closes_a_project_enum_owner` uses `.Empty` and returns 42 in
both engines. `contextual_project_variant_payload_closes_its_owner` exercises
the payload example above and now returns 42 in both engines. Constructor
lookup uses the parent's contextual enum shape, while the child opens formal
parameters issued by the real nominal declaration. Its signature ID belongs
to the declaration-template owner; its checked application owns the result
instantiation. The copied `ExpectedEnum` types and the already-checked-expression
constructor reader were deleted. Final semantic collection retains generic
nominal owners, and final execution projection checks the completed result.

The expanded matrix exposes the remaining parent/child inference cases:

```arcw
enum Slot<T> { Empty, Full T }
fn fallback<T>(input: Slot<T>, value: T) -> T { value }
flow main() -> i64 { return fallback(.Empty(), 42i64) }
```

```arcw
enum Either<A, B> { Left A, Right B }
fn fallback<A, B>(input: Either<A, B>, value: A, other: B) -> A {
    if let .Left(item) = input { item } else { value }
}
flow main() -> i64 { return fallback(.Left(42i64), 0i64, "other") }
```

These fail in semantic call constraints in both engines: child arguments do
not independently determine every result parameter. By contrast, `.Full(value)`
inside `pack<T>(value: T) -> Slot<T>` executes correctly after `pack(42i64)`
is instantiated. Define the typed parent/child constraint obligation, scope
transport, source completion, and final sealing order for the remaining cases.
Constructor lookup/schema identity, generic opening, and argument inference
must agree with the eventual result owner across all cases, including parameters
used only by an unselected case. The current successful subset does not close
this call boundary.
Do not mint a provisional owner digest, fabricate case/parameter identities,
or exclude generic project constructors to avoid this dependency.

### Callable value examples

These programs appear in `arcweft-compiler/tests/callable_execution.rs`, which
executes both the native Engine and AWBC VM. Re-audit their expected behavior
and preserve the distinction between observed failures and design decisions.

```arcw
fn add(first: i64)(second: i64) -> i64 { first + second }
fn apply(handler: i64 -> i64 effects {}, value: i64) -> i64 { handler(value) }
flow main() -> i64 {
    let prefix = add(1i64)
    return apply(prefix, 41i64)
}
```

The monomorphic prefix/callback case must execute and return `42` in both
engines through the ordinary callable authority.

```arcw
fn choose<A, B>(first: A)(second: B) -> B { second }
fn apply(handler: i64 -> i64 effects {}, value: i64) -> i64 { handler(value) }
flow main() -> i64 {
    let prefix = choose("first")
    let text = prefix("text")
    return apply(prefix, 42i64)
}
```

Close the semantics and exact implementation for this published-scheme use.
An accepted monomorphic use must not consume or mutate the original scheme;
sibling uses must be independently instantiable. A rejection requires a
coherent language-level argument and complete supported alternative behavior,
independent of the current runtime gap.

```arcw
fn increment(value: i64) -> i64 { value + 1i64 }
fn apply(handler: i64 -> i64 effects {}, value: i64) -> i64 { handler(value) }
flow main() -> i64 {
    let handler = |value: i64| increment(value)
    return apply(handler, 41i64)
}
```

This callback requires executable control. Also decide the exact semantics
when the callback type's effect annotation is omitted. The current final seal
reports an unknown row; replacing that row with pure is not a decision.

The new `higher_order_effects.rs` semantic probes must also be included.
Explicit callback rows validate the invoked-only union. Omitted rows require
separate nonempty, two-callback, unused-callback and distinct-application cases.
The final source value type, captured/local types, physical source observations,
candidate constraints, frozen substitutions, prefix ABI and instance body must
retain one effect authority. Provisional empty concrete sets currently enter
closure function types before callable-body effects close; correcting only the
final catalog or runtime parameter projection leaves stale earlier evidence.
Adjudicate declaration-owned effect parameters, pending body/closure dependencies,
candidate applicability and effect-scheme residual constraints together. If the
row algebra gains variable unions, specify its constraint retention and unique
completion rules rather than assuming that this is a representation-only edit.

## Decisions to close together

1. **Scoped application algebra.** Give exact keys, owners, inventories,
   opening permissions, binder shifts, occurs checks, and normalization for
   declaration templates and scheme-valued operands. Explain when an actual
   operand is kept rigid and when a quantified scheme may be instantiated.
   A caller's Free parameter must never become a callee inference variable.
2. **Result and continuation quantification.** Define residual type/constant/
   effect binders, unused quantifiers, empty binders, nested binders,
   fresh reopening, and the distinction between template application and
   closing caller-Free references. Completed carriers retain no active issuer
   or substitution history. Give one flat closed instance substitution.
3. **Function-value semantics.** Cover bare generic declarations, declared
   partial applications, ordinary closures, returned/stored values, explicit
   callback types, contextual uses, and values passed through aggregates or
   control-flow joins. Separate supported scheme use from higher-rank and
   impredicative cases. Do not let the easiest call-site shape define scope.
4. **Specialization evidence.** Select one exact owner relating a use to its
   source scheme, substitution, captures, group position, and target. Handle
   multiple possible value origins, aliases, closures, and two uses of one
   prefix. Do not adopt the first observed source, infer a callee from a
   runtime value, or introduce a second resolver/solver or untyped registry.
5. **One callable-value execution contract.** Define the final value/carrier,
   function-site/group representation, operations, and ordinary dispatch for
   direct functions, closures, monomorphic prefixes, and supported specialized
   schemes. Preserve typed ProjectCall obligations; explain necessary
   amendments. A nonterminal group must not falsely claim a closed executable
   instance. No synthetic wrapper function or expression/AOT fallback may
   conceal a missing callable operation.
6. **Effects, suspension, and evaluation order.** Connect inferred and explicit
   effect rows, source transactions, captures, defaults, and native/AWBC call
   frames. Prefix operands evaluate once; specialization itself does not rerun
   them or execute an attached default early. Return, fallthrough rejection,
   nonlocal control, cancellation, and cleanup use the shared owner.
7. **Atomic construction.** Close source probes, branch evidence, substitutions,
   projections, and target discovery before publication. Define every failure
   class and rollback boundary. Validation must precede observable frame or
   environment mutation when an invalid callable cannot be entered.
8. **Stable encoding and restore.** Specify exact version-1 fields for schemes,
   scoped type children, specialization evidence, captures, callable values,
   function sites, and dormant saved values. Reject foreign programs, missing
   targets, malformed bound references, and active issuers before admission.
   Reuse the existing program-bound restore authority; do not build a second
   snapshot or callable catalog.
9. **Finite discovery and limits.** Specify how every supported specialization
   enters the compiler's deterministic instance worklist, including recursion,
   nested closures/defaults and values with more than one possible origin.
   Set typed inclusive work/node/edge/instance limits, cancellation points,
   and failure atomicity. Runtime execution must not run type inference.
   The [instance graph follow-up](../../implementation/2026-09-09-instance-graph-and-type-dependencies.md)
   adds graph-count/work bounds, cancellation, exact root/edge membership and
   same-key evidence checks. It also connects owned type dependencies across
   global, instance and closure scopes. Preserve that validated substrate.
   Structural type/constant/effect visits, depth, bounded substitution/encoding,
   materialization accounting and the complete transcript remain required;
   graph admission alone does not establish their completion.
   The [controlled projection follow-up](../../implementation/2026-09-09-controlled-type-projection.md)
   adds iterative type substitution, typed structural/depth limits and one
   shared discovery/materialization ledger across body and nested consumers.
   Preserve its tested bounds and rollback behavior. The subsequent
   [controlled encoding follow-up](../../implementation/2026-09-09-controlled-type-encoding.md)
   moves canonical type/payload hashing to one iterative encoder and connects
   completed instance keys, constants and effects to the same control. Preserve
   its version-1 identity and abort evidence. Runtime normalization, surrounding
   copies/equality, remaining default encoding entry points and the complete
   transcript still require a complete accounting boundary.
10. **Deletion and validation.** Name final Rust owners, visibility, all
    producers/consumers, obsolete representations and success paths, and
    migration order. Do not designate a smaller compiling subset as the final
    contract while these decisions remain coupled.

## Minimum current consumer inventory

Paths are relative to `crates/`. Discover and include children and other real
consumers revealed by typed APIs and the dependency graph.

- `arcweft-lang-sema/src/types/{generics,generic_use,digest,substitution,variant_payload,project_nominal_visit}.rs`
  and `types/constraints/{context,references,shape,normalization,solution,transaction}.rs`;
- `arcweft-lang-sema/src/{effect_row,effect_model,effects}.rs` and the current
  callable effect-summary, row-inference and substitution owners;
- `arcweft-lang-sema/src/callable/{schema,continuation,checked_application,checked_catalog,identity,facts,join}.rs`
  and `callable/resolver/{effect_instantiation,prepared_identity}.rs`;
- `arcweft-lang-sema/src/final_analysis/analyzer/calls/constraints.rs`,
  `analyzer/call_seal.rs`, expression expectations, semantic transcripts,
  nominal request collection, final model/seals and ownership;
- `arcweft-lang-sema/src/final_analysis/model/{variant_owner,record}.rs`,
  `final_analysis/{nominal_semantic,nominal_schema}.rs`, and compiler/runtime
  payload projection and type-identity consumers;
- `arcweft-compiler/src/lower.rs`, `lower/project_instances.rs`, executable
  reachability, captures and attached-default projection;
- `arcweft-runtime-plan/src/{final_expr,final_flow,semantic_facts}.rs`, their
  project-function and closure children, and AWBC lowering;
- `arcweft-core/src/{value,plan,engine,pure}.rs`, value function/continuation
  and AWBC-save children, `engine/eval/function.rs`, shared Flow call frames,
  and `awbc/{schema,codec,verifier,vm,fiber}.rs` or their current split files;
- maintained function/currying/effect rules, formatter/LSP/signature-help
  consumers affected by the selected semantics, and compiler/runtime tests.

## Acceptance and implementation order

Provide named native and AWBC execution tests for the examples, same-type
self/mutual recursion, growing specialization limits, repeated prefix use with
different types and constants, and a caller-Free variable sharing a callee's
declaration ID. Include alpha-equivalent schemes, nested and unused binders,
effect variables, rejection of unsupported inference forms, and stale/foreign
opening evidence.

Cover values selected by branches and carried through closures/aggregates,
multiple remaining curried groups, source order, rest/receiver destinations,
input patterns, once-only prefix captures, omitted/present attached defaults,
effectful and suspending callbacks, return and unwind behavior, and save/restore
of reachable callable values. Include negative codec/semantic-identity and
atomic-publication tests. Source spelling and file placement are not gates.

First close the whole semantic and runtime-value model. Then migrate the
types/constraint owner, prepared and final source/application evidence, callable
value and instance discovery, native/AWBC execution, and persistence consumers.
Delete the old unscoped/layered and unsupported-success paths, then run the
focused and required workspace/Clippy/codec/structure/runtime tiers. These are
stages of one coherent implementation obligation. A design archive alone does
not complete the active convergence goal.

## Non-goals and returned artifact

Do not redesign content grammar, CharacterDialogue/line identity, rendering,
unrelated View/task-plan features, or host scheduling policy. Preserve the
existing Sans-I/O and layer direction. These exclusions do not remove the
callable execution and program-bound restore obligations above.

Return exactly:

```text
arcweft-aw-ah-009.4.2.1.1.1-function-scheme-specialization-and-callable-value-execution-final-contract.zip
```

Include `README.md`, `FINAL_CONTRACT.md`, `RUST_SHAPES.md`,
`PRODUCTION_RECONCILIATION.md`, `CONSUMER_INVENTORY.md`, `VALIDATOR_MATRIX.md`,
`ACCEPTANCE_MATRIX.md`, `IMPLEMENTATION_ORDER.md`, `REQUIREMENTS_TRACEABILITY.md`,
`REPOSITORY_EVIDENCE.md`, `OPEN_QUESTIONS.md`, `FINAL_STATUS.md`, and a sorted
manifest with SHA-256/member evidence. Keep every sidecar inside the archive,
exclude manifest self-entries, and include no production patch.

Use `READY_FOR_IMPLEMENTATION` only after every result-changing decision is
closed and `OPEN_QUESTIONS.md` is exactly `none`. Investigation size or a dirty
compilation baseline is not an external blocker. Record actual validation
results without claiming that this design repaired the production tree.
