# Lang-01.4.2.1 — Resource extension-manifest repository reconciliation

## Sequence position

This is the repository-grounded redelivery of Lang-01.4.2. It follows
Lang-01.4, the accepted generic `res` registry substrate, and Lang-01.4.1's
retained-identity correction. It must be completed before any public extension
manifest decoder, canonical encoder, bundle publication, or third-party
resource-type loading is implemented.

The returned package
`arcweft-lang-01.4.2-resource-extension-manifest-wire-contract-final-contract.zip`
has archive SHA-256
`01F308C08FE818E247E41E94278EB2D69D5A12AC597794A9109390840C0D95D3`.
Its `FINAL_CONTRACT.md` is explicitly a generation fallback: no checkout was
available, no HEAD was pinned, no command ran, and
`contract_agent_validated=false`. That package is evidence of the unresolved
request, not an implementation-ready final contract.

## Required repository reconciliation

1. Pin the latest reachable `main` commit and record whether it equals
   `origin/main`. Read the applicable `AGENTS.md` files and Rust skill before
   making contract decisions.
2. Inspect the implemented `res` syntax/HIR/sema path and the immutable typed
   registry in `arcweft-resource-model`. Inventory the exact existing identity,
   descriptor, field, constraint, capability, codec, constant-value, digest,
   limit, and diagnostic types. Do not infer them from the old request text.
3. Inspect the current Lang-01.4.1 retained-reference implementation and its
   repository-visible reconciliation notes. The wire schema must consume its
   final typed categories and must not collapse Character, View, Action, Layer,
   Signal, presentation target, or scroll region into `ResourceRef`.
4. Inspect current strict manifest/codec owners and source-backed decoder
   patterns. Decide the crate that owns neutral DTOs, strict decoding,
   canonical encoding, source ranges, and semantic conversion without adding
   filesystem I/O to data-format crates.
5. Re-adjudicate every decision in
   `2026-07-20-lang-01.4.2-resource-extension-manifest-wire-contract-correction.md`
   against the actual current types and tests. For every deviation from that
   request, cite the concrete repository fact that requires it.
6. Demonstrate that the proposed schema has one direct-final reader and one
   canonical encoder. Do not preserve a provisional spelling, add an alias,
   introduce a version-compatibility reader, or speculate about a released
   format.

## Decisions the final contract must close

- exact transport and `ResourceTypeManifestFileV1` top-level record;
- exact `PackageCoordinateFile` representation and normalization;
- every tag/content spelling for current closed resource-model variants;
- exact integer, finite-float, string, byte, typed-ID, optional, list, ordered
  map, record, enum constant, `AssetRef`, exact `ResourceRef`, and retained-ID
  encodings;
- whether one document publishes multiple resource types and how same-package
  references resolve;
- duplicate key/record, missing/null/malformed/unsupported, unknown-field, and
  wrong-shape behavior at every boundary;
- strict version dispatch with no fallback;
- deterministic ordering and canonical bytes used by signatures, digests, and
  cache keys;
- independently recomputed descriptor digest behavior when a manifest claims a
  digest;
- nested source-range and related-range ownership for decode and registry
  validation diagnostics; and
- exact byte, depth, string, collection, record, and work budgets with atomic
  publication.

## Implementation order to specify

1. Neutral wire identities, scalars, limits, and source-backed DTOs.
2. Complete tagged records and canonical examples.
3. Strict decoder and canonical encoder in the selected Sans I/O owner.
4. Typed conversion into the existing immutable registry.
5. Package/build loading and deterministic artifact publication.
6. Positive, negative, round-trip, tamper, determinism, budget, and integration
   tests.

The contract must name the concrete current Rust types and modules for each
step and identify any dependency edge that must be added. It must not redesign
the existing registry substrate unless a concrete contradiction is proven.

## Required test contract

- minimal and full canonical manifests;
- every current enum variant, retained-reference category, and constant-value
  shape;
- decode/encode/decode semantic equality and byte-for-byte canonical
  regeneration;
- input-order independence for semantic sets and order preservation only where
  order is semantic;
- missing, null, malformed, unsupported version, unknown field, duplicate,
  wrong tag/content, forged digest, and cross-category reference negatives;
- NaN, infinity, non-canonical negative zero, integer overflow, invalid UTF-8
  or byte encoding, malformed ID, and nested range negatives;
- inclusive and one-over resource limits with no partial registry publication;
  and
- integration through the actual package/build owner without directory
  enumeration or permissive fallback.

No test may scan implementation source for symbol spellings or file paths.
Architecture evidence must use typed APIs or structured dependency metadata.

## Constraints

- No compatibility alias, dual reader, migration shim, source gate, removed
  syntax recognizer, CSS route, or Takumi route.
- No `serde_json::Value`, generic TOML value, or raw type-label semantic
  boundary.
- Keep codecs Sans I/O; filesystem and package discovery stay in adapters.
- Do not redesign already implemented and verified Lang-01.4/01.4.1 substrate
  without a cited failing invariant.
- Do not implement production code as part of this contract task.

## Expected output

Return one independently throwable final-contract ZIP containing:

- the pinned revision and repository/AGENTS/Rust-skill inventory;
- a complete repository-specific final contract, not a fallback;
- exact DTO and semantic conversion shapes;
- required decisions, implementation order, ownership/dependency map, and test
  matrix;
- executed repository-aware validation commands and logs; and
- machine-readable status proving `OPEN_QUESTIONS=0`,
  `IMPLEMENTATION_READY=true`, and repository contract validation succeeded.

If repository access or validation is unavailable, return a blocked package
instead of relabeling the original request as a final contract.
