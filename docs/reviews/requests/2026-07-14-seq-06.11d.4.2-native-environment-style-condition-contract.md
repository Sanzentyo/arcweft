# Design request: seq-06.11d.4.2 native environment Style-condition contract

- Date: 2026-07-14
- Revised: 2026-07-15 after repository-backed package intake
- Kind: independently throwable design request
- Output language: English

> **Returned-package status (2026-07-15): not accepted for implementation.**
> The returned final-contract archive passed integrity and baseline checks, but
> its production intake found unresolved comparison ownership, transition-state,
> checked-codec, adapter-lifecycle, tooling-API, and structural-decomposition
> decisions. Do not implement from that archive. Continue with the independent
> [d.4.2.1 production-reconciliation request](2026-07-15-seq-06.11d.4.2.1-native-environment-style-condition-production-reconciliation.md).

## Task mode: final design only

This task produces the final implementation-ready contract. It does **not**
modify production Rust, schemas, fixtures, or codecs. A later implementation
task will apply the returned contract end to end.

The target Arcweft checkout is required evidence. Start from the latest `main`,
record its Git commit and Jujutsu change identifier when available, and inspect
the concrete APIs named below before choosing types or file boundaries. Do not
claim a completed design if the repository cannot be inspected. In that case,
return a short blocked report and no delivery ZIP.

The previously supplied
`arcweft-native-environment-style-condition-contract-implementation.zip` is
not an implementation or a final design. It contains no repository checkout,
patch, changed file, manifest, or verification result; its only substantive
input is the earlier revision of this request. Do not use that package as
implementation evidence and do not reproduce its empty-delivery structure.

## Sequence position and delivery boundary

This is an independent adaptive-Style design cut after the implemented
seq-06.11d.1 native typed Style, d.2 sheet-owned canonical product, d.3 single
computed-style resolver, and d.4.1 native logical-axis core. It defines source
authoring and end-to-end semantics for closed presentation-environment
conditions. It neither depends on nor designs container queries.

This request plus the target repository checkout is the complete task input.
The requested result is one final, implementation-ready contract. Do not
require a previous design request, package, or external link to resolve a
decision. Repository documents may be cited as evidence, but every normative
decision must be repeated in the returned design.

The dependency order is fixed:

1. d.4.1 logical-axis core is already landed and must be preserved.
2. This d.4.2 final contract is designed now and implemented in a separate task.
3. d.4.3 container conditions begins only after d.4.2 plus d.4.1.1 provider
   lifecycle and d.4.1.2 physical geometry implementations land.
4. d.5.1.1 trace reconciliation begins only after those predecessors and d.4.3
   land.
5. d.5.3 owns shared formatter/LSP infrastructure. This contract must freeze
   environment-condition feature behavior and typed inventories without
   redesigning that shared infrastructure.

The unresolved d.4.1.1 through d.4.1.4 host/geometry/scroll/motion contracts
are not prerequisites for this design. Do not absorb or redesign them.

## Problem and background

The canonical Style model and resolver can represent typed conditions for color
scheme, contrast, reduced motion, and text scale, and runtime already carries a
typed `PresentationEnvironment`. `.arcw` Style authoring, however, has no final
closed grammar for those conditions. Without that contract, parser/lowering
cannot distinguish an environment guard from a general runtime expression,
specificity semantics are unclear, diagnostics and tooling cannot be stable,
and environment revisions may invalidate more or less Style state than intended.

The feature must be declarative and deterministic. It is not a general `if`
expression over runtime state and is not a CSS media-query implementation.

## Confirmed substrate that must be preserved

- Style has one native typed pipeline from syntax through checked catalog,
  compiler-owned `ViewStyleProgram`, runtime applications, and the sole
  `arcweft-view` computed resolver.
- `ViewEnvironmentPredicate` is a closed typed product inventory with reduced-
  motion boolean, color-scheme comparison, contrast comparison, and text-scale
  comparison forms. `ViewStyleComparison` is also typed.
- `PresentationEnvironment` is the runtime authority for `ColorScheme`,
  `ContrastPreference`, reduced motion, text scale, locale, and an explicit
  environment revision. Native and web-hosted players already feed the same
  typed resolver contract; headless authority and defaults remain to be frozen
  by this design.
- The resolver evaluates the existing environment predicate forms and includes
  environment values/revision in computed-style cache identity. Trace rejection
  already distinguishes an environment mismatch.
- Sheet ownership, tokens, source/declaration/application order, inheritance,
  typed invalidation, work budgets, and deterministic cache behavior are
  implemented.
- Canonical product codecs serialize typed Style rules/predicates and validate
  their structural budgets without reopening source files. Cross-field
  predicate validation is not yet complete.
- Source Style rules currently have selector/state authoring but no complete
  environment-condition AST/HIR/sema surface.
- The landed d.4.1 resolver has a separate typed `ViewBoxAxisMode`, axis
  provider revision, two-phase logical-to-physical cascade, and canonical
  physical computed boundary. Environment design must not overload or merge
  the axis revision, axis usage set, or logical alias machinery.

Environment conditions are activation guards and therefore contribute **zero**
selector specificity. This is a required outcome, not an open choice. The
current representation counts every `ViewStylePredicate` in specificity, so
the final design must specify the smallest typed model/resolver correction. It
must retain one cascade and the existing priority tuple for actual selector
specificity, source order, declaration order, application order, and scope.

## Repository-backed gaps the contract must reconcile

These are observed facts at the `11f5acec` d.4.1 landing point. Recheck them on
the task's latest `main`; if they changed, record the new evidence and design
against the current shape.

1. `arcweft-lang-syntax::ast::style::StylePredicate` and the corresponding HIR
   carry only a bare name and range. The parser accepts colon state names, and
   sema currently recognizes only interaction and element-state inventories.
   There is no typed environment-condition source node.
2. `arcweft-view::style::ViewEnvironmentPredicate` is embedded in
   `ViewStylePredicate` and has four variants: reduced motion, color scheme,
   contrast, and text scale. `ViewStyleComparison` exposes all six equality and
   ordering operators to all three comparable variants.
3. The resolver currently turns `ColorScheme::{Light, Dark}` and
   `ContrastPreference::{Standard, More}` into numeric ranks. The final
   contract must remove accidental enum ordering: enum and boolean fields may
   use only the explicitly selected equality forms; numeric ordering is only
   for text scale.
4. The current predicate stores text scale as `ViewRatioMilli`, whose checked
   range is 0 through 1000. `PresentationEnvironment` stores
   `TextScaleMilli(u16)` and existing tests use 1250. The final contract must
   choose one canonical checked text-scale condition type and exact range; it
   must not retain this mismatch or add a conversion shim.
5. `ViewStyleSelector::specificity` currently counts environment and container
   predicates exactly like interactive states. The design must separate
   activation guards from specificity-bearing selector facts and define the
   checked overflow/budget behavior after that separation.
6. The computed cache key currently includes every environment value, locale,
   and the caller-supplied environment revision. There is no condition-field
   dependency mask or equality-aware revision owner. The design must choose an
   exact authoritative update API and invalidation algorithm rather than merely
   saying that revisions advance.
7. Runtime session construction currently derives one environment from theme
   defaults plus a host color scheme, initially at revision zero. Native and
   web consume the shared session snapshot, but no complete live-update
   lifecycle exists. Headless authority and equal-snapshot behavior are also
   not final.
8. Product decoding validates selector structure and budgets, but the derived
   environment enum shape does not reject every semantically illegal
   field/operator pair. The contract must route decoding through checked owned
   constructors and state exact merge validation.
9. The existing trace can report only `EnvironmentMismatch` for a rejected
   rule. It does not retain a typed condition identity/range/evaluated fact.
   This contract owns the minimal typed evidence required from d.4.2; d.5.1.1
   owns the later public trace query, paging, cursor, redaction, and cache API.
10. No stable formatter or Style-aware LSP surface currently implements this
    authoring form. This contract must define environment-specific formatting,
    inventories, ranges, completion, hover, semantic-token, and code-action
    behavior while leaving common tooling architecture to d.5.3.

## Required decisions

1. Define the single canonical `.arcw` grammar and placement for an environment
   condition. Show whether it wraps rules, attaches a typed guard to one rule,
   or uses another closed form. Include exact formatter output.
2. Define the complete first-cut field inventory and source names for color
   scheme, contrast, reduced motion, and text scale. Do not leave aliases.
3. Define legal operators per field. Enum/boolean fields should not inherit
   ordered comparisons accidentally; numeric text scale must define equality
   and ordering precisely, including accepted unit and range.
4. Define boolean spelling for reduced motion, including whether a bare positive
   form, explicit equality, and negation are allowed. Select one canonical form
   and specify formatter normalization without retaining multiple semantic
   spellings unnecessarily.
5. Define grouping, conjunction, disjunction, negation, and nesting for the first
   cut. If only one condition or an implicit conjunction is supported, reject
   the rest structurally; do not fall back to the general expression checker.
6. Define AST, HIR, checked-sema, compiler, product, codec, and resolver types
   and source ranges. The checked form must contain typed field/operator/value,
   not expression text.
7. Define cascade semantics under the required zero-specificity rule. State how
   two active guarded rules compete and how guarded and unguarded rules interact
   under the unchanged priority tuple.
8. Define rule normalization when a condition wraps multiple selectors/rules or
   is nested with another accepted condition. Preserve authored rule and
   declaration order deterministically.
9. Define runtime authority and defaults for native, web-hosted, and headless
   execution. Clarify who may update `PresentationEnvironment`, how revisions
   advance, and how equivalent repeated snapshots are handled.
10. Define cache and invalidation behavior for each field change. State whether
    dependency information permits invalidating only rules/nodes that reference
    the changed field, how inherited computed values propagate, and how stale
    transitions are canceled or rebased. Locale is not a first-cut condition;
    state whether a locale-only snapshot change can affect Style cache identity
    through any other existing consumer.
11. Define the typed environment evidence retained for active, inactive,
    malformed-at-build, and unavailable facts. Provenance must identify the
    condition and source range without storing/reparsing source text at runtime.
    Do not design d.5.1.1 public query/paging/cursor/cache transport here.
12. Define canonical codec validation and tamper behavior for unknown field
    tags, illegal field/operator pairs, out-of-range values, invalid nesting,
    impossible source ranges, and product merge.
13. Define structured parser and semantic diagnostics for unknown fields,
    misspelled enum cases, illegal operators, wrong value kinds, out-of-range
    text scale, unsupported boolean/group syntax, and illegal placement.
14. Define feature-level formatter and LSP completion, hover/type display,
    semantic tokens, code actions, definition/reference behavior for enum
    cases/tokens, and incremental diagnostic invalidation. State the typed
    inventory APIs d.5.3 must consume; do not redesign generic LSP transport.
15. Define how reduced motion affects Style selection versus transition
    execution. A condition may choose declarations; any global transition-
    suppression policy must remain an explicit, separately owned runtime rule.

## Constraints

- Do not introduce CSS media queries, CSS source, Takumi, browser match-media,
  CSSOM, or an adapter-owned condition path.
- Do not permit arbitrary runtime expressions, function calls, state reads,
  user variables, clocks, viewport strings, or host callbacks in a Style
  condition.
- Preserve seq-06.11d.1-d.3 syntax/HIR/sema ownership, sheet-owned product,
  resolver priority, typed values, and one cascade. Make only the focused
  specificity correction required to keep activation guards out of selector
  specificity.
- Preserve the landed d.4.1 axis types, provider revision, logical alias
  mapping, computed physical slots, and cache identity. Environment and axis
  revisions remain distinct typed facts.
- Runtime and codecs must consume typed condition records and
  `PresentationEnvironment`; they must not parse source strings.
- Keep host environment acquisition outside Sans-I/O core/data crates.
- Use a single authoritative environment revision contract across native,
  web-hosted, and headless paths.
- The surface is unreleased. Choose the final form directly; no aliases, dual
  parsers, deprecated fields, migration readers, or compatibility wrappers.
- Do not add a source gate. Validate parser, sema, product, codec, runtime, trace,
  and tooling behavior through their owned APIs.
- Preserve deterministic fixed-point text scale, bounded work, stable source
  provenance, and current crate dependency direction.
- Do not preserve the provisional `ViewRatioMilli` text-scale predicate or
  enum-rank comparison behavior if the final truth table requires a different
  owned type. This surface is unreleased; replace the wrong shape directly.

## Non-goals

- Container size/style queries, viewport queries, device-class detection, or
  arbitrary feature probes.
- CSS media-query syntax or browser parity.
- A general conditional expression language inside Style.
- OS preference polling inside `arcweft-core`, `arcweft-view`, or bundle codecs.
- New color-scheme, contrast, accessibility, or locale values beyond the closed
  first-cut inventory unless the design explicitly justifies their typed model.
- Redesigning theme palettes, system-color resolution, property values,
  rendering, or the View evaluator.

## Required contract and eventual implementation order

1. Freeze the closed field/operator/value truth table and canonical source
   grammar, including grouping limits and formatter form.
2. Specify AST/HIR checked types, exact ranges, recovery, and the diagnostic
   catalog.
3. Specify the zero-specificity activation model and the smallest typed
   product/resolver correction required to enforce it.
4. Specify compiler lowering, deterministic rule normalization, source maps,
   codec validation, and merge behavior.
5. Specify host snapshot/revision authority and dependency-aware runtime
   invalidation across native, web-hosted, and headless players.
6. Add the d.4.2 evidence seam and feature-level formatter/LSP behavior without
   implementing the broader d.5.1.1 or d.5.3 contracts.
7. Implement positive, negative, priority, revision, codec, runtime, and tooling
   tests before declaring environment authoring supported.

## Required test specification

### Grammar, HIR, sema, and diagnostics

- Each legal field/operator/value form parses with exact ranges, lowers to a
  closed HIR/check record, and formats idempotently.
- Unknown fields, invalid enum cases, boolean misuse, illegal ordered
  comparisons, malformed operators, wrong units/kinds, out-of-range text scale,
  illegal nesting/grouping, and misplaced guards produce structured recovery
  and semantic diagnostics.
- General expressions, calls, variables, and CSS-like media syntax are rejected
  without a compatibility parser.

### Cascade and resolver

- Truth-table tests cover every field, comparison, boundary value, and false
  case against `PresentationEnvironment`.
- Guard activation contributes zero specificity; guarded and
  unguarded rules with equal/different selector priority retain deterministic
  source/application order and trace provenance.
- Multiple accepted conditions normalize without changing authored rule order.
- Inherited values recompute correctly when an ancestor's guarded winner changes.

### Revisions, invalidation, and players

- Changing one environment field advances the authoritative revision according
  to the selected rule and invalidates exactly the required dependent nodes.
- Reapplying an equal snapshot does not create nondeterministic churn.
- Native, web-hosted, and headless players produce the same computed Style for
  the same typed snapshot.
- Reduced-motion condition selection and any transition-suppression policy are
  tested as distinct contracts.

### Codec and tamper resistance

- Canonical encode/decode round-trips all legal predicates, source provenance,
  rule order, and typed values deterministically.
- Unknown tags, illegal field/operator pairs, bad enum/scalar values, invalid
  nesting, impossible ranges, duplicate/malformed records, and merge damage
  fail within configured decode budgets.
- No decode or runtime test requires `.arcw` source or a legacy reader.

### Formatter and LSP

- Completion is field/type/operator aware; hover displays the closed condition
  type; semantic tokens and code actions use exact condition ranges.
- Incremental edits remove stale diagnostics and invalidate only the affected
  Style/condition inventory.

No test may use source-file text searches as correctness evidence.

## Required repository evidence

The design package must identify the current symbols, owners, and call sites it
expects the later implementer to edit. At minimum inspect and record the
current responsibility of:

- `crates/arcweft-lang-syntax/src/ast/style.rs` and
  `crates/arcweft-lang-syntax/src/parser/style.rs`;
- `crates/arcweft-lang-hir/src/style.rs`;
- `crates/arcweft-lang-sema/src/style/{catalog,check,diagnostic}.rs`;
- `crates/arcweft-compiler/src/style.rs`;
- `crates/arcweft-view/src/style/{selector,resolver,trace}.rs` and selector
  codec ownership;
- `crates/arcweft-presentation/src/appearance.rs`;
- the View product/resource codec and its decode budgets;
- runtime-driver session environment construction and the native, web-hosted,
  and headless adapter seams that actually exist; and
- the d.4.1 implementation note and landed axis/cache types.

Do not prescribe a file or symbol merely from this list if current `main` has
moved that responsibility. Record the actual owner and explain any difference.

## Expected output

Deliver a self-contained design package containing:

- the normative grammar, formatter examples, field/operator/value truth table,
  and rejected grammar;
- exact AST, HIR, checked, product, codec, resolver, environment revision, and
  invalidation APIs;
- the activation/specificity and rule-normalization algorithms;
- host/default/headless authority and deterministic update behavior;
- diagnostic codes with ranges, notes, and fixes;
- feature-level formatter/LSP behavior and the minimal typed environment
  evidence consumed later by trace/explain;
- a file-by-file implementation order and complete test/validation matrix;
- explicit non-goals for every omitted condition family;
- a repository-evidence table with inspected revision, current owner, proposed
  owner, and reason for every changed public/boundary type;
- a decision ledger with no `TBD`, alternatives left for the implementer, or
  mutually valid source spellings; and
- a requirements traceability table mapping every item in this request to a
  normative section and a planned direct behavior/codec/type test.

Return exactly one design ZIP named:

`arcweft-seq-06.11d.4.2-native-environment-style-condition-final-contract.zip`

It must contain:

- `README.md` with reading order and inspected repository revision;
- `DESIGN.md` as the sole normative contract;
- `DECISIONS.md` with the closed grammar/truth tables and rationale;
- `IMPLEMENTATION_PLAN.md` with file ownership and ordered compiling cuts;
- `REQUIREMENTS_TRACEABILITY.md`;
- `VERIFICATION_PLAN.md` with focused and reviewable-cut commands;
- `REPOSITORY_EVIDENCE.md`;
- `FINAL_STATUS.md` stating whether every acceptance item is resolved; and
- `MANIFEST.txt` with SHA-256 hashes for every delivered file.

Do not include a repository snapshot, production patch, generated schema,
`changed-files/`, build output, cache, or fabricated validation logs in this
design-only package. Also return outside the ZIP a concise Markdown summary,
machine-readable status text, and the ZIP SHA-256.

## Acceptance criteria

The design is implementation-ready only when the grammar is closed, every
field/operator pair has exact semantics, condition activation and specificity
are unambiguous, revisions/invalidation are deterministic, typed records survive
codec and merge, and tooling/diagnostics require no guesswork. It must preserve
the native-only single-resolver d.1-d.3 path, introduce no CSS/Takumi or general
expression escape, and add neither compatibility machinery nor source gates.

It must additionally reconcile the `ViewRatioMilli`/`TextScaleMilli` mismatch,
remove enum-rank ordering from the legal contract, enforce zero specificity for
activation guards, keep d.4.1 axis identity separate, and draw an explicit
ownership boundary around d.5.1.1 trace transport and d.5.3 tooling
infrastructure. A package that repeats the questions, defers a decision to the
implementer, or cannot cite a repository-backed API shape is not accepted.
