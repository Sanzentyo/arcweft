# Request: AW-AH-009.2 character nominal definition and rename contract

Date: 2026-07-14

## Superseded

This combined request was superseded on 2026-07-16 after AW-AH-009.1.1 landed
the canonical character registration, source-token, project-symbol, registered
world, and accepted-environment substrate.

Do not dispatch or implement this file. Use the following independent,
sequence-preserving requests instead:

1. [AW-AH-009.2.1 character nominal definition and source-index contract](2026-07-16-aw-ah-009.2.1-character-nominal-definition-source-index-contract.md)
2. [AW-AH-009.2.2 character nominal rename and atomic-edit contract](2026-07-16-aw-ah-009.2.2-character-nominal-rename-atomic-edit-contract.md),
   only after the AW-AH-009.2.1 final contract is accepted

The remainder of this document is retained unchanged as historical evidence of
why definition and rename were originally separated from AW-AH-009. It contains
pre-AW-AH-009.1.1 facts, including the now-obsolete claim that no typed
manifest-token source map exists, and is not a current source of truth.

## Readiness result and task kind

This is a **final-design task**, not an implementation task. The current
checkout does not contain a typed manifest-token source map, a character symbol
resolver for definition, or any rename policy. Choosing ordinary versus
breaking rename, edit coverage, conflict behavior, and stale-document semantics
would create public LSP behavior and cannot be inferred safely.

The task is gated on the accepted AW-AH-009.1 final contract. If that artifact
is not attached, the assignee must report the missing prerequisite and must not
invent a parallel alias, provenance, diagnostic, revision, or scope model.

## Sequence position and reason for the split

This request follows AW-AH-009 structural identity and depends on the finalized
AW-AH-009.1 registration/alias/provenance contract. It is independently
throwable once that prerequisite design is available.

The current LSP definition feature resolves presentation-style contributors,
not character manifests or typed member identities. The rename module is an
explicit placeholder. The AW-AH-009 package nevertheless marked these original
acceptance items as separate features without specifying their ownership,
locations, edits, conflicts, or failure semantics.

This request must not redesign the implemented structural nominal identity,
enum inventories, completion details, or typed hover disambiguation unless a
concrete defect is shown.

## Required inputs and repository evidence

Attach both this request and
`arcweft-aw-ah-009.1-character-nominal-registration-alias-diagnostics-final-contract.zip`.
The original AW-AH-009 delivery is not otherwise required. The assignee must
also inspect the current checkout and the implementation note at
[`2026-07-14-aw-ah-009-character-nominal-type-identity.md`](../../implementation/2026-07-14-aw-ah-009-character-nominal-type-identity.md).

At minimum, account for these current facts:

- `arcweft-lsp::features::definition` resolves effective presentation-style
  contributors; it does not resolve character owners or members.
- `arcweft-lsp::features::rename` is an explicit placeholder and defines no
  rename capability or workspace-edit behavior.
- `arcweft-project-loader::character_manifest::load` decodes a manifest but
  does not preserve JSON token ranges.
- `LspProfile` retains a decoded `CharacterCatalog`; it does not retain a
  complete typed owner/member/provenance index.

The design must state which existing general symbol-resolution facilities can
be reused and which new typed character-specific descriptors are necessary. It
must not make `source_label()` or a repository-wide text search into a resolver.

## Fixed substrate

- A character enum identity contains a validated owner and, for variants, a
  validated owning part.
- Look, part, and variant member inventories are queried by typed identity.
- Source labels are display only and have no inverse parser.
- Renaming an import/source alias does not change nominal identity.
- Renaming `CharacterId` or `CharacterPartId` is a breaking identity change and
  must not create a runtime compatibility alias.
- LSP profiles currently retain decoded manifests but not a complete typed
  manifest-token source map.

## Required decisions

1. Exact typed symbol descriptor for character owners, look members, part
   members, and per-part variant members, including manifest source provenance.
2. How a cursor token is resolved through module qualification, aliases,
   expected `CharacterNominalType`, and local member spelling without parsing a
   label.
3. Definition targets for a character reference, look, part, and variant;
   specify URI/range behavior for JSON manifests, directory manifests, imports,
   generated manifests, and unavailable sources.
4. Rename categories and scope: source alias only, public `CharacterId`, look
   ID, part ID, and variant ID. State which files and manifest references are
   edited for each category.
5. Atomic preflight, conflict detection, edit ordering, version checks, stale
   document handling, and workspace edit limits.
6. Whether public-ID and part-ID rename is offered automatically, requires an
   explicit breaking-operation flag, or is rejected by ordinary LSP rename.
7. Structured errors for ambiguous owner/member, stale inventory, missing
   provenance, read-only/generated manifest, collision, invalid replacement,
   and partial workspace coverage.
8. Cache invalidation and project/profile rebuild after a successful edit.

## Implementation order to specify

1. Freeze typed symbol/provenance and cursor-resolution APIs.
2. Preserve manifest token ranges at the I/O adapter boundary.
3. Implement go-to-definition for owners and all member families.
4. Implement alias-only rename, then explicitly gated breaking ID/member
   renames with atomic preflight.
5. Connect session capabilities, invalidation, and deterministic workspace edits.

## Required tests

- Definition for canonical, compact, qualified, and aliased owner references.
- Definition for same-spelling look/part/variant members across characters and
  across parts, resolved only by typed context.
- Alias rename changes source spellings without changing nominal identity.
- Owner and part rename follow the documented breaking-operation policy and
  update every required manifest/project/source reference atomically.
- Invalid replacement, collision, ambiguity, generated/read-only source,
  stale version, missing file, exact edit limit, and one-over limit fail without
  partial edits.
- Multi-root and simultaneous-profile projects do not leak definitions or edits
  across nominal scopes.
- No source-text gate and no `source_label()` parsing.

### Mandatory negative and edit-atomicity matrix

For every rename category, the final contract must state the `prepareRename`
result, rename result, stable error code, edit set, and mutation behavior for:

- invalid replacement, same-name replacement, local/alias/public-ID/member
  collision, and ambiguity;
- stale document version, stale profile/inventory epoch, missing file, missing
  provenance, and partial multi-root coverage;
- generated, read-only, non-file, and dependency-owned manifests;
- canonical, compact, qualified, and arbitrary alias uses;
- owner, look, part, and per-part variant renames, including same-spelling
  members in another owner or part;
- exact URI/edit/byte budget limits and one-over cases;
- client support with and without versioned `documentChanges`.

Preflight failures must produce no partial `WorkspaceEdit`. Definition tests
must observe typed resolution results and precise URI/ranges, not repository
source locations or spellings.

## Constraints and non-goals

- No compatibility aliases or migration shims for renamed public IDs.
- No ad-hoc global text replacement.
- No manifest I/O in sema, character data, or core crates.
- Do not expand rename to unrelated Arcweft symbols in this request.

## Expected output

Produce a standalone final design with exact Rust and LSP APIs, provenance and
resolution data flow, rename decision table, atomic workspace-edit algorithm,
limits/errors, cache invalidation, implementation order, and full test matrix.

Deliver it as a new independent archive named
`arcweft-aw-ah-009.2-character-nominal-definition-rename-final-contract.zip`.
The archive must contain:

- `FINAL_CONTRACT.md` with exact typed symbol, definition, `prepareRename`, and
  rename APIs plus the final breaking-operation policy;
- `REQUIREMENTS_TRACEABILITY.md` covering this request and every consumed
  AW-AH-009.1 contract item;
- `RENAME_DECISION_TABLE.md` covering every symbol/source category;
- `IMPLEMENTATION_HANDOFF.md` with crate/file order, capability wiring,
  invalidation, removal of placeholders/bypasses, and validation commands;
- `TEST_MATRIX.md` with positive, negative, stale, multi-root, exact-limit, and
  one-over cases;
- `OPEN_QUESTIONS.md`, whose final state must say `none` or mark the delivery
  incomplete.

Do not include a whole repository, `target/`, VCS data, caches, or an
implementation patch. The contract must consume the AW-AH-009.1 registrar,
provenance, diagnostics, and revision identity directly rather than restating
or replacing them.

## Acceptance and next handoff

This task is complete only when all rename categories have one explicit policy,
all editable references have a bounded typed inventory, all failures are
preflighted atomically, and no open question can change a public LSP result or
edit. After acceptance, throw a separate implementation task with this archive
and the accepted AW-AH-009.1 archive attached. The implementation task must
land definition first, alias-only rename second, and explicitly gated breaking
renames last, without redesigning the accepted contracts.
