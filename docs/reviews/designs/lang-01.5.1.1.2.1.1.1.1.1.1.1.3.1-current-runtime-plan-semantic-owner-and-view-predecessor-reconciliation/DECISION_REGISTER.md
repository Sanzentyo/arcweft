# Decision register

| ID | Decision | State |
|---|---|---|
| D01 | `.1.2` intake/implementation precedes `.1.4` dispatch/intake/implementation; `.1.3.1` finalizes only afterward | closed |
| D02 | Current `.1.3` `READY_FOR_IMPLEMENTATION` claim is rejected | closed |
| D03 | Current builder `Arc<RuntimePlanConstructionIssuer>` plus ordinal owns task build coordinates; no global numeric token | closed |
| D04 | Runtime-plan constructs a `RuntimeTaskPlanSeed`; core builder alone constructs the private final row | closed |
| D05 | Final executable edges contain a task table index; candidate semantic edges contain an owner-checked coordinate ordinal | closed |
| D06 | Runtime-plan returns one unpublished draft so compiler can join View authority before core finish | closed |
| D07 | Compiler is the only compiler-catalog/bundle join owner; bundle never imports compiler/sema | closed |
| D08 | Bundle input may contain only actual types from legitimate shared dependencies; no byte projection is invented in Cut 5 | closed, exact types gated on `.1.4` |
| D09 | Core owns and retains the complete prefix hasher; one-use request appends View payload through one closed operation | closed, exact borrowed types gated on `.1.4` |
| D10 | Existing live View validation remains; seal-scope and digest methods extend the same protocol | closed |
| D11 | No `RuntimeControlEffectContractId` table is invented; final task row owns one inline closed contract | closed, exact variants gated on predecessor inventory |
| D12 | Returned local mutability/init/owner atoms are deleted because no final execution owner exists | closed |
| D13 | Record/case accepted identity is added to the sole core row through `.1.2`'s existing compiler/facts/lowering path | closed, exact identities gated on `.1.2` |
| D14 | Producer-function digest cannot exist until the final function row owns accepted identity/roles/modes/result/endpoints | closed, exact types gated on `.1.2` |
| D15 | Host request strings are resolved through the Cut 4 catalog; final FlowOp contains only a task reference | closed |
| D16 | Builder and purpose-built decoder push the same seeds and invoke the same sealer; expected bytes are assertions only | closed |
| D17 | One global duplicate check and final reference resolution precede one atomic publication | closed |
| D18 | Error precedence is outer predecessor/product validation, then common core preflight/structure/View scope/semantic/seal/expected/duplicate/final reference/publication | closed |
| D19 | Exact transcript tags for predecessor-owned identities/request/control/View payloads and numeric defaults remain open until intake | deliberately blocked |
| D20 | Final status cannot become ready while D19 or `OPEN_QUESTIONS.md` remains open | closed |
