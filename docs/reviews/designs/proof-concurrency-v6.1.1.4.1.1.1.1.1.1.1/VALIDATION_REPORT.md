# Validation report

Status: `READY_FOR_IMPLEMENTATION`

## Repository and source review

- latest audited main: `004ff3d69f241954eb808985878c348b165a815c`;
- complete AGENTS.md and Rust skill read;
- complete current request, primary request, previous continuation request, and
  both rejection intakes read;
- leaf/source/tail direct member ledger reused from the immediately preceding
  E13 audit and cross-checked against the 2026-07-30 intake's independent 67-row
  confirmation;
- synthetic-role repository ZIP fetched/opened directly, CRC checked, manifest
  verified, and every member hashed;
- current lexer, lossless expression/path events, semantic lexer/Pratt parser,
  attachment families/roles/kinds/access, syntax limits/budget, source byte
  owner, private HIR owner, and representative downstream consumers inspected.

## Decision validation

The package contains no unresolved alternative. It fixes one result for each
requested decision:

1. only Name/Missing final member states;
2. one central projection/attached owner;
3. Try then Select for `?.`;
4. parenthesized current-language poisoned-target producers, with compact `..`
   and ambiguous spaced-path rows excluded;
5. target `RecoveredChild` poison precedence and payload-derived member
   obligation;
6. sole source query with role/ordinal before document/revision/length;
7. zero E13 syntax-diagnostic delta and exact HIR deltas 0/1/2;
8. independent syntax expressions, identity, source, name, diagnostics, HIR
   expressions, total slots, and component arithmetic evidence; and
9. one deletion-driven switch with concrete consumer inventory.

## Mechanical package checks

The generation validator checks:

- exact request/intake Git blob hashes;
- `FINAL_STATUS.md` exact bytes and `OPEN_QUESTIONS.md` exact four bytes;
- unique safe flat member paths;
- every TSV has one fixed-width header, no malformed rows, and unique primary
  IDs where applicable;
- producer byte ranges against literal UTF-8 lengths;
- diagnostic/accounting truth-table deltas;
- predecessor audit row count and direct synthetic member hashes;
- all traceability rows are `CLOSED`;
- every non-self manifest byte length and SHA-256; and
- ZIP CRC, member inventory, and manifest recomputation.

No production Rust, Cargo file, branch, patch, PR, overlay, or extracted
workspace tree was created. Rust compilation is not applicable to this
design-only package; the included matrices specify the implementation tests.
